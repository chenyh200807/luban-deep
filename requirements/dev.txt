# ============================================
# DeepTutor Development Dependencies
# ============================================
# Install: pip install -r requirements/dev.txt
# Includes full runtime + development tooling.

-r server.txt

pytest>=7.0.0
pytest-asyncio>=0.23.0
# Per-test timeout so a hung test fails fast with a thread dump instead of
# stalling the whole gate (used by the hermetic full-tree CI job).
pytest-timeout>=2.1.0
pre-commit>=3.0.0
safety<3.0.0
bandit>=1.8.0
# JSON Schema validation used by the observability snapshot regression test
# (tests/services/observability/test_observer_snapshot.py). Declared here so the
# hermetic full-tree gate can collect it instead of skipping on a missing import.
jsonschema>=4.0.0
