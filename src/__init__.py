"""Compatibility package for legacy tests and tooling.

This repository moved its runtime code under ``deeptutor/``. Some older tests
and tooling still expect a top-level ``src`` package to exist, so we keep this
minimal compatibility shim.
"""

