# 编译资产盘点(知识编译 + 题库判分编译)

- **日期**:2026-06-16
- **范围**:`artifacts/knowledge_compiler/` + `artifacts/luban_grading_artifacts/` + `artifacts/luban_consensus_gold/` + `artifacts/luban_agentic_grading_harness/` + `deeptutor/services/{compiled_knowledge,source_compiler,taxonomy/compiled,construction_grading}` + `agent-skills/luban-rich-leaf-compiler` + `docs/plan/知识编译与检索`
- **方法**:两个专家 agent 并行深析(知识编译 / 题库判分),智能抽样 + 看 schema + 抽真实记录
- **核心修正**:之前"分点 rubric=0、专家判分=0、moat=0"是只看了 `scoring_point_assets.jsonl` 一个文件的误判。真实:**编译引擎已建 ~80%,但全冻在 shadow(published:false / runtime 默认 OFF / WEAK_GO_SHADOW_CANDIDATE)。**

---

## 一、知识编译产物(逐层 + 路径 + schema + 深度)

### A. RichLeaf 现行基座(installed runtime_supply)
- 路径:`runtime_supply/v_rich_leaf_context/rich_leaf_context_bundle.json`
- 量:**1595 records**(1612 − 17 quarantine 硬排除);schema `luban_rich_leaf_runtime_token_pack.v2.3`;源 `v3.1.1_frozen_v11_quarantine_annotated`
- 每叶:`compiled_context{concepts, exam_patterns, rules, teaching_cards, scoring_points}` + 全量 `source_ref{chunk_id, file_sha256, page_num, span_hash, source_lane}`(有教材 provenance + sha,**无 essence/不变量/canonical_logic 命名字段**)
- 状态:**`published:false`,runtime 默认 OFF**(`rich_leaf_runtime_enabled()` 仅 env flag 开才消费,否则 fall open 到 legacy);canonical_pointer `record_count:1595, quarantine_excluded_count:17`

### B. RichLeaf v3.2 采分点富化层(M35)
- 路径:`artifacts/luban_grading_artifacts/rich_leaf_v32_scoring_point_compile_20260613/runtime_token_pack_v32_scoring_points.json`
- 量:**1612 单元 / 5705 采分点**(来源拆分:chunk_assessment 1567 + knowledge_card 3933 + m35_artifact 205);schema `luban_rich_leaf_scoring_point_compile.v1`(unit 仍 v2.3)
- 每采分点:`point_id, statement, required_terms, policy_type(semantic_allowed), provenance{quote, quote_verified:true, source_authority:textbook}` —— **采分点带 verified 教材原文溯源**;verdict `PASS_SCORING_POINT_COMPILE`

### C. LLM 深编译候选(最深的因果/程序/数值层)
- 路径:`artifacts/luban_grading_artifacts/rich_leaf_full2026_llm_deep_compile_runner_20260612/llm_deep_compile_runner_deepseek_full313_merged_candidates.json`
- 量:**313 candidates**,DeepSeek 实跑(341 calls,success_rate 1.0,~21 万 completion tokens);schema `rich_leaf_deep_compile_candidate.v1`
- `compiled_fields` 覆盖率:concepts 98% / definitions 97% / rules 93% / **procedures 76% / numeric_constraints 70%** / common_mistakes 91% / grading_relevance 89% / learner_memory_event_templates 91% / exam_patterns 91%;negative_evidence 仅 2%
- 样本(资质资格叶):
  - `rules`:"取得工程设计综合资质的企业,可承接各行业、各等级的建设工程设计业务"(因果/适用范围)
  - `numeric_constraints`:"一级资质可承担单项合同额 3000 万元以上…高度 200m 以下";"二级…高度 100m 以下、建筑面积 4 万 m² 以下、单跨 39m 以下"(精确数值约束 = 案例题不变量原料)
- ⚠️ C 层按**源文件**粒度(work_order=source_corpus_gap per file),非严格 per-leaf;喂深母题前需按叶/chunk 重新归集
- ⚠️ **用 runner 产物,别用 `..._llm_deep_compile_packets.json`**(那只是 13 packets / 313 work orders 的任务单,`execution_mode: llm_packet_generation_only, provider_call_count:0`,未跑)

### D. Scoring-point assets(教材术语锚点层)
- 路径:`artifacts/knowledge_compiler/2026/scoring-point-assets-20260602/`(生产版 `scoring_point_assets_by_node.json` 18 万行;`scoring_point_assets.jsonl` 6134 行)。`pytest-scoring-point-assets/` 是 92 行 fixture,**勿当生产数据**
- schema `luban_scoring_point_assets.v0.1`:`point_type, required_terms, list_rule{mode:term_exact_match}, provenance{content_hash, quote, anchor_verified}, score_status:pending_calibration_not_official`

### E. Lecture 编译 / F. 生产 taxonomy
- E:`artifacts/knowledge_compiler/2026/lecture_compile_20260608/` 多 jsonl(teaching_cards 540 / rubric_candidates / standard_clauses / question_capsules / option_reasoning_backfill),已签发进 `runtime_supply/v_lecture_answer_skill_pack_all8`
- F:`deeptutor/services/taxonomy/compiled/construction_2026_taxonomy.compiled.json`,2112 nodes,`duplicate_codes:0 / ambiguous_codes:0`(残留 `ambiguous_names:293` 是名非 code),schema_version 1 —— 仅 code/id/keywords/level/name/path,叶 id 权威骨架,**无 compiled_context**

---

## 二、题库 / 判分编译产物(逐项 + 样本)

### 逐题判分对象(最接近"分点 rubric")
- 路径:`artifacts/luban_grading_artifacts/per_question_grading_object_full_compile_20260614/`
- `summary.json`:218 案例题,**179 编译 OK,39 隔离**,**482 采分点**,mean 2.69 点/题
- 每点结构完整:`point_id` + `atomic_official_slice`(官方答案原文切片)+ `authority_source: official_answer_verbatim` + `term_provenance[]`(逐 term 带 chunk_id/span_hash/anchor_verified 教材溯源)
- **但全部 482 点 `score: null`、`score_authority: pending_calibration_not_official`;obj 级 `per_point_score_authority` 同样全 pending**;题级 `official_total_score`(如 5.0)有,**逐点分值 = 0**
- 样本:`objects/2015__EXAM_XW2015_CASE_2__E1.json`(点 `sp_1316c3e3...`,score=null)
- ⚠️ 粒度问题:部分题 stem 含两问却只切 1 个 point;隔离原因含 `granularity_collapse` / `analysis_as_answer`

### 分点 rubric 母本(带命中判据)
- 路径:`artifacts/luban_grading_artifacts/case_rubric_structuring_m3_20260604/scoring_point_candidates.json`
- **138 点**,带 `policy_type`(list_rule/exact_required/calculation)+ `required_terms` + `list_rule.denominator` + `penalty_rule` + `needs_jury_review`;**`max_score: null`**;覆盖仅约 16-30 题(m5 输入 16 题/85 点;m5b PO 队列 30 题)

### 边界答案样本(分层完备,但非真人终裁)
- `artifacts/luban_agentic_grading_harness/multimodel_jury_gold_20260603/jury_consensus_points.json`:**131 点(107 full-consensus)**,jury_hit 分布 **hit 75 / partial 11 / miss 21**,带 `jury_score`(0.0/0.8/满分)+ 4 模型 rationale
  - 样本:Q14 P1 **partial score=0.8**(学生写"挖掘机"缺"反铲",扣 0.2);Q14 P9 **miss score=0.0**
- 合成边界答卷:`artifacts/.../m35_gold_labeling_full_patched_20260611/student_answers.jsonl`,**162 条**,每条带 `answer_quality_label`(excellent 等)+ `estimated_score_range` + `gold_score` + `gold_point_matches`(hit/partial/miss)+ `downgrade_reasons` + `adversarial_review` + `mutation_test`。**label_authority 主要是 `ai_council_directional`(154)+ `ai_governed_gold`(8)**

### 能跑的判分器(实测指标)
- 路径:`artifacts/luban_grading_artifacts/four_arm_ab_20260611/.../v5_patched_gold/report.json`
- artifact_first_llm_judge:**MAE 0.0749 / point_precision 1.0 / fail_open 0.0062**(vs legacy MAE 2.13)
- **但 `verdict_ceiling: DIRECTIONAL_SHADOW`、`quality_claim_allowed: false`、`gold_label_count: 8`**(其余 154 是 ai_council_directional = LLM 标 → 自评 leakage 风险);带分值的判分 key 真正落地在 `v5_patched_gold` 的 `compiled_47_points` + `rich_leaf_v32` 47 点级

---

## 三、壁垒命门复核(逐条裁决,带产物自述)

- **真人教师/PO 签字 = 0(证实)**:`luban_consensus_gold/model_jury_teacher_review_pilot_20260604/FINDING` 第 1 行白纸黑字「**是否真人老师? NO。无真人专家。reviewer_type=llm_jury**」;`case_rubric_po_review_m5c` 的 `po_review_queue_final.json` 全部 `po_status: "pending_po"` —— **PO(人类终裁)从未实际签字**,只排队 hand-off
- **多模型陪审判分 = 真实存在(推翻"=0")**:
  - `model_jury_teacher_review_pilot`:4 真实缓存模型(gpt55/opus48/deepseek_v4/qwen37)投票,带 verdict+score+evidence_span+dissent;**仅 3 case,12 点 needs_human**(P1/P2/P3 各 score=2)
  - `case_rubric_jury_live_m5b_20260604`:真 3-juror live jury 跑通(GPT5.5 via Codex CLI + DeepSeek + Qwen,6/6 调用成功),**仅 2 题试点**,把 published_candidate 全部下调,votes_fabricated=false
  - `case_rubric_po_review_m5c`:真 jury 复核 11 题/33 票,坐实 9 个 source-anchor dispute
  - 注:`case_rubric_jury_review_m5`(非 live 那轮)是 4 模型全 `provider_unavailable`、0 真票、`registry_emitted=false`
- **逐点分值标定 = 0**;**真实考生作答 = 0**(162 全合成);39 隔离题 + 分点粒度塌陷(一问当一点)

---

## 四、污染状态 / 版本谱系(已知历史:非判别 keyword 误链 + 重复 taxonomy code)

- **重复 code 已修干净**:生产 `taxonomy.compiled.json` `duplicate_codes:0 / ambiguous_codes:0`(残留 ambiguous_names:293 是名非 code)
- **误链已批量重锚但有残留**:v232 batch_relink `relinked 40/60, unresolved 20, ambiguous 9`,verdict `PASS_..._WITH_UNRESOLVED`;17 quarantine 单元已硬排除出 runtime 供给(loader 强制)
- **status_ledger 总裁决 `WEAK_GO_SHADOW_CANDIDATE`**,5 blockers:`manual_review_backlog:123`、`semantic_decisions_incomplete:123`、`live_provider_authorization_missing`、`learning_brain_signed_authorization_missing`、`release_governance_not_exercised`。`not_exercised`:runtime_default / canonical_truth_write / official_score / production_db_write。`production_write_count:0`
- **legacy 旧编译 `NO_GO_FOR_DIRECT_REUSE`**:123 manual review 缺 source lane(standard 52 / textbook 34 / lecture 25 / question 12)
- **版本谱系(同源演进,非竞争)**:frozen_v1(v3.0, 1534 叶)→ frozen_v11(v3.1.1 coverage_expansion +quarantine 标注)→ **v3.2 scoring_points_enriched(1612 叶 + 5705 采分点)**。**v25 是更早中间态(v2.5x),已被 v3 取代;别用 v24/v232/v26 候选**

---

## 五、活服务接口(深母题复用点)

- **加载器**:`deeptutor/services/construction_grading/rich_leaf_runtime.py` — `build_runtime_supply_bundle()` 四门验证(schema pin + safety + content_hash + 强制排除 quarantine);`rich_leaf_runtime_enabled()` 默认 OFF;缺/篡改/未开 → fall open;**rich-leaf 永不冒充缺失的 grounding**(line 301-303)
- **签名验证**:`compiled_registry_resolver.verify_bundle`((content_hash|namespace|status) 约定,各 lane 通用)
- **消费者**:`deep_question_adapter.py`、`rubric_grader_v1.py`、`open_world_diagnostic.py`、`per_question_grading_object.py`、`certified_grading_adjudication.py`、`capabilities/deep_question.py`、`compiled_knowledge/{general_knowledge,lecture_answer_methods}.py`(M34 teaching overlay,read-only,低信号 fall open)
- **编译器源**:`deeptutor/services/source_compiler/`(`scoring_point_asset_compiler.py`、`scoring_point_recall_calibration.py`、`rubric_evidence_aligner.py`、`answer_rubric_extractor.py`)
- **深母题复用**:直接读 `v_rich_leaf_context` bundle(已签名/已验证/已去 quarantine)或 v3.2 token pack,按 leaf_id 索引取 compiled_context + scoring_points。**只引用不改写**(与深母题数据标准所有权边界一致)

---

## 六、缺口与战略含义

**精确缺口(5 条,是"最后一公里 + 治理",不是"没有"):**
1. 逐点分值标定 = 0(482 点 score null + 138 母本 max_score null)
2. 真人教师/PO 签字 = 0(jury 全 LLM;po_review 全 pending_po)
3. 规模 = pilot(jury 2-3 题;governed gold 8 条)
4. 真实考生作答 = 0(162 全合成)
5. 39 隔离题 + 分点粒度塌陷(一问当一点)

**战略重框**:工作不是"从零编译",是"补最后一公里 + 用留存实验产出唯一缺的真实考生作答"——真实作答正是把 shadow 编译候选推向 reviewed / release_candidate 的关键缺料。**留存实验只把作答信号回流到既有 `learning_evidence` / review queue / compiler feedback 链路,不新增学习事实或编译 truth。**

## 七、未抽到 / 下次补
- 313 深编译 runner 与 v3.2 token pack 是否已合流(runner 是 per-file;v3.2 pack 的 rules/concepts 看似来自 frozen 链而非 deep runner,二者是否合流**未确认**)
- `rubric_compile_20260607`(仅 process_stats.json,未深读)、`blocked_point_rubric_normalization_m35` backlog 未深读
- `docs/原始数据/.../题库` 各年案例 `correct_answer` 是范文还是分点(仅从 per_question source_path 推断为范文整段切片,未逐年开)
