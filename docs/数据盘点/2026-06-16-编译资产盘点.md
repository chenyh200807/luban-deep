# 编译资产盘点(知识编译 + 题库判分编译)

- **日期**:2026-06-16
- **范围**:`artifacts/knowledge_compiler/` + `artifacts/luban_grading_artifacts/` + `artifacts/luban_consensus_gold/` + `artifacts/luban_agentic_grading_harness/` + `deeptutor/services/{compiled_knowledge,source_compiler,taxonomy/compiled}` + `agent-skills/luban-rich-leaf-compiler` + `docs/plan/知识编译与检索`
- **方法**:两个专家 agent 并行深析(知识编译 / 题库判分),智能抽样 + 看 schema + 抽真实记录
- **核心修正**:之前"分点 rubric=0、专家判分=0、moat=0"是只看了 `scoring_point_assets.jsonl` 一个文件的误判。真实:**编译引擎已建 ~80%,冻在 shadow。**

## 一、知识编译产物

| 层 | 路径 | 量 | 深度 |
|---|---|---|---|
| RichLeaf 现行基座 | `runtime_supply/v_rich_leaf_context`(源 v3.1.1_frozen_v11) | 1595 records | 每叶 compiled_context{concepts/exam_patterns/rules/teaching_cards/scoring_points} + source_ref(chunk/sha/page);**published:false、runtime 默认 OFF** |
| **RichLeaf v3.2 采分点富化** | `rich_leaf_v32_scoring_point_compile_20260613` | 1612 单元 / **5705 采分点** | 每点 statement+required_terms+policy_type + `provenance{quote, quote_verified:true, source_authority:textbook}` —— **采分点带 verified 教材原文溯源** |
| **LLM 深编译候选** | `rich_leaf_full2026_llm_deep_compile_runner_..._full313_merged_candidates.json`(DeepSeek 实跑,341 calls) | **313 叶** | procedures 76% / numeric_constraints 70% / rules 93% / common_mistakes 91% / grading_relevance 89% —— **不变量的现成原料(因果/程序/数值)** |
| 生产 taxonomy | `deeptutor/services/taxonomy/compiled/construction_2026_taxonomy.compiled.json` | 2112 nodes,duplicate_codes:0 | 仅 code/keywords/level/path,叶 id 骨架 |
| Lecture 编译 | `artifacts/knowledge_compiler/2026/lecture_compile_20260608/` | 540+ | teaching_cards/rubric_candidates/option_reasoning_backfill,已签发 v_lecture_answer_skill_pack |

## 二、题库 / 判分编译产物

| 资产 | 路径 | 实情 |
|---|---|---|
| 逐题判分对象 | `per_question_grading_object_full_compile_20260614` | 218 案例题,179 编译OK,**482 采分点**;每点 atomic_official_slice(官方答案切片)+ official_answer_verbatim + 教材溯源;**但逐点 score 全 null(pending_calibration),题级总分有、点级=0** |
| 分点 rubric 母本 | `case_rubric_structuring_m3/scoring_point_candidates.json` | **138 点**,带 policy_type(list/exact/calc)+required_terms+penalty_rule+needs_jury_review;**max_score null**;覆盖 ~16-30 题 |
| 多模型陪审 gold | `multimodel_jury_gold_20260603/jury_consensus_points.json` | 131 点(107 full-consensus),hit75/partial11/miss21 + jury_score + 4 模型 rationale(如"挖掘机缺反铲扣0.2") |
| 合成边界答卷 | `m35_gold_labeling_full_patched_20260611/student_answers.jsonl` | **162 条**,带 answer_quality_label/estimated_score_range/gold_point_matches/downgrade_reasons/adversarial_review |
| 能跑的判分器 | `four_arm_ab_20260611/.../v5_patched_gold/report.json` | artifact_first_llm_judge **MAE 0.0749 / point_precision 1.0 / fail_open 0.006**(vs legacy 2.13);**但 verdict_ceiling=DIRECTIONAL_SHADOW、quality_claim_allowed=false、governed gold 仅 8(余 154 ai_council_directional)** |

## 三、壁垒命门复核(推翻一半/证实一半)

- **真人教师/PO 签字 = 0(证实)**:`model_jury_teacher_review_pilot` 自述"是否真人老师?NO,reviewer_type=llm_jury";`case_rubric_po_review_m5c` 队列全 `pending_po` 从未真签。
- **多模型陪审判分 = 真实存在(推翻"=0")**:4 真实模型(gpt55/opus48/deepseek_v4/qwen37)投票带 verdict+score+evidence_span+dissent;`case_rubric_jury_live_m5b` 3-juror live 跑通——**但全是 pilot 规模(2-3 题)**。
- **逐点分值标定 = 0**;**真实考生作答 = 0**(162 全合成);39 隔离题 + 分点粒度塌陷(一问当一点)。

## 四、状态与当前 canonical 版

- 全系 **published:false / runtime 默认 OFF / status=WEAK_GO_SHADOW_CANDIDATE**;123 manual_review backlog + 20 unresolved relink。
- RichLeaf 用 **v3.2**(frozen_v1→frozen_v11→v3.2);深逻辑用 **runner**(实跑)非 packets(任务单未跑);taxonomy 用 `construction_2026_taxonomy.compiled.json`;scoring_point 生产版在 `scoring-point-assets-20260602/`(18 万行 by_node)非 pytest fixture。
- 活服务:`rich_leaf_runtime.py`(四门验证 + 默认 OFF + fall open,永不冒充缺失 grounding)、`compiled_registry_resolver.verify_bundle`、`source_compiler/`(scoring_point_asset_compiler 等);消费者:deep_question_adapter / rubric_grader_v1 / open_world_diagnostic / per_question_grading_object 等。

## 五、缺口与战略含义

**精确缺口(5 条,是"最后一公里+治理",不是"没有"):**① 逐点分值标定=0 ② 真人教师/PO 签字=0 ③ 规模=pilot ④ 真实考生作答=0 ⑤ 39 隔离题+分点粒度塌陷。

**战略重框**:工作不是"从零编译",是"补最后一公里 + 用留存实验产出唯一缺的真实考生作答"——真实作答正是把 shadow 编译引擎提升到 published 的那味缺料。**留存实验 = 既测留存又喂飞轮。** 复用纪律:只引用不改写,消费前过治理闸。

## 未抽到 / 下次补

- 313 深编译 runner 与 v3.2 token pack 是否已合流(per-file vs per-leaf 归集未确认)
- `rubric_compile_20260607` / `blocked_point_rubric_normalization_m35` 未深读
