# 原始数据资料盘点

- **日期**:2026-06-16
- **范围**:`/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor/docs/原始数据/`(4.6G,3094 文件:859 md / 611 png / 410 json / 95 pdf)
- **方法**:专家 agent 智能抽样(find/ls 摸结构 + json/md head 抽样 + 关键词 grep 探针);未开 PNG/PDF 二进制
- **诚实边界**:611 PNG 未开(按命名判为 docx 渲染校验图);95 PDF 未解析正文;讲义 ~335 个 page-level json 仅抽样 schema;MCQ correct_answer 计数非空但未逐题打印

## 目录结构

- `PDF/`:`{真题, 教材, 讲义, 行业标准文件, 一建建筑实务（智能体资料）_副本, 建筑实务11.20_副本}` —— 更老的素材/中间产物层
- `2026_副本/`:`{题库, 2026教材, taxonomy, 标准文件, 讲义, scripts}` + `Supabase_Schema_Evaluation_Report.md` + 嵌套 `.git` —— **最新工作集**

## 结构化资产清单

| 资产 | 路径 | 量 | 形态 |
|---|---|---|---|
| 历年真题(结构化) | `2026_副本/题库/*/FINAL_CLEANED_EXAM_V20XX.json`(11 年 2015-2025) | **337 选择 + 218 案例** | 每题 `question_data{stem,options,correct_answer,analysis,score,difficulty,logic_chain}` + `option_reasoning/pitfalls/predicted_node`,全映射 taxonomy `node_code` |
| 章节练习库 | `864考证宝典ZL/FINAL_CLEANED_ZL500.json` + `章节千题斩SMR/FINAL_CLEANED_QIANTIZAN.json` | **403 + 630 ≈ 1033 客观题** | 带答案、全节点映射,按 41-52 章节号组织 |
| 知识卡片 | `PDF/建筑实务11.20_副本/graphify-out-full-2026-textbook/cards-from-json-all/` | **651 md(manifest 650)** | frontmatter + 核心讲解 + 原子卡 + `结构化规则(条件/要求/后果)` + 易错点 + 考频星级 + `grading_keywords`;源=2026 教材 650 content_blocks |
| 规范库 | `标准文件/` | 8 部国标/行标 | 已结构化 JSON(GB50352/GB55003/JGJ120 等) |
| taxonomy | `taxonomy/FINAL_CLEANED_TAXONOMY2026.json` | L1-L6,total_node 2116 / leaf 1976 | book_derived_leaf 1681 |

## 壁垒命门逐条裁决(最关键)

- **真实考生作答(非标准答案/非 AI 生成):没有。** `学生答案/考生作答` 全库 0 命中;Supabase 的 `user_mistakes/practice_sessions/grading_results` 是空线上表 schema。
- **专家分点 rubric / 采分点级标注 / 边界答案:基本没有。** `采分点` 15 文件全是讲解旁白;`踩点/边界答案/评分参考` 0 命中;`评分标准` 仅 6 文件、2024 真题全文仅 1 次("写出3项即得3分")。
- **案例题官方评分参考:部分。** 218 案例里 **99 道带实质官方答案,但是整段范文,非分点 rubric**;约一半 `correct_answer` 是占位串待回填。

## 能就地开工 vs 缺的关键料

- **能就地开工**:① 不变量/骨架 ← taxonomy 1976 叶 + 650 知识卡结构化规则;② 客观题判分 ← ~1370 道(337+1033)带确定答案;③ 误解候选 ← 逐题 `option_reasoning/pitfalls/易错点`;④ 案例开放世界判分 grounding ← 99 范文 + grading_keywords。
- **关键缺口(唯一真壁垒)**:① 真实考生作答 = 0;② 专家分点 rubric(每点几分/边界/扣分理由)≈ 0。必须自采(真人作答)+ 自标(专家踩点),现有料补不出。

## 版本 / 重复提醒

- 当前应用版 = `2026_副本/`;`PDF/` 是更老素材层。
- taxonomy 当前版 = `FINAL_CLEANED_TAXONOMY2026.json`(其余 `_pre_*_backup_20260612/13` 是中间快照,勿用)。
- 教材用 `第二次加强/*v3_fixed.json`(content_blocks 形态),非 `第一次清洗/`。
- 真题三处重复(智能体资料副本 / 11.20 合集 / 题库结构化)——**用题库结构化 JSON**,PDF 仅 OCR 回溯。
