# Learning State Synthesis Performance Baseline

Generated: 2026-05-22
Workload: 2000 synthetic LearnerStateEvents, 20 iterations per scenario, single-threaded.
Method: ``time.perf_counter`` around ``synthesize_learning_truth``; all events held in memory; no Supabase or network I/O.

## Budget gate

- synthesize_learning_truth p95 (no window) = **109.8 ms** — PASS (≤ 200ms)
- Budget per plan §Phase -1.D: p95 ≤ 200ms on 2000 events.

## Results

| scenario | p50 (ms) | p95 (ms) | p99 (ms) | min | max | iterations |
| --- | --- | --- | --- | --- | --- | --- |
| full (no window) | 75.4 | 109.8 | 126.7 | 59.3 | 131.0 | 20 |
| window=500 | 14.2 | 19.1 | 36.7 | 12.1 | 41.1 | 20 |
| window=200 | 6.1 | 10.4 | 26.2 | 4.9 | 30.2 | 20 |

## Window flag behavior

When ``event_limit`` is set and exceeded, ``synthesize_learning_truth`` returns a projection with ``window_truncated=True``; ``synthesis_run.input_event_count`` reflects the windowed count. Read model consumers must honor this flag and show a 'covered N most-recent attempts' hint in the UI.

## Reproduce

```bash
python scripts/bench_learning_synthesis.py --events 2000 \
    --out docs/qa/$(date -u +%F)-learning-state-performance-baseline.md
```

---
Generated by `scripts/bench_learning_synthesis.py` (read-only).
