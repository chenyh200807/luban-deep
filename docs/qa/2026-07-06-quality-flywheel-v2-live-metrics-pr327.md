# Quality Flywheel V2 Live Metrics PR #327

PR #327 preserves the first V2 live verification evidence for the quality flywheel:

- `domains/quality-flywheel/LOG.md`
- `domains/quality-flywheel/metrics/accuracy.jsonl`

The payload is evidence-only and does not change runtime code. This note keeps
the PR in the documented QA surface so the required `Tests` workflow runs before
the branch is merged to `main`.
