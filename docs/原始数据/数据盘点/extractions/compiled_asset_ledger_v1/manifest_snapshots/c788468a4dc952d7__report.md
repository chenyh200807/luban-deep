# DeepTutor OA / OM / Observability 日报（2026-07-13）

生成时间：2026-07-14 09:28 Asia/Shanghai  
冻结窗口：2026-07-13 00:00:00–23:59:59 Asia/Shanghai（1783872000–1783958399）

## 结论

**Observability verdict = BLOCKED。Release readiness = HOLD。**

这次不是业务指标红，而是 release truth authority 未成立：candidate `de5c000816c4` 与 live `/metrics` runtime `954c830c7bc5` 不同，且两边都标记 `git_dirty=true`，FF snapshot 与 deploy manifest 也不同。Canonical runner 在 preflight 后以 exit code 1 fail-closed；没有发送 unified WS smoke，session/turn IDs 为空，也没有为 current candidate 拼接 daily/OA/release/benchmark/readiness。

已修好的部分必须单列：2026-07-13 的 runtime-authority hardening 在真实 mismatch 路径上生效，旧行为中“错误 runtime 仍继续拼接在线证据”的风险被挡住。剩余 blocker 是 live runtime authority 仍旧、candidate/runtime 都 dirty，以及 current candidate 没有新的 release/readiness truth。

## Tier 0 与 Git authority

- execution cwd / `pwd -P`：`/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor/artifacts`，PASS。
- git toplevel：`/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor`，PASS；`core.worktree` 为空。
- 配置 authority：父仓 `.env` 可读；`artifacts/.env` 不存在，按本轮新合同不构成 blocker，也未复制/软链 secrets。
- branch：`codex/old-blue-workspace-snapshot-20260710`；HEAD `de5c000816c4`；`origin/main` `0308b0224b56`；HEAD 相对 `origin/main` 为 68 left / 315 right，不能视为 main release candidate。
- dirty 一等信号：治理面（`AGENTS.md`、skill catalog/README、新 skill 与测试）；真实入口面（`yousenwebview/app.json`、四个 internal diagram page 删除、`project.private.config.json`）。本轮未 reset/stash/checkout/overwrite。

## Freshness / lineage

| 证据 | latest / 年龄 | 表面 freshness | authority 判断 |
| --- | --- | --- | --- |
| daily_trends | `observability-daily-1783905684` / 24.1h | 未超过 36h | 只覆盖 2026-07-12，SHA `87599ea5a0cc`，payload 自报 STALE；本冻结日缺失 |
| oa_runs | `oa-daily-1783905684` / 24.1h | 未超过 36h | SHA `87599ea5a0cc`，STALE；不是 current candidate OA |
| om_runs | `om-1783992205` / 0.1h | fresh by age | live SHA `954c830c7bc5`，WS=DEFERRED；foreign-lineage，不可用于 candidate |
| observer_snapshots | `observer-snapshot-1783905682` / 24.1h | 未超过 36h | 只覆盖 2026-07-12，SHA `87599ea5a0cc`，coverage 6/11；本冻结日未重建 |
| change_impact_runs | `change-impact-1783905684` / 24.1h | 未超过 36h | SHA `87599ea5a0cc`；之后已有 5 个 commit，scope 已漂移 |
| release_gate_runs | `release-gate-1783905684` / 24.1h | 未超过 7 天 | 旧 SHA、final_status=FAIL；之后有 release/observability commit，按合同升级 **RELEASE-P0** |
| benchmark_runs | `benchmark-1783905682` / 24.1h | 未超过 7 天 | 旧 SHA 26/26 PASS，只证明旧候选离线 spine，不清除 current lineage/true-entry blocker |
| readiness_checks | `readiness-matrix-1783905684` / 24.1h | 年龄可接受 | 旧 SHA：contract_guard PASS、Playwright FAIL、WeChat DevTools FAIL；current candidate 未评估 |

因此本轮是典型的 **fresh by age, stale/blocked by lineage**。不能因为 OM 刚更新或旧 benchmark 26/26 就写 TRUSTED。

## 昨日 commit / diff impact

旧 release gate 在 09:21 绑定 `87599ea5a0cc`；18:02–18:12 之后出现 5 个 commit，current HEAD 变为 `de5c0008`：

- `91e01e57`：收紧 release gate 与 runtime identity。
- `c7dbd018`：daily runner / observer runtime authority fail-closed。
- `02ddfce0`：覆盖 metrics surface authority precedence。
- `de5c0008`：修正 prerelease surface evidence authority。
- `f9e27fda`：广范围混合提交，包含 observability 代码但也带入大量既有资产；其审计边界不能被窄修复提交自动洗白。

这些都是 release/observability 相关变更，却没有 current-SHA release gate，因此 RELEASE-P0 成立。当前 dirty 的 `yousenwebview` 真入口文件进一步要求 current-scope Playwright 与 real WeChat DevTools evidence，旧 readiness 不能继承。

## OA / OM / benchmark / release evidence

### OM

本轮新 OM `om-1783992205` 只是旧 runtime 辅助遥测：ready=true、6/6 turns completed、0 failed、SLO compliance 0.8、p95 proxy 4.0576s；HTTP 84 次中 16 次 401、errors_total=0。这些数字只描述 `954c830c7bc5` 本地进程，不描述 candidate。usage / token / provider summary 仍为空，成本与 provider 负载无法下结论。

值得保留的正证据：unified WS smoke 明确 `DEFERRED`，未产生 synthetic session/turn；没有用 token fallback、TestClient 或 artifact 覆盖伪造 same-SHA。

### OA 与 raw linkage

没有 current candidate OA。旧 OA `oa-daily-1783905684` 绑定旧 SHA、verdict=STALE，raw evidence bundle 只有旧 run IDs，不能给 2026-07-13 当前候选做因果闭环。

冻结日直接只读核验：backend log 45 行，无 ERROR/WARN，也没有 `trace_id` / `turn_id`；turn-event JSONL 不存在；chat_history 的 sessions/messages/turns 均为 0；product_behavior DB events=0；Langfuse 无可连接 trace。这说明“无错误日志”不等于“链路健康”，因为关键 join keys 与事实源根本未形成。

### Surface / true entry

旧 runtime metrics 看见 7 条 accepted `wechat_yousenwebview` events（5 module_viewed、1 learning_action_started、1 retest_item_answered），但 surface coverage 数组为空，event metadata 的 release identity 为空，持久化 product_behavior DB 同窗口仍为 0。它证明 producer 曾到达旧进程，不证明 current release、真实微信包验收或持久化观测闭环。

### Benchmark / release gate

旧 benchmark 三套 suite 共 26/26 PASS；旧 release gate 仍为 FAIL/hold，blockers 是 runtime dirty、Playwright evidence missing、WeChat true-entry pending、artifact stale vs HEAD。由于 current candidate 在 gate 之后变化，这些旧 blocker 只能作为背景，不能替代 current-SHA gate；当前最上层 blocker 是 runtime authority mismatch + current gate absent。

## P0 / P1 / P2

- **RELEASE-P0**：live runtime 与 candidate SHA/manifest/FF snapshot 不一致，且两侧 dirty；runner 已停止 current evidence assembly。
- **RELEASE-P0**：5 个 release/observability commit 晚于 latest release gate，current SHA 无 release gate。
- **RELEASE-P1**：current scope 涉及 dirty `yousenwebview` 真入口文件，但 current-SHA Playwright / real WeChat DevTools readiness 缺失。
- **AUTOMATION-P1**：BLOCKED preflight 仍推进 `om_runs/latest` 到 foreign-lineage OM。虽然 WS 正确 DEFERRED，但 `latest` 消费者仍可能把“刚写入”误读成 current freshness。
- **OBS-P1**：5 层 blind spot 持续存在：turn event、surface coverage、recent conversations、product behavior persistence、Langfuse trace linkage；另有 usage/provider summary 缺失。
- **OBS-P2**：旧 runtime 84 个 HTTP 请求中 16 个 401，而 errors_total=0；保留为 auth-surface 信号，不升级成业务 incident。

## Persistence continuity

按不含 SHA/run_id/release_id 的稳定 key、以唯一 report_date 计数：

- `runtime_authority_mismatch`：2026-07-12 → 2026-07-13，consecutive_count=2。
- `missing_true_entry_evidence`：2026-07-12 → 2026-07-13，consecutive_count=2。
- `observer_five_layer_blindspots`：2026-07-12 → 2026-07-13，consecutive_count=2。
- `foreign_om_latest_advanced_on_block`：首次可证 2026-07-13，consecutive_count=1。
- `observer_dailytrend_prior_wrapper`：代码路径已改，但本轮 same-SHA rerun 被 preflight 阻断，continuity=UNKNOWN，不能宣称 evidence-level resolved。

在修正后的自然日 continuity 合同下尚无可证明的连续 3 日项，因此本轮不靠猜测追加“三连”升级；现有 P0/P1 来自当前风险本身。

## 建议动作（按最小闭环排序）

1. 先由 release owner 明确一个 clean candidate authority；当前 battlefield 68/315 分叉且 dirty，不应直接被当发布候选。
2. 让 live runtime 运行该 clean candidate 的 exact SHA + FF snapshot + deploy manifest，且 runtime/candidate 均 clean；之后再从 artifacts cwd 重跑同一日报。
3. 修正或治理 BLOCKED 路径的 OM store 行为：要么不推进 `om_runs/latest`，要么所有消费者强制按 candidate lineage 过滤 foreign OM。
4. same-SHA 后按 `change_impact.required_readiness_checks` 重新生成 current-SHA contract_guard / Playwright / real WeChat DevTools evidence；浏览器/DevTools 前必须先做 process guard。
5. 收口 7 条 runtime surface events 到持久化 product behavior + release identity + coverage，并恢复 turn-event / chat / Langfuse join keys；否则 OA 只能报告“未见”，不能解释“为何”。

## 下一步最小 prompt

> 在 `/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor/artifacts` 只读复跑 2026-07-13 observability daily；先证明 live `/metrics` 与 clean candidate 的 git_sha、ff_snapshot_hash、deploy_manifest_hash、git_dirty 全部匹配。只有 preflight PASS 才允许 unified WS；随后直接审计 current-SHA daily/OA/OM/release/benchmark/readiness inner payload，并确认 BLOCKED 路径不再把 foreign OM 推成可误读的 latest。

## 边界

本轮未修复、未部署、未 SSH、未写生产 Supabase、未运行浏览器/Computer Use/Next、未 kill 进程、未发送 unified WS smoke。
