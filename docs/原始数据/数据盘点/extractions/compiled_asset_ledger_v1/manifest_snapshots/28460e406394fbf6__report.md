# DeepTutor OA / OM / Observability 日报

- report_date：`2026-07-14`（Asia/Shanghai）
- frozen window：`1783958400..1784044799`
- run_at：`2026-07-15 09:22-09:28 +08:00`
- Observability verdict：**BLOCKED**
- release verdict：**RELEASE-P0 / HOLD**

## 一句话结论

今天不能把 OA、OM、benchmark 或 release gate 当成 current-candidate 工程真相：canonical runner 在读取 live `http://127.0.0.1:8001/metrics` 时直接 `Connection refused`，TestClient fallback 正确禁用；没有 current-candidate runtime identity，就没有资格运行 unified WS、online ARR/benchmark，也没有资格拼接下游日报。现有 latest 大多已超过 36 小时，唯一年龄较新的 OM 又绑定旧 dirty SHA `954c830c7bc5`，不是当前 candidate `de5c000816c4`。

## Tier 0 authority

| 检查 | 结果 | 证据 |
|---|---|---|
| `pwd -L` / `pwd -P` | PASS | 均为 `/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor/artifacts` |
| git toplevel | PASS | `/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor` |
| `core.worktree` | PASS | 空 |
| 配置 authority | PASS | 父仓 `.env` 可读；`artifacts/.env` 不存在且不作为第二 authority |
| branch / HEAD | WARN | `codex/old-blue-workspace-snapshot-20260710` / `de5c000816c417ac777f38974d7e81585a6d0560` |
| `origin/main` | WARN | `416c59d813abc3c614007b03925f9af13c791ca8`；`HEAD...origin/main = 68 left / 347 right` |
| candidate identity | BLOCKED | `git_sha=de5c000816c4`, `ff_snapshot_hash=a638f39bf909`, `deploy_manifest_hash=local-662fa6eb8885`, `git_dirty=true` |

## Runner 与 runtime authority

- 命令：`python "$repo/scripts/run_observability_daily.py" --output-dir tmp/observability/control_plane`
- `runner_exit_code=1`
- 失败点：GET `http://127.0.0.1:8001/metrics` → `httpx.ConnectError: [Errno 61] Connection refused`
- `lsof -iTCP:8001 -sTCP:LISTEN` 无监听者。
- 当前没有生成新的 `runtime_authority_preflight.json`；`tmp/.../runtime_authority_preflight.json` 的 mtime 是 `2026-07-14 09:23:25+0800`，属于上次运行，不得冒充今天证据。
- online boundary：`unified_ws_smoke=DEFERRED`；没有 session/turn；online ARR、online benchmark、current OA/release/readiness assembly 均未运行。

## Freshness 与 lineage

审计时刻约 `2026-07-15 09:25 +08:00`。

| store | latest run | age | lineage | 判定 |
|---|---|---:|---|---|
| daily_trends | `observability-daily-1783905684` | 48.07h | `87599ea5a0cc`，窗口仍是 2026-07-12 | **OBS-P1 / stale** |
| oa_runs | `oa-daily-1783905684` | 48.07h | `87599ea5a0cc` | **OBS-P1 / stale** |
| observer_snapshots | `observer-snapshot-1783905682` | 48.07h | `87599ea5a0cc` | **OBS-P1 / stale** |
| change_impact_runs | `change-impact-1783905684` | 48.07h | `87599ea5a0cc` | **OBS-P1 / stale** |
| readiness_checks | `readiness-matrix-1783905684` | 48.07h | `87599ea5a0cc` | **OBS-P1 / stale** |
| om_runs | `om-1783992205` | 24.03h | 旧 runtime `954c830c7bc5`, dirty | 年龄未超阈值，但 **foreign-lineage / inadmissible** |
| release_gate_runs | `release-gate-1783905684` | 48.07h | `87599ea5a0cc` | 未超 7 天，但其后有 5 个 release/observability commit，**RELEASE-P0** |
| benchmark_runs | `benchmark-1783905682` | 48.07h | `87599ea5a0cc` | 未超 7 天，但不是 current candidate evidence |

## 昨日 commit / diff 与 dirty state

- 2026-07-14 Asia/Shanghai 自然日内没有 commit。
- 最后 gate SHA `87599ea5a0cc` 到当前 HEAD 有 5 个 2026-07-13 晚间提交：`f9e27fda`、`91e01e57`、`c7dbd018`、`02ddfce0`、`de5c0008`。它们覆盖 contract 简化、runtime authority、release gate、surface metrics precedence 与 prerelease surface evidence；旧 release gate 未覆盖这些提交。
- 当前 tracked diff：18 个文件，`513 insertions / 1031 deletions`；另有大量 untracked 资产。
- dirty 分组：
  - 治理/skill：`AGENTS.md`、`agent-skills/README.md`、`agent-skills/catalog.yaml`、新 `luban-zip-practice-extractor` 与相关测试。
  - 鲁班动画资产：`P40_N03` 大量修改/删除，以及新 `P40_B02`、`P40_D14`、`P40_N02`、分段音频与截图。
  - real WeChat surface：`yousenwebview/app.json`、`project.private.config.json` 修改，内部 `diagramWebviewTest` 四个页面文件删除。
- 这些 dirty files 只作为发布风险记录；本次未 reset、stash、checkout 或覆盖。

## OA / OM / benchmark / release gate 真相

### OA

- 没有 2026-07-14 current-candidate OA。
- latest OA 是 48.07h 前的 `oa-daily-1783905684`，并把 foreign OM 作为 stale input；其中 `root_causes=[]` 不能解释为“无根因”，只能解释为“证据链不够”。
- 它记录 5 个盲区：turn event log、surface coverage、recent conversations、product behavior、Langfuse trace linkage。

### OM

- latest OM `om-1783992205` 来自旧 dirty runtime `954c830c7bc5`，unified WS 为 `DEFERRED`。
- 旧 runtime 曾看到 7 个 accepted `wechat_yousenwebview` surface events，但全部 `release_id` 为空、coverage 为空，发生时间早于本报告冻结窗口，不能归因到 current candidate 或 2026-07-14 产品使用。
- 旧 OM 的 84 个 HTTP 请求中 16 个为 401；`errors_total=0`，因此只能保留为 auth-surface 信号，不能直接宣告 incident。
- usage/token/provider 汇总仍不存在，无法下成本与 provider 负载结论。

### Benchmark

- 旧 benchmark 为 26/26 PASS，覆盖 `pr_gate_core`、`regression_watch`、`real_exam_quality_spine`。
- 但它绑定 `87599ea5a0cc`、`git_dirty=true`、没有 runtime evidence links，且早于 5 个关键提交；只能证明旧 artifact 的离线质量，不证明 current candidate 或 live release。

### Release gate / readiness

- 旧 release gate 本身已是 `FAIL / hold`，blockers：`runtime_release_dirty`、`playwright_evidence_missing`、`wechat_devtools_true_entry_pending`、`artifact_release_stale_vs_head`。
- 旧 change-impact 要求 `contract_guard + playwright + wechat_devtools`；readiness 只有 contract guard PASS，Playwright 与真实微信 DevTools 均 FAIL。
- 当前又有未纳入 change-impact 的 `yousenwebview` dirty 变化，因此 Web harness、Playwright、DevTools project-open、real WeChat package 仍必须分层，绝不能互相替代。

## 2026-07-14 冻结窗口直接原始审计

| 原始面 | 结果 | 正确解释 |
|---|---|---|
| turn event log | `turn_events_2026-07-14.jsonl` 不存在 | writer/ingestion 证据缺失，不是 0 failure |
| chat history | sessions=0, messages=0, turns=0 | 无法做 chat/trace linkage |
| product behavior | events=0, users=0, visits=0 | 无法证明真实产品使用或退出路径 |
| backend log | 9 行，全为 INFO；0 warning/error，字段无 trace id | 仅证明少量 runtime lifecycle 日志，不能证明业务健康 |
| Langfuse | 冻结窗口无 trace id 可连接；旧 snapshot 显示 disabled | 无端到端 trace truth |
| live metrics | 当前端口拒绝连接 | runtime identity 与实时 SLO 均不可证明 |

## Persistence continuity

| stable gap | first_seen | last_seen | consecutive_count | 升级 |
|---|---|---|---:|---|
| `live_runtime_authority_unverified` | 2026-07-12 | 2026-07-14 | 3 | **AUTOMATION-P1**；前两天是 identity mismatch，今天是 endpoint unavailable，但共同业务事实仍是 live authority 未通过 |
| `missing_true_entry_evidence` | 2026-07-12 | 2026-07-14 | 3 | **RELEASE-P1 / AUTOMATION-P1** |
| `observer_five_layer_blindspots` | 2026-07-12 | 2026-07-14 | 3 | **OBS-P1 / AUTOMATION-P1** |
| `daily_payload_missing_for_report_date` | 2026-07-13 | 2026-07-14 | 2 | WATCH；latest window 仍停在 2026-07-12 |
| `foreign_om_latest_lineage` | 2026-07-13 | 2026-07-14 | 2 | WATCH；今天没有新 OM，但 latest 仍是 foreign lineage |
| `live_metrics_endpoint_unavailable` | 2026-07-14 | 2026-07-14 | 1 | 新 signature；不与昨天 SHA mismatch 混写 |
| `observer_dailytrend_repair_evidence` | UNKNOWN | 2026-07-14 | UNKNOWN | 无 same-SHA rerun，不能宣告 repaired-at-evidence-level |

## P0 / P1 / P2

### P0

1. **RELEASE-P0：current runtime truth 不存在。** live metrics endpoint 不可达，candidate dirty，current release payload 未生成。
2. **RELEASE-P0：release gate stale by lineage。** 最后 gate 之后有 5 个 release/observability commit，并且当前真实微信 surface 仍 dirty。

### P1

1. **OBS-P1：daily/OA/observer/change-impact/readiness 超过 36h。** latest window 落后两个自然日。
2. **AUTOMATION-P1：runtime authority 连续 3 个 report_date 未通过。** 今天的具体 failure signature 已从 SHA mismatch 变为 endpoint unavailable。
3. **OBS-P1：五层观测盲区连续 3 天。** turn log/chat/product/surface coverage/Langfuse 没形成可 join 的 evidence chain。
4. **RELEASE-P1：真实 Playwright / WeChat DevTools 证据连续缺失。** 不能用 `/wechat-harness`、截图或 project-open 代替 real package/auth-chain。

### P2

1. 旧 OM 的 401 auth-surface 比例需要在 same-SHA runtime 恢复后再判定，当前不可比较。
2. OM 缺 usage/token/provider summary，成本与 provider load 仍是未知面。

## 建议动作

1. **Runtime owner：**先确定一个 clean candidate authority，再让 `127.0.0.1:8001` 明确运行该 candidate；验收要求 live provenance、SHA、FF hash、deploy manifest、candidate/runtime clean 六项同时 PASS。不要随手启动 demo server。
2. **Release owner：**裁决当前 old-blue branch 与 `origin/main` 的 68/347 分叉，建立干净 release candidate；不要在当前 dirty battlefield 直接宣布 release-ready。
3. **Observability owner：**same-SHA 恢复后第一件事是重跑 canonical daily，并验证 current report_date payload；随后逐层证明 turn-event writer、chat trace、product behavior、surface coverage、Langfuse linkage，不要只刷新 latest wrapper。
4. **Web/WeChat owner：**因 `yousenwebview` 有 current dirty change，先做 memory/process guard，再分别补 Playwright 与 `real_wechat_package` DevTools/auth-chain 证据；Web harness 只保留 shadow 等级。

## 下一步最小 prompt

> 从 `/Users/yehongchen/Developer/CYH_2/Markzuo/deeptutor/artifacts` 开始，只做 runtime/release authority 恢复：先只读裁决 clean candidate SHA 与当前 old-blue/origin-main 分叉，不碰用户 dirty files；再让受管 `127.0.0.1:8001` 的 live `/metrics` 同时满足 candidate 的 git_sha、ff_snapshot_hash、deploy_manifest_hash、deployment_environment 且双方 clean。通过后重跑 `python "$repo/scripts/run_observability_daily.py" --output-dir tmp/observability/control_plane`，直接核 payload lineage；未通过则保持 BLOCKED，禁止 TestClient、demo server、synthetic WS 或字段覆盖。
