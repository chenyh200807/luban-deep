# OKF 落地差距报告 v0

## 范围

本报告比较三件事：

1. `case_rubric_canonical.json` 中可作为 OKF rubric candidate 的目标范围。
2. `json_source_ledger_v0` 中已登记的清洗 JSON 原料范围。
3. 当前 `okf_rubric_pilot_v0` 已生成的 OKF-like source-layer pilot 范围。

生成产物：

- `docs/原始数据/数据盘点/extractions/okf_landing_gap_v0/report.json`
- `docs/原始数据/数据盘点/extractions/okf_landing_gap_v0/report.md`

复现命令：

```bash
python docs/原始数据/数据盘点/scripts/build_okf_landing_gap.py --generated-at 2026-06-19T00:00:00+08:00
python -m pytest tests/scripts/test_okf_landing_gap.py -q
```

## 关键发现

| 项目 | 目标 | 当前 OKF candidate scope | 差距 |
|---|---:|---:|---:|
| cases | 25 | 25 | 0 |
| rubrics | 117 | 117 | 0 |
| scoring points | 431 | 431 | 0 |

按年份目标：

| 年份 | cases | rubrics | scoring points |
|---|---:|---:|---:|
| 2021 | 5 | 25 | 85 |
| 2022 | 5 | 21 | 110 |
| 2023 | 5 | 23 | 89 |
| 2024 | 5 | 25 | 67 |
| 2025 | 5 | 23 | 80 |

`json_source_ledger_v0` 已登记 `383` 个清洗 JSON，其中真题清洗 JSON `11` 个，足够作为下一轮 source alignment 的 raw evidence 入口。

## Authority Guard

报告状态为：

```text
source_layer_target_matched
```

所有输出继续保持：

```json
{
  "runtime_consumable": false,
  "canonical_write_allowed": false,
  "learner_truth_write_allowed": false,
  "gbrain_write_allowed": false,
  "production_registry_write_allowed": false,
  "official_score_allowed": false
}
```

## 结论

当前 OKF 不是“失败”，而是 source-layer 目标已经落地。`okf_dry_consumer` 已完成：当前 compiled inspection pack 可以被读取，并且不会写 runtime / canonical truth。

已完成的中间件：

1. `okf_dry_consumer`：证明 compiled inspection artifacts 可以被读取，同时拒绝 runtime / canonical write。
2. `ledger_to_okf_source_alignment`：把 `exam_cleaned_json` 与 rubric candidate 的 source path、case/sub-question、page/chunk 对齐。
3. `expand_okf_candidate_scope`：生成 25 cases / 117 rubrics / 431 scoring points 的 source-layer candidate artifacts。

下一步进入签发前验证，而不是继续扩格式：source/STORM review ledger、deterministic schema validator、adversarial scoring guard、signed runtime_supply candidate、shadow consumer。
