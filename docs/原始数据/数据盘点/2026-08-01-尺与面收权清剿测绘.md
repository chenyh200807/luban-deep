# 尺与面收权清剿测绘（2026-08-01）

> 病理：**病 A（第二把尺子）**同一事实被多套独立实现测量；**病 B（错误的面）**消费者拿组装后的
> turn 信封（`[History Context]` / `### 局部工作记忆投影` / `## 参考证据` + `## 当前用户问题`，
> 由 `services/session/turn_runtime.py:5891-5907` 拼装，**随账号历史逐轮变化**）当"学生这轮的
> 真实提交"用。后者的毒性在于：**离线用干净存档永远复现不出来**。
>
> 权威：
> - 提交面 = `AgentLoop._case_submission_surface`（loop.py:1987）：
>   `raw_user_message` → `_extract_current_user_question_section` → 原串
> - 小问计数 = `_extract_case_question_titles_for_scope`（loop.py:334）→
>   `rubric_grader_v1._extract_case_question_titles`（先按 `CASE_ANSWER_MARKER_PATTERN` 切作答）
> - 作答标记 = `CASE_ANSWER_MARKER_PATTERN`；分数写者 = `finalize_case_score`
>
> 今日已修五张脸：#631 判分 ctx / #631 直批探针 / #638 入口安全闸主语 / #641 narration 尺 /
> #642 narration 面。本文件登记**剩下的**。

判定口径：**语义主语**是"学生这轮真实提交"→ 必须过 surface 权威；是"整段组装消息（含系统
注入上下文）"或零判据权力 → 豁免并写明理由。不一刀切。

**结论摘要：病灶 12 处（已全部收权），豁免 9 处（已具名登记），残留缺口 2 处（登记未改）。**

---

## 一、病 B（错误的面）

### 1.1 `deeptutor/tutorbot/agent/loop.py` —— 病灶 6 处

| # | 位置 | 表达式 / 函数 | 判定与理由 |
|---|---|---|---|
| B1 | 2654（原） | `looks_like_practice_generation_request(current_message)` @ `_run_case_grading_direct` | **病灶**（已登记 task#23）。判据是纯子串测试（`question_followup.py:3592`，marker 含"下一题/出题/再来一道"共 24 条）。包装里注入的旧题干/解析/工作记忆投影只要含任一 marker → **整卷提交被踢出直批判分链**。→ 过 surface。 |
| B2 | 2848（原） | `looks_like_practice_generation_request(user_message)` @ `_case_grading_no_authority_score_fallback` | **病灶**（B1 镜像）。finalize 链四个调用点（5180/5245/5315/5409）一律传 `user_message=current_message`。同 marker 污染 → 无权威轮的判分降级兜底被静默跳过。→ 函数头收面（同时覆盖下游 `build_case_grading_diagnostic_only_response`）。 |
| B3 | 2728-2734（原） | `re.findall(CASE_ANSWER_MARKER_PATTERN, str(current_message))` @ 直批探针 | **病灶**。同函数上一行的 stem/answer 探针已在 #642 收面，**唯独 marker 计数还在数组装面**。该探针是"同题面不同作答走不同通道"的判别位——面不对＝**观测说谎**。→ 复用同一 `_probe_surface`。 |
| B4 | 1139/1141 | `looks_like_free_text_mcq_answer_request` / `_extract_answer_letter_claim` @ `_should_guard_degraded_exact_answer_claim` | **病灶**。主语是"学生**这轮**声明的答案字母"。包装里旧轮的"我选 A、C"被抽成本轮 claim → 降级闸对着**上一题的字母**吐"我不能确认或否定 A、C"。→ 函数头收面。 |
| B5 | 1191/1206/1210 | 同族 @ `_degraded_mcq_grading_response` | **病灶**（同族）。→ 函数头收面。 |
| B6 | 1434-1435 | 同族 @ `_should_suppress_stream_for_degraded_answer` | **病灶**（同族）。此处决定**要不要抑制流式**——面错了纯案例轮吐字被误抑制。→ 收面。 |

### 1.2 `deeptutor/capabilities/deep_question.py` —— 病灶 4 处

该文件在 4419 自建了一个**弱化版** surface（`metadata.get("raw_user_message") or context.user_message`，
缺 `[User Question]` 剥离器那一跳），且有三处根本绕过它直接吃 `context.user_message`。

| # | 位置 | 表达式 / 函数 | 判定与理由 |
|---|---|---|---|
| B7 | 4419-4421 | `raw_user_message = metadata.get("raw_user_message") or context.user_message` @ `run` | **病灶（口径不足）**。metadata 缺 `raw_user_message` 的入口（非 turn_runtime 通道）会静默退回信封，而这一个变量是 §E1-E7 全部下游判据的面。→ 收敛到新的 `_submission_surface(context)`（与 `_case_submission_surface` **逐字同口径**，剥离器懒导入复用同一实现，不造第二个剥离器）。 |
| B8 | 3906 | `resolve_general_knowledge_context(getattr(context, "user_message"))` @ `_maybe_attach_general_knowledge_context` | **病灶**。教学包解析的主语是"学生这轮问了什么"；跨轮 `[History Context]` 会把上一轮话题解析成本轮教学包。→ `_submission_surface`。 |
| B9 | 4687 | `user_message=context.user_message` @ `_prefer_followup_without_semantic_decision` | **病灶**。同一分支上下两个兄弟调用（4679/4698）早已用 `raw_user_message`，**唯独这行喂信封**。该判据的 marker 集含裸 `"?"` 与 `"为什么"`——挂了 `[History Context]` 之后它实际**恒真**。→ `raw_user_message`。 |
| B10 | 4753 | `raw_topic = overrides.get("topic") or context.user_message` @ `run` | **病灶**，且**与既有合同字面冲突**。`raw_topic` 下游喂 8 个判据（域门 `practice_generation_topic_domain_status`、锚点排除、出题意图、轻量 topic 门…），而 `contracts/turn.md §出题考点单一 decider` 明写「只许喂**单条用户消息**，禁喂 transcript / history 拼接文本」。4965 处的行内注释甚至自称在判"用户自己的话"——**声称与实际输入不符**。→ `_submission_surface`。 |

### 1.3 `deeptutor/runtime/orchestrator.py` —— 病灶 2 处（超出原始四文件范围，同一条链）

| # | 位置 | 表达式 / 函数 | 判定与理由 |
|---|---|---|---|
| B11 | 1035 | `resolve_submission_attempt(context.user_message, qctx)` @ `_prepare_question_submission_context`（action 分支） | **病灶**。作答解析的主语是"学生这轮交了什么"。下游 `_extract_explicit_option_letter_submission` 用的是**无锚 `re.search(r"我选([A-E])")`**——信封里旧轮的"我选B"会被采成本轮作答。本类**早有单一权威** `_routing_user_message`（lifecycle scene 入口已用），这两处漏了。→ `_routing_user_message`。 |
| B12 | 1062 | 同上（主分支） | **病灶**（同族）。 |

### 1.4 豁免（语义主语确实是整段信封，或零判据权力）

| # | 位置 | 豁免理由 |
|---|---|---|
| E1 | loop.py:1340 | `"【题目】" in user_message …` 整个表达式只进 `logger.debug` 的格式化参数，**零门控权力**。升格为判据时必须重判。 |
| E2 | loop.py:3561 | `_resolve_tool_query` **本身就是一个 surface 解析器**（先试 raw_user_message/user_visible_content… 再退到剥离器），是权威同族，不是消费点。 |
| E3 | loop.py:4209 | `_select_progressive_skill_names`：决定"这轮把哪些 skill 装进 prompt"。多装一个 skill 无破坏性（不夺判分权、不改路由终点），且注入上下文出现出题语汇时把 deep-question 备着是合理的。**主语确实是整段**。 |
| E4 | loop.py:1328 | `normalize_anchor_terms_in_response`：主语是"这段对话里用户用过的术语"（跨轮锚点归一化本就该看上下文），纯拼写归一化，零判分/路由权力。 |
| E5 | loop.py:1358 | `_content_truth_guard(user_message=…)`：下游 `teaching_modes.content_truth_guard_response` **根本不读 `user_message`**（死参数），真值全由 `standard_evidence_text` 裁决。 |
| E6 | loop.py:5050 | `classify_tutorbot_user_input(persist_user_content)`：#638 已收权。残留见 R1。 |
| E7 | orchestrator.py:768 | `build_question_lifecycle_clarification_context(context.user_message)`：澄清卡 topic 回显，只当文本给学生看，零判据权力。 |
| E8 | orchestrator.py:1316 | `Event(user_input=context.user_message)`：EventBus 观测载荷。 |
| E9 | `question_lifecycle_skills.py` / `question_followup.py` **全模块** | 两个文件的函数**全部只拿字符串形参**（`question_followup.py` 连 `metadata` 入参都没有），信封纪律**归调用侧**。主路径已收：`orchestrator._select_capability` 用 `_routing_user_message(context)` 构造 `SimpleNamespace` 再喂 `resolve_question_lifecycle_scene_decision` → `derive_question_lifecycle_scene`，**lifecycle scene 全族在入口就已经看的是干净面**。这是正确的架构（调用侧单点收口），不是病灶。 |

> ⚠️ E9 的重要限定：这两个文件里确实存在大量对信封**极其敏感**的写法（锚定 `fullmatch` 在挂
> 信封后**静默失效**；裸 `in` 判据在挂信封后**恒真**），一旦将来有人**新开**一个不走
> `_routing_user_message` 的调用方，整族判据会同时失灵且无声。因此它们虽非当前病灶，仍进
> scanner 的扫描目标（当前登记为空集）。

---

## 二、病 A（第二把尺子）

| # | 位置 | 表达式 | 判定与理由 |
|---|---|---|---|
| A1 | loop.py:2622-2628（原） | `re.findall(r"(?:^\|\n)\s*(\d+)\s*[.．、]\s*([^\n？?]{2,40}[？?]?)", user_stem)` @ `_case_grading_live_preview_text` | **病灶（已删）**。#641 收权后**残留在 fallback 分支**的第二把尺子。<br>**实测分叉（非推断）**：权威的序号闸是 `[1-9]\d{0,1}` 且 `idx<=30`，fallback 用裸 `\d+`。题面为 `31./32./33.` 时权威返回 **0**、fallback 返回 **3**；`01./02.` 时权威 **0**、fallback **2**。即：开场白会报一个**判分分母根本不会用**的数字——正是 #641 要治的"同一事实两把尺子"从 fallback 复发。<br>**修法**：删除，回落"不报数"分支。权威数不出来时**不报数**——报错数比不报数坏。 |
| A2 | loop.py:1685 | `re.search(r"(?:^\|\n)\s*\d+\s*[.．、]", text)` @ `_has_current_question_anchor` | **豁免**。只判"这段文本里**有没有**问题锚点"（布尔存在性），不产出小问数、不当分母。**不同事实**，非第二把尺子。 |
| A3 | `case_output_policy.py:329-334` | `len(re.findall(r"问题\s*[:：]?\s*\d\|第\s*\d+\s*问\|…", raw)) >= 2` @ `case_submission_stem_candidate` | **豁免（登记观察）**。自带一套 `问题N/第N问/N.` 正则并做 `>=2` 计数，与权威的 alternation 不同（无 `（N）`、不切作答标记）。但它**只被当布尔形状闸用**（"这看起来像可判分的案例作答吗"），不产出分母、不进任何得分/覆盖率计算。若将来有人把这个计数当小问数用 → 立即升格为病灶。 |
| A4 | `per_question_grading_object.py:93/175/219`、`blueprint_service.py:32/1138` | 各自的 `问题N` / `N.` 正则 | **豁免（不同主语）**。前者切**官方答案**（产出的是 covered 分子，不是题面分母）；后者是 assessment blueprint 的"是不是多问题干"形状闸。都不是"这份题面有几个小问"这同一事实。 |
| A5 | `question_followup.py:1765` / `response_mode.py:186` / `question_turn_policy.py:678` | 三份独立的 `第\s*([0-9一二…]+)\s*[题问]` 正则 | **豁免（登记观察，超出本次范围）**。`第N题` 定位族有三份实现且分歧真实（`question_turn_policy.py:678` 的 `[题问]?` 尾部可选，会匹配裸"第3"）。这是**另一条独立的收权战役**（作答定位族），与判分链的尺/面病同型但不同事实；不在本次一次收权范围，交 owner 排期。 |

---

## 三、残留缺口（本轮不改，登记）

| # | 位置 | 说明 |
|---|---|---|
| R1 | loop.py:4877 / 5050 | `persist_user_content = raw_user_message or current_message` 与 `_case_submission_surface` 是**两套"学生真实提交"解析**：raw 存在时一致；raw 缺失（CLI / 无 raw 通道）时前者直接退回信封，**#638 的吸收态 SEV 在该通道仍可复现**。#638 的回归测试显式钉住了这一行为（"CLI / 无 raw_user_message 的通道行为逐字不变"），改它属于**改既有裁决**，超出本次收权范围。→ 交 owner 拍板。 |
| R2 | `deep_question.py` 案例路径的分母 | `deep_question` 从不写 `user_stem` / `case_stem_subquestion_count`（只读，写者只有 `loop._build_v1_case_ctx`）。在 deep_question 自持的案例路径上，`_sub_n` 塌缩成 `len(_case_reference_subquestions(ctx))` —— 一个**纯参考答案侧**的分母，**没有任何题面交叉核对**。这不是"面"或"尺"的问题，是**分母权威在该路径上缺席**。→ 独立议题，交 owner 排期。 |

---

## 四、防复发闸（诚实分层）

| 层 | 形态 | 挡得住 | 挡不住 |
|---|---|---|---|
| 语义层 | `tests/tutorbot/test_case_submission_surface_authority_sweep.py`（11 例）| 每个病灶一条**可证伪**用例：构造包装污染/行内数字污染输入，**修前 7 红、修后全绿**；另有 4 条**反向不变量**（真实出题请求仍让路、真实字母声明仍拦、真实声明仍抑制流式）防止"收权收成一刀切"。 | 未被想到的新场景。 |
| 止血层 | `tests/tutorbot/test_surface_authority_registry.py`（AST scanner）| 五个文件里**判据形状**（`looks_like_*` / `_extract_*` / `_split_*` / `re.*` / `in` / 切片）消费信封变量的点 ⊆ 白名单；新增即红，必须显式登记 + 写判定理由。已实证能抓出新注入的违规点。 | **语义级绕过**：①先把信封赋给别名再消费；②已登记 EXEMPT 项后来被改成有权力的判据；③这五个文件**之外**的新消费者。scanner 只看语法，不看对错。 |
| 契约层 | `contracts/turn.md §判分链学生提交单一来源` | 扩展 #638 立的那条（读取侧 vs 写入侧），不开第二条款。明确：三处 surface 实现封顶、不得新增第四个解析器、豁免必须具名登记、同一事实一把尺子、权威数不出来时不报数。 | 不读合同的人。 |
