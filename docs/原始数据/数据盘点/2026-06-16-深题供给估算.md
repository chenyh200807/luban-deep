# 深题供给估算(早期方法记录,结论已被取代)

- **日期**:2026-06-16
- **范围**:`artifacts/knowledge_compiler/2026/scoring-point-assets-20260602/scoring_point_assets.jsonl`(**6134 采分点**)
- **方法**:随机抽 120 采分点(seed=42)→ DeepSeek(deepseek-chat)分类「可做深题(因果/程序/鉴别/构造)vs 仅记忆题(法条/数值/名词/列举)」→ 按比例外推
- **数据/脚本产物**:`/tmp/f16_deep_supply_result.json`

## 发现

### 章节分布(6134 采分点按 node 前 4 位)
- `1A43`(施工技术):3614(58%)
- `1A41`:2020(32%)
- `1A42`(法规):500(8%)

### 抽样 120 分类结果
- **deep(可做深题):仅 2(~1-2%)**
- recall(仅记忆):118(~98%),类型细分:**名词 55 / 法条 26 / 数值 15 / 程序 14 / 列举 10**
- 注:DeepSeek 把 14 个标 `程序` 类型的点里绝大多数仍判为 recall(="列施工程序"=可背,非"为什么这个顺序"=因果),判定偏严

### 外推
- scoring_point_assets 里**直接可做深题的 ≈ 100-300**,绝大部分是记忆型术语锚点
- 采分点样本形态印证(全是"《城市道路管理条例》规定…""机动车不得在桥梁试刹车"这类单条法条/记忆点,`list_rule.mode=term_exact_match, term_count=1`)

## 结论(已细化/被取代)

> **本估算的方法没错,但结论已被 [编译资产盘点](2026-06-16-编译资产盘点.md) 取代:**
> 深题原料**不在 scoring_point_assets 的 deep%**(它本就是"教材术语锚点抽取层",`score_status=pending_calibration_not_official`,设计上就是记忆锚点),
> 而在 **RichLeaf 的 313 个 LLM 深编译叶**(`rich_leaf_full2026_llm_deep_compile_runner`:procedures 76% / numeric_constraints 70% / rules 93% / common_mistakes 91%)——**那才是因果/程序/数值不变量的现成原料**。

→ 后续判"深题能做多少"以**编译资产盘点的 RichLeaf 深编译层**为准,不以本文的 scoring_point deep% 为准。本文保留作"**为什么不能用 scoring_point_assets 直接抽深题**"的方法论记录(它是记忆锚点层,不是不变量层)。
