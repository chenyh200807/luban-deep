# 收官架构复审(Battle1+Battle2 之后)— 终审报告

> 五复审官(17-MUST核销/新债审计/绑定约束/交互对抗/局外人)+指挥官终审。审查树=74750afd(main+Battle2 四组)。全部结论亲读代码取证。

## 一、原审计 17 条 MUST 核销表

**9 条 RESOLVED**(结构收权级修复,非补丁):①路由双跑 ②SQLite 全局锁(3.1× 实测) ③分类器超时 ④tiktoken 同步重编码 ⑤adapter 冷加载 ⑦并发闸虚设 ⑧模型档位死接线 ⑨修正链手抄 ⑯TTFT 未埋。
**3 条部分解决**:⑥多 worker 轮询(生产已 2 worker,轮询仍在——下一战役候选) ⑬学情 JSONL(LLM 主病已治,存储扫描留待体感) ⑮性能回归闸(生产观测通电,W2-T4 微基准脚本未入库)。
**5 条未动(全是组织/基建债)**:⑩fat-skill 48KB(按 F3/F4 新证据**降档**——耗时与 prompt 大小无关+前缀缓存摊薄) ⑪**编译线 bus factor=1(仍 MUST,半天任务:语料上 OSS+校验和入 git)** ⑫**supply manifest(仍 MUST,先做只读对账 job)** ⑭**CI 暗测试 27% 覆盖没动(仍 MUST)** ⑰**金标 150 条(仍 MUST,非代码)**。

## 二、新债审计(less-is-more 自查)

**运行时复杂度净减**(真删除:全局锁/双跑/4 处手抄/heartbeat 1510 次 LLM/摘要双份注入;6 类新进程内状态逐个核验均为可丢弃派生态;magic numbers 带实测校准注释)。**但债务转移进了验证基建**:
- ⚠️ B2/B4 的 14 天赎罪条款**在 main 上零落地**(git grep 无命中)——需补账
- ⚠️ tests/tutorbot 隔离污染 13→**37**(loguru 窄 stub 根因已知,一处修)
- 64% 新测试文件不在任何 CI shard(判分差分门测试全暗)
- 路由 6s 超时击中率零埋点;token 缓存无跨 session 淘汰
- PRAISE:DEEPTUTOR_MCQ_FEEDBACK_COMPACT 的 flag 生命周期是全仓库模板级样板

## 三、泄露红测试根因(独立问题结案)

`test_deep_question_blocks_unanswered_direct_answer_reveal` 红 12 天=**陈旧测试断言**(2026-06-30 PR#317 落地 owner 边界#2"显式要答案放行"后未更新),**非生产泄露**。真病=契约"登记≠执行"机制(tests.yml:452 自认)。修法:更新断言(30 分钟)+contract_guard 加"登记的 domain 测试必须在某 CI shard 可达"静态检查。

## 四、交互对抗抽查(7 攻击面)

6 未打穿(批量落库×门控读一致/TTFVT 口径/heartbeat 记账/三 flag 组合(判分永不吃轻结构性成立)/双 RPC 合一爆炸半径/skip 返回形状)。**1 半打穿已当日修**:摘要门陈旧上限注释写死 N-1,生产 2 worker 实为 workers×(N-1)=4(093a4083 修正,账本共享故永不漏跑只会延迟)。灰度观察项:compact 截断时 markdown 直渲面与 sections 面的不一致(default OFF 受控);判分后 followup 轮"弱上下文+弱模型"复合降质。

## 五、绑定约束裁决(现在卡脖子的是什么)

**「刀造好了,闸全关」**——判分-39.6%/followup 瘦身/摘要门控 fast-tier 三把刀全部默认关,灰度列车未发车;生产 LLM_FAST_MODEL 未配。开闸之后的地板才是主生成本身(耗时≈输出 token÷65/s)。**价值兑现的瓶颈已从写代码转移到走流程。**

## 六、局外人终审(战略层)

- 两场战役与产品漏斗**交集为零**(逐 commit 核实无 yousenwebview/首跑文件);42 用户/60% 零消息/D1≈0/金标空白四个数字结构上不可能被这两场战役改变
- 最值得的一刀=**摘要维护器门控**(唯一同时砍成本/砍尾巴/放并发);最不值的一刀=W2-T3 批量落库(为不存在的负载追 3.1×,唯一真实负载档 8 并发还倒贴 15%)
- 战役机器(专家组+指挥官+对抗审查)真产出=抓了 3 个 MAJOR+2 个潜伏 bug;真缺陷=**没有目标选择器**——只优化"怎么打赢",从不质疑"该不该打这仗"

## 七、指挥官终审

**总裁决**:运行时架构已从"不信任已有真值、重复支付成本的系统"收敛为"单一权威+fail-open 门控+结构性不变量"的健康形态,工程质量达到教科书级;但**价值兑现卡在两处非代码的地方——闸门(灰度流程)与组织(验证基建+金标无人扫)**。

**下一步 Top6(收益×确定性÷成本)**:
1. **走完发布列车**(PR-1 合并→部署#1 基线→PR-2/3→部署#2→toggle 序)——所有已造价值锁在这
2. 修泄露红测试断言 + contract_guard 补"登记必可达"检查 + loguru stub 一处修(tests/tutorbot 纳管)
3. 编译语料上 OSS+校验和(半天,关 bus factor=1 最坏情形)
4. B2/B4 赎罪条款补落 main 文档;W2-T4 微基准脚本入库 scripts/bench/
5. **金标 150 条人工标注**(非代码,收入闸,穿越两场战役仍空白)
6. supply manifest 只读对账 job(先可见后加闸)

**该停止**:①第三场性能战役(64 并发容量已远超真实负载一个数量级) ②为不存在的负载继续优化热路径 ③漏斗侧零动作时继续供给侧大额投资——下一次战役机器启动前先过一道"该不该打"的目标选择审查。

**给 owner 的最后一段话**:这两天你的系统从"会在考前高峰塌掉的架构"变成了"能扛 3 倍并发、消息不丢、每一分钱花在哪都看得见"的架构——这份保险在你需要它的那天会值回全部成本。但保险不带来住户。现在最贵的资源不是并发容量,是你自己的下一个决定:把灰度闸打开(工程收尾),然后把接下来两周的主力火力从代码转向那 150 行空着的金标模板和 60% 零消息的首体验——那两个才是这栋房子有没有人住的问题。
