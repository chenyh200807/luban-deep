# 案例题轻练 · 你怎么测我做的这些(一页纸)

- **日期**: 2026-07-09 · 分支 `feat/luban-case-light-practice-p-1`(worktree `deeptutor-p1-worktree`)
- **一句话**: 判分引擎侧后端全绿(141 测试 · Codex 修 26 真 bug · 真 DeepSeek+真 Qwen 阿里云实测)。下面 4 种测法,从"最快眼见为实"到"真 LLM 实测"。

---

## ① 最快:一键眼见为实(30 秒,纯本地)

```bash
cd /Users/yehongchen/Developer/deeptutor-p1-worktree
python3 scripts/verify_case_light_practice.py
```
它逐个引擎打印**"引擎算出的 == 真题官方金标"**(CPM 关键线路/总工期、DAG+ECF 造价链 64539.54、荷载组合集合、工序排序、合取门),再把 **F16 起鼓割补整条链**(采分点→出题→RTG 门→判分)跑一遍,复现 live 验证:**作答 A 漏『分层剥开』=1.2 / 作答 B 写了=1.5**(漏关键点判出更低分 = 污染 rubric 虚高被治好的证据)。全绿即"引擎真算对、链路真跑通"。

## ② 全部单元测试(反自证,读全输出)

```bash
python3 -m pytest tests/services/construction_grading/ -k "case_light_practice or case_cpm or flaw_correction or calc_dag or photo_diagnosis or recognition_to_writing or load_combination or process_ordering or grading_dispatch or flaw_spotting or scoring_point_resolver or quality_gate or rtg9" -q
```
141 passed。每个 Codex 揪出的真 bug 都有回归测试锁死(集合/浮点 TF/ECF 缺上游/否定/类型混淆/环…)。再跑 `python3 scripts/check_schema_registry.py --closure`(CLOSED 215)+ `check_contract_guard.py --base origin/main --head HEAD`(全绿)。

## ③ 看得见的 HTML demo(已发你)

F16 起鼓割补链路 demo(`case_light_practice_demo.html`,已交付):真采分点 → 生成器 → 真 RTG1-8 门裁决 → 真确定性判分,浏览器直接看。重生成:`python3 scripts/build_case_light_practice_demo.py out.html`。

## ④ 真 LLM 实测(阿里云容器只读,不部署)

`scripts/aliyun_probes/` 三个 probe,走阿里云已部署容器只读 `docker exec`(容器自带 key,不碰 host、跑完清 /tmp):
- `case_light_practice_gen_probe.py` —— 真 DeepSeek 生成干扰项 + 过真 RTG 门(已实测:整条真链路转起来)。
- `case_segmentation_candidate_probe.py` —— 真 DeepSeek 对 5 优先题切分候选(已回填 review.json)。
- `case_rtg9_cross_source_probe.py` —— 真 Qwen(异源)判"其实也对"分流(已实测:近义干扰「分层剥离」被 Qwen 判"也对"→ 可疑队列)。

投送法(记忆里也有):`base64 harness | ssh Aliyun-ECS-2 "docker cp deeptutor:/tmp && docker exec deeptutor sh -lc 'cd /app && PYTHONPATH=/app python /tmp/x.py'"`。

---

## ⑤ 测"整条生产链"(需你先动一个人门)

现在**生产链是空转的**(白名单空 = fail-closed,未过教研验收的 qid 不出给学员)。要测"一道真题 → 学员真能练 → 判分",需要:
1. 你拉两名教研,按 `docs/原始数据/考点原料/segmentation_gold/_教研验收指南.md` 填 5 题 `<qid>.review.json` 的 verdict;
2. 一填完:`python3 scripts/fill_case_whitelist_from_review.py --write`(过质量闸 + consensus 才灌白名单);
3. 然后 `resolve_scoring_points(qid)` 就能出真采分点 → 整条链对白名单题活起来。

在那之前,①②③④ 已经能让你完整验证"引擎算得对、门拦得住、链路跑得通"。

> 判分引擎接进**生产判官** `deep_question`/`rubric_grader` 的接线,见 `2026-07-09-案例题轻练判分引擎接线架构提案.md`(待你拍落点/灰度/policy 映射三点)。
