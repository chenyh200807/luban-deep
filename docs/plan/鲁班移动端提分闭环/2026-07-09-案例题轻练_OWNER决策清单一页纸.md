# 案例题轻练 · OWNER 决策清单(一页纸,签完就能开所有门)

- **日期**: 2026-07-09 · 分支 `feat/luban-case-light-practice-p-1`(已 push,未开 PR)
- **一句话**: 教研-independent 后端已 100% 写完且每条声称都反自证证明(943 passed / 闭包 CLOSED 215 / contract_guard 全绿 / 真 DeepSeek+真 Qwen 阿里云实测 / 32 个 Codex+自审对抗核治本修的真判分 bug)。**现在所有剩余项都只等你**。下面 6 个决策,签完人门全开。

---

## 你要拍的(按解锁力排序)

| # | 决策 | 我的推荐 | 拍了它解锁什么 |
|---|---|---|---|
| **A** | **双教研填 5 题 review.json verdict** | 派两人,按 `_教研验收指南.md`(sub_no/原子/锚/合取role/顺序rank/kind) | **唯一真阻塞**。填完 `fill_case_whitelist_from_review.py --write` 一跑,整条链对白名单题即活(续6 已端到端证明) |
| **B** | **优先清单 5 题**是否照提案确认 | 照提案(P0011_01 起鼓割补锚题等,§2.7) | 教研开切的对象 |
| **C** | **Q1 接线落点**:同层 `_grade_one_case_v1` vs 新 capability | 同层(最小改动+复用单权威门) | 层③生产调用点怎么写 |
| **D** | **Q2 灰度**:先白名单 5 题 vs 全量 | 先 5 题(与"未过验收不出"一致) | 层③开多大 |
| **E** | **Q3 题型 tag 形态** | 已 register-before-use 注册 `practice_grading_kind`(3值)+ 派生逻辑,**你确认即可** | 层①kind 派生落地(已写) |
| **F** | **Q4 CALC/CPM/SET 的结构化 spec 从哪来**(公式DAG/活动网络/集合矩阵) | **(c) 先只上 ORDERING/CONJUNCTION/点选**(spec 可自动建、已写),**(b) 把 CALC/CPM/SET 排进"命题作者轨"**(另建 schema+工具,别让教研手编到 review.json) | 层② 剩下 3 个 kind |

---

## 一个方向分歧,必须你裁(计划内部张力)

计划 §2.5-§2.6 把「接线进生产判官」列为 **P0-P2 代码块**;但计划 §3 红线又是「**review-only,不改生产判分逻辑 `deep_question`/`rubric_grader`**」。**层③(改 `_grade_one_case_v1` 加 policy 分发)天然改生产判分逻辑 —— 这两条在计划内部直接冲突。** 我按红线**没写**层③。

**这不是我偷懒,是计划自身的方向分歧,只有你能裁**:
- 要么**你授权**"层③ 可改生产判分逻辑"(解除该处红线),我在你定的 C/D 口径下实现(窄改动:judge 前加 policy 分发 + 补 domain 测试);
- 要么**你指定由谁改生产判官**(我交付组合层 `case_grading_composition` + `dispatch_grade` 作为它调用的单元)。

**在你裁之前,我不擅动 `deep_question`/`rubric_grader`。**

### 剩余两块阻塞是**两种不同性质**(精确区分,别混为"都做不了")

| 剩余块 | 性质 | 为什么我没写 |
|---|---|---|
| **层③ 生产调用点**(改 `_grade_one_case_v1`) | **硬阻塞** | §3 红线明文"不改生产判分逻辑"。未经你授权写它 = **破红线**,不是我保守 |
| **层② CALC/CPM/SET 装配** | **决策门 + 过早** | 技术上能写。但其结构化 spec(公式DAG/网络/矩阵)是**命题时作者产物、现在不存在**;为不存在的生产者写消费者 + 把 schema 形状钉死在 Q4 未定的位置 = **违"不提前解决明天的问题"(你给的 Less-Is-More)**。**你拍 Q4 定了作者来源,我立刻按 register-before-use 写 schema+装配+金标**(和 ORDERING/CONJUNCTION 已做的完全同一套路,证明可写) |

**一句话**:层③ 是"红线不许",CALC/CPM/SET 是"等你 Q4 一句话我马上写、现在写是过早"。两者都不是"技术上做不到",但**现在硬推都会破你给的某条原则**。

---

## 已经备好、你一动就接的(证据在分支)

- 层① `derive_grading_kind` + 层② `assemble_ordering_spec`/`assemble_conjunction_pairs`(真题金标+对抗核修 2 scope bug)
- `dispatch_grade`(7 kind)+ `resolve_scoring_points`(白名单+教研门 fail-closed)+ 7 判分引擎 + RTG1-9 + 生成器
- 白名单填充器 + 切分候选 + 教研指南(含 ordering_rank/conjunction_role/kind 怎么填)
- 真 LLM 端到端 probe(`scripts/aliyun_probes/case_e2e_real_llm_probe.py`)

## 一个 housekeeping
文档(本文件 + 提案 + 计划 §2.6 续5-10)在 CYH_2 `release/old-blue-frontend` 本地 commit;该共享分支与 origin 分叉(ahead/behind),未推 —— 不为文档 rebase 共享 release 分支,里程碑对账时带上。

> **签完 A–F + 裁完方向分歧,我这边没有任何"等你"之外的可写项了。** 教研-independent 可写面已穷尽且反自证证明;真正下一步在你的 6 个决策上。
