# 会话交接 · 案例题轻练能力(新窗口接续总纲)

> **给下一个窗口的第一句话**:本窗口把「案例题轻练」的**教研-independent + 红线安全的后端**全部写完并验证过(950 测试全绿 / 真题金标 / Codex 对抗核修 34 真 bug / 真 DeepSeek+真 Qwen 阿里云实测)。**剩下的每一步都卡在人门或 owner 决策**,不是代码没写完。先读本文,再按 §7 决定接什么。

- **日期**:2026-07-10
- **能力**:任意已教研验收的案例题 → 当场变一道少打字的轻练,判分确定、诊断挂采分点/误解
- **计划母文档**(唯一权威):`docs/plan/鲁班移动端提分闭环/2026-07-08-luban-case-question-light-practice-capability-plan.md`(v1.3)
- **本交接文档 = 母文档 §2.6 的操作化续接**,不重复计划正文,只讲"做到哪了 + 怎么接"

---

## 1. 代码/文档/资产都在哪(先定位)

| 类型 | 位置 |
|---|---|
| **代码** | worktree `/Users/yehongchen/Developer/deeptutor-p1-worktree`,分支 `feat/luban-case-light-practice-p-1`(已 push origin,**未合 main、未开 PR**) |
| **计划 + 提案 + 交接文档** | CYH_2:`docs/plan/鲁班移动端提分闭环/`(本文件同目录) |
| **判分/组合引擎** | `deeptutor/services/construction_grading/case_*.py`(见 §3) |
| **验证脚本** | `scripts/verify_case_light_practice.py`(一键)、`scripts/analyze_case_dispatch_policy.py`(分发分析) |
| **真 LLM 只读实测** | `scripts/aliyun_probes/case_*.py`(4 个 probe,走阿里云容器) |
| **教研验收指南 + 5 题骨架** | worktree `docs/原始数据/考点原料/segmentation_gold/`(`_教研验收指南.md` + `<qid>.review.json`) |
| **演示 HTML(给 owner 看)** | 见 §6 |
| **记忆锚点** | `~/.claude/projects/-Users-yehongchen-.../memory/luban-case-light-practice-p1-skeleton-landed.md`(有全部裁断史) |

---

## 2. 一句话现状 + 完成度

**教研-independent 后端 = 100% 写完并验证。** 整条"确定性判分链"从 qid → 判分已端到端跑通(见 §4 capstone),**只差一个生产调用点(层③)和 CALC/CPM/SET 的结构化 spec 作者来源(Q4)** —— 这两个都是 owner 门,不是代码欠账(为什么见 §5)。

**证据**:`950 passed / 16 skipped` · 闭包 `CLOSED 215` · `contract_guard` 全绿 · 真 DeepSeek+真 Qwen 阿里云只读实测 EXIT=0。

---

## 3. 完成清单(逐件 + 验证方式)

> ✅ = 已写 + 有测试/金标/对抗核。每件都能独立复现验证。

### 3.1 契约与门(P-1 骨架)
- ✅ **`case_light_practice_contract.py`** — `LubanCaseScoringPoint`(采分点唯一真值,frozen)+ `PointType`(6 型)+ 非平点结构(ordering_group/conjunction_group/list_cap)+ `PracticeGradingKind` 枚举(单一权威)+ `ConjunctionRole` + **白名单门 `assert_qid_allowed`**(fail-closed,未过教研的 qid 代码层拒绝出题)+ `score_conjunction_group`(找错∧改正)。schema `luban_case_scoring_point.v1` 注册为 **T2 PINNED**(裁断理由见记忆,别重议)。
- ✅ **RTG1-8 确定性门** `case_light_practice_rtg.py`(撞车/去重/错因码/形态/近义…),未跑到的门显式 NOT_EXERCISED(反假绿)。
- ✅ **RTG9 异源分流门** `case_light_practice_rtg9.py` — 确定性相似度预过滤(Jaccard≥0.5)+ 注入式异源 judge_fn,**只分流不当真值**。Qwen 真异源阿里云实测过。
- ✅ **切分质量闸** `case_segmentation_quality_gate.py` — 结构校验(sub_no/合取组≥2/list_cap/无重复/consensus),接进白名单填充器。

### 3.2 七个确定性判分引擎(每个真题金标 + Codex 对抗核)
- ✅ **CPM 关键线路** `case_cpm_solver.py` — ES/EF/LS/LF/TF,金标 N01 网络 `START-A-C-E-END 工期10`。Codex 修 1 bug(浮点 TF 容差)。
- ✅ **DAG+ECF 造价链** `case_calc_dag.py` — 安全 AST 求值 + ECF(上游错不连坐),金标 EXAM_1A432000_P0016_02 `合同价 64539.54`。Codex 修 7 bug。schema `luban_case_calc_step.v1` T2 PINNED。
- ✅ **合取门 判断改正** `case_flaw_correction.py` — 找错∧改正缺一即 0。Codex 修 4 bug(**组作用域必须按题**,别用全局 key)。
- ✅ **荷载组合 集合判分** `case_load_combination.py` — 集合精确匹配,金标 EXAM_1A434020_P0009_01。schema `luban_case_set_membership.v1` T2 PINNED。Codex 修 5 bug。
- ✅ **工序排序** `case_process_ordering.py` — 拓扑序校验,金标 EXAM_1A434000_P0010_02。Codex 修 3 bug(环/零宽/None)。
- ✅ **拍照诊断** `case_photo_diagnosis.py` — **图↔判分解耦**(只吃抽取文本不吃图,OCR 是上游注入边界,§1.5 defer);required_terms 全需 + 否定守卫 + 最高置信 span。Codex 修 3 bug。真 F16 采分点 + 模拟 OCR 误差验过。
- ✅ **认→写阶梯** `case_recognition_to_writing.py` — 铁律:点选(档1)永不写掌握;升档只由延时空手产出兑现。Codex 修 3 bug(裸 int 绕过 IntEnum)。

### 3.3 生成 + 源 + 组合(接线的红线安全半)
- ✅ **运行时生成器** `case_light_practice_generator.py` — LLM 注入式(complete_fn seam);correct=采分点原文逐字,LLM 只造干扰项;过 RTG 门重生成≤2。
- ✅ **分发器** `case_grading_dispatch.py` — `PracticeGradingKind` → 引擎路由,`dispatch_grade`,official_score_allowed 恒 False。
- ✅ **采分点源 resolver** `case_scoring_point_resolver.py` — 白名单门 + 教研 review.json(sub_no/角色/rank)+ 编译库原文 → 投影 `LubanCaseScoringPoint`。**fail-closed 到教研 verdict**(编译库缺 sub_no=欠切分)。纯只读,不改生产模块。
- ✅ **组合层** `case_grading_composition.py`:
  - `derive_grading_kind` — 小问 → kind(tag 优先→conjunction/ordering 派生→None)。
  - `assemble_ordering_spec` / `assemble_conjunction_pairs` — 从采分点结构装配引擎 spec(ORDERING/CONJUNCTION);**按 (qid,sub_qid,group) 题作用域**(2 个真判分 bug 已治本修)。
  - `grade_ready_subquestion` — **层③会调用的红线安全单元**:derive→assemble→dispatch→汇总一次调用;CALC/CPM/SET→报错(Q4);点选→None。
  - `SPEC_BUILDABLE_FROM_REVIEW` — 钉边界:ORDERING/CONJUNCTION 可从 review 建;CALC/CPM/SET 需作者产物。

### 3.4 register-before-use 的字段(都过 contract_guard)
`practice_grading_kind`(3 值 tag)、`ordering_rank`(1-based)、`conjunction_role`(FLAW/CORRECTION)、`common_wrong_expressions`、`condition_tags` —— 全部进 `luban_case_scoring_point.v1` canonical_fields + both-ways 漂移锁 + resolver 投影 + 教研指南教怎么填。

---

## 4. 验证清单(新窗口怎么亲手复现,反自证)

```bash
cd /Users/yehongchen/Developer/deeptutor-p1-worktree

# ① 一键眼见为实:每引擎 vs 真题金标 + F16 全链路(30秒,纯本地)
python3 scripts/verify_case_light_practice.py

# ② 全部单测(应 950 passed / 16 skipped)
python3 -m pytest tests/services/construction_grading/ tests/scripts/test_fill_case_whitelist.py -q

# ③ schema 闭包 + contract_guard(应 CLOSED 215 / 全绿)
python3 scripts/check_schema_registry.py --closure
python3 scripts/check_contract_guard.py --base origin/main --head HEAD

# ④ 分发可判别性分析(回答提案 Q3)
python3 scripts/analyze_case_dispatch_policy.py
```

**已验证的关键端到端事实**:
- **capstone**(commit 36a4b8f26,`test_resolver_to_orchestrator_conjunction_end_to_end`):模拟教研 verdict passed → 白名单开 → `resolve_scoring_points` 出真采分点 → `grade_ready_subquestion` 合取门判分。**证明:owner 一填 verdict + 确认 tag,整条红线安全链跑通,只差层③。**
- **真 LLM**(阿里云只读,`scripts/aliyun_probes/case_e2e_real_llm_probe.py`):真 DeepSeek 出题 → 真 RTG 门 → 真 Qwen 异源 → 判分,EXIT=0。投送法:`base64 probe | ssh Aliyun-ECS-2 "docker cp deeptutor:/tmp && docker exec deeptutor sh -lc 'cd /app && PYTHONPATH=/app python /tmp/x.py'"`(容器自带 DEEPSEEK_API_KEY / DASHSCOPE_API_KEY;跑完清 /tmp)。

---

## 5. 卡在哪(为什么不是代码欠账,是人门)

接线拆三层,卡点性质**不同**(别混为"都做不了"):

| 层 | 内容 | 状态 |
|---|---|---|
| ① kind 派生 | 小问→走哪个引擎 | ✅ 已写 |
| ①.5 per-小问编排器 | derive→assemble→dispatch→汇总 | ✅ 已写(`grade_ready_subquestion`) |
| ② spec 装配(ORDERING/CONJUNCTION) | 采分点/review→引擎输入 | ✅ 已写+对抗核 |
| ② spec 装配(CALC/CPM/SET) | 需结构化命题作者产物 | ⛔ **Q4 owner 决策**(作者来源不存在,提前建=猜方向违 Less-Is-More) |
| ③ 生产调用点 | 改 `_grade_one_case_v1` 前置 policy 分发 | ⛔ **§3 红线**(不改生产判分逻辑)+ **Q1/Q2 owner 拍落点/灰度** |
| case 级编排 | 跨小问混合确定性+coverage 怎么合分 | ⛔ 取决于 Q1/Q2 |

**唯一真数据阻塞 = 双教研填 5 题 review.json verdict**(白名单现在空 = fail-closed,没过验收的 qid 代码层拒绝出给学员,§4 红线)。

---

## 6. 演示资产(给 owner / 产品看的)

本窗口做的可视化(scratchpad HTML + 已发布 artifact):
- **学员端(防水/起鼓割补)** `student_view.html` — artifact https://claude.ai/code/artifact/f0bdb928-56d7-4c70-9732-5d7866037a34
- **学员端(高处作业安全,非防水)** `student_gaokong.html`(证明泛化,同一引擎换主题)
- **真 LLM 证据页** `real_llm_evidence.html` — artifact https://claude.ai/code/artifact/75a4fa41-b0e1-4fb6-a22f-f901eca0f47a
- **一天成果总览** `overview.html` — artifact https://claude.ai/code/artifact/649d5170-2769-4ab7-9af2-b590ae81be83
- **泛化+token 成本** `generalization.html`
- scratchpad 路径:`/private/tmp/claude-501/-Users-yehongchen-Developer-CYH-2-Markzuo-deeptutor/f9921fd6-.../scratchpad/`

> **HTML 直接 open 必须带 `<meta charset="utf-8">`**,否则中文乱码(本窗口踩过)。

**主题覆盖真相**:编译库 174 真题 / 13 章节(安全 36 题、消防 10、基坑 7、塔吊 6、起重吊装 5、脚手架 5、高处作业 4、混凝土 77 采分点、造价/施组…),**不止防水**。引擎主题无关,内容(采分点)分主题。

---

## 7. 新窗口能接的下一步(按能不能自己推排序)

**A. 自己能推(红线安全,不需 owner)**:
1. 更多题型的**学员端演示**(判断改正走合取门 / 排序 / 荷载勾选)—— 纯前端 HTML,证明泛化。
2. **扩大切分候选**:对更多优先题跑 `scripts/aliyun_probes/case_segmentation_candidate_probe.py` 产 review.json 候选(教研审而非写)。

**B. 需 owner 一句话才能接(交接给 owner 的决策清单)**:
见 `2026-07-09-案例题轻练_OWNER决策清单一页纸.md`(6 决策 A-F + 方向分歧)+ `2026-07-09-案例题轻练判分引擎接线架构提案.md`(Q1-Q4 已数据答/tag 已注册)。
- **A 双教研 verdict**(唯一真阻塞)→ 填完 `python3 scripts/fill_case_whitelist_from_review.py --write` → 链对白名单题活。
- **Q1 落点 / Q2 灰度**(提案已推荐)→ 授权后写层③(窄改动,下游已 capstone 证)。
- **Q4 CALC/CPM/SET spec 作者来源**(建议先上 ORDERING/CONJUNCTION/点选,CALC/CPM/SET 排作者轨)。

**C. 里程碑(上阿里云时)**:PR 合 main + 部署(改 Python 必 build);文档 push(本窗口 doc 在 CYH_2 `release/old-blue-frontend` 本地 commit,共享分支分叉未推,对账时带上)。

---

## 8. 硬约束(接续时不许破)
采分点是唯一真值,LLM 不得改写/越权当 ground truth · 计算类判分绝不走 LLM · 未过教研验收的 qid 不许出给学员(代码级白名单 gate)· official_score_allowed=false 直到金标 kappa 转正 · review-only 不改 `deep_question`/`rubric_grader` 生产判分逻辑(需 owner 授权)· Aliyun 只写 `/root/deeptutor`,改 Python 必 build · 判分引擎写砸比没写更糟(必真题金标 + Codex 对抗核)。

---

## 相关文档索引
- 计划母文档 `2026-07-08-luban-case-question-light-practice-capability-plan.md`(§2.6 有逐轮进展)
- 接线架构提案 `2026-07-09-案例题轻练判分引擎接线架构提案.md`(Q1-Q4 + 三层分解)
- OWNER 决策清单 `2026-07-09-案例题轻练_OWNER决策清单一页纸.md`(6 决策)
- 怎么测一页纸 `2026-07-09-案例题轻练怎么测(一页纸).md`
- 教研验收指南 worktree `docs/原始数据/考点原料/segmentation_gold/_教研验收指南.md`
- 记忆锚点 `luban-case-light-practice-p1-skeleton-landed.md`(全部裁断史 + 踩坑)
