@import "/packageDeeptutor/theme.wxss";
@import "/packageDeeptutor/styles/paper-ink.wxss";

/* pages/report/report.wxss — 学情页(10e 诊断单)样式 */

.report-page {
  height: 100vh;
  background: var(--bg-primary);
  overflow: hidden;
}

/* 10e:诊断单主面走宣纸底(纸墨朱竹 --pk-* token,夜/亮随 .light 切换) */
.report-page.paper {
  background-color: var(--pk-paper);
  background-image: radial-gradient(var(--pk-dot) 1.3rpx, transparent 1.3rpx);
  background-size: 34rpx 34rpx;
}

/* ── 导航栏 ──────────────────────────────────────── */
.nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 50;
  background: rgba(10, 10, 10, 0.9);
  border-bottom: 1rpx solid var(--border);
}

.nav-content {
  height: 96rpx;
  display: flex;
  align-items: center;
  padding: 0 24rpx 0 0;
  gap: 16rpx;
}

/* 返回按钮 */
.nav-back {
  display: flex; align-items: center; gap: 4rpx;
  margin-left: 20rpx; padding: 8rpx 12rpx 8rpx 4rpx;
  border-radius: 8rpx;
}
.nav-back:active { background: rgba(255,255,255,0.08); }
.nav-back-arrow {
  font-size: 40rpx; color: var(--pk-grn); font-weight: 300;
  line-height: 1; margin-top: -4rpx;
}
.nav-back-txt { font-size: 28rpx; color: var(--pk-grn); font-weight: 500; }

.nav-title {
  flex: 1;
  font-size: 34rpx;
  font-weight: 700;
  color: var(--text-primary);
}

/* ── 页面滚动区 ──────────────────────────────────── */
.page-scroll {
  position: fixed;
  left: 0;
  right: 0;
  overflow-y: auto;
}

.page-inner {
  padding: 28rpx 24rpx 0;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.focus-summary {
  padding: 18rpx;
  border-radius: 14rpx;
  background: rgba(15, 23, 42, 0.28);
  border: 1rpx solid rgba(148, 163, 184, 0.12);
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.focus-label {
  font-size: 20rpx;
  font-weight: 700;
  color: var(--pk-grn);
}

.focus-text {
  font-size: 28rpx;
  line-height: 1.4;
  font-weight: 800;
  color: var(--text-primary);
}

.primary-training-cta {
  margin-top: 2rpx;
  padding: 16rpx;
  border-radius: 14rpx;
  background: rgba(6, 78, 59, 0.18);
  border: 1rpx solid rgba(72, 128, 106, 0.22);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.degraded-hint {
  padding: 14rpx 18rpx;
  border-radius: 12rpx;
  background: rgba(194, 101, 85, 0.1);
  border: 1rpx solid rgba(194, 101, 85, 0.18);
}

.degraded-hint-text {
  font-size: 22rpx;
  line-height: 1.45;
  color: var(--pk-warn);
}

.guest-report-card {
  padding: 24rpx;
  border-radius: 24rpx;
  background: var(--pk-card);
  border: 1rpx solid var(--pk-bd);
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.guest-report-copy {
  flex: 1;
  min-width: 0;
}

.guest-report-kicker {
  display: block;
  font-size: 22rpx;
  font-weight: 800;
  letter-spacing: 2rpx;
  color: var(--pk-grn);
}

.guest-report-title {
  display: block;
  margin-top: 8rpx;
  font-size: 30rpx;
  line-height: 1.24;
  font-weight: 860;
  color: var(--text-primary);
}

.guest-report-desc {
  display: block;
  margin-top: 8rpx;
  font-size: 23rpx;
  line-height: 1.42;
  color: var(--text-secondary);
}

.guest-report-action {
  flex-shrink: 0;
  height: 64rpx;
  padding: 0 24rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--pk-ink-btn);
}

.guest-report-card .guest-report-action-txt {
  color: var(--pk-ink-btn-t);
}

.guest-report-action-txt {
  font-size: 24rpx;
  font-weight: 820;
  color: var(--pk-t1);
}

.module-mini-stats {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.module-mini-stats text {
  padding: 6rpx 10rpx;
  border-radius: 8rpx;
  background: rgba(255, 255, 255, 0.08);
  font-size: 19rpx;
  line-height: 1.2;
  color: rgba(255, 255, 255, 0.76);
}

/* ── 二级详情页头图与图表 ───────────────────── */
.report-detail-hero {
  padding: 26rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  border-color: rgba(148, 163, 184, 0.16);
}

.detail-evidence {
  background:
    linear-gradient(145deg, rgba(8, 145, 178, 0.24), rgba(15, 23, 42, 0.72));
}

.detail-map {
  background:
    linear-gradient(145deg, rgba(79, 70, 229, 0.24), rgba(15, 23, 42, 0.72));
}

.detail-training {
  background:
    linear-gradient(145deg, rgba(5, 150, 105, 0.24), rgba(15, 23, 42, 0.72));
}

.detail-progress {
  background:
    linear-gradient(145deg, rgba(217, 119, 6, 0.22), rgba(15, 23, 42, 0.72));
}

.detail-hero-kicker,
.detail-hero-title,
.detail-hero-sub {
  display: block;
}

.detail-hero-kicker {
  font-size: 22rpx;
  font-weight: 800;
  color: var(--pk-grn);
}

.detail-hero-title {
  font-size: 34rpx;
  line-height: 1.25;
  font-weight: 850;
  color: var(--text-primary);
}

.detail-hero-sub {
  font-size: 23rpx;
  line-height: 1.55;
  color: var(--text-secondary);
}

.detail-metric-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
}

.detail-metric-row > view {
  padding: 15rpx 12rpx;
  border-radius: 12rpx;
  background: var(--pk-card);
  border: 1rpx solid var(--pk-bd);
}

.detail-metric-num,
.detail-metric-label {
  display: block;
}

.detail-metric-num {
  font-size: 32rpx;
  line-height: 1.1;
  font-weight: 850;
  color: var(--pk-grn);
}

.detail-metric-label {
  margin-top: 5rpx;
  font-size: 19rpx;
  color: rgba(226, 232, 240, 0.66);
}

.report-map-summary {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.map-summary-label,
.map-summary-num {
  display: block;
}

.map-summary-label {
  font-size: 20rpx;
  color: rgba(226, 232, 240, 0.62);
}

.map-summary-num {
  margin-top: 2rpx;
  font-size: 52rpx;
  line-height: 1;
  font-weight: 850;
  color: var(--pk-grn);
}

.map-summary-bars {
  height: 18rpx;
  display: flex;
  gap: 6rpx;
}

.map-summary-bar {
  height: 18rpx;
  border-radius: 99rpx;
}

/* 四态暖色语义(10e):稳了/再看一眼/待复验——禁红灯墙 */
.map-summary-bar.strong { width: 120rpx; background: var(--pk-grn); }
.map-summary-bar.normal { width: 90rpx; background: rgba(72, 128, 106, 0.38); }
.map-summary-bar.weak { width: 54rpx; background: var(--pk-warn); }

.training-route {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.training-route-line {
  flex: 1;
  height: 2rpx;
  background: rgba(110, 231, 183, 0.24);
}

.training-route-step {
  min-width: 92rpx;
  height: 46rpx;
  padding: 0 12rpx;
  border-radius: 99rpx;
  background: rgba(255, 255, 255, 0.07);
  border: 1rpx solid rgba(255, 255, 255, 0.08);
  display: flex;
  align-items: center;
  justify-content: center;
}

.training-route-step.active {
  background: rgba(72, 128, 106, 0.16);
  border-color: rgba(72, 128, 106, 0.28);
}

.training-route-step text {
  font-size: 21rpx;
  font-weight: 800;
  color: rgba(255, 255, 255, 0.78);
}

.training-prescription-meta {
  margin-top: -4rpx;
}

.training-loop-card {
  margin-top: 16rpx;
  padding: 26rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  background:
    radial-gradient(circle at top right, rgba(20, 184, 166, 0.18), transparent 34%),
    linear-gradient(145deg, rgba(6, 95, 70, 0.22), rgba(15, 23, 42, 0.78));
}

.training-loop-head {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 18rpx;
}

.training-loop-kicker,
.training-loop-title,
.training-loop-desc {
  display: block;
}

.training-loop-kicker {
  font-size: 21rpx;
  line-height: 1.3;
  font-weight: 800;
  color: var(--pk-grn);
}

.training-loop-title {
  margin-top: 8rpx;
  font-size: 32rpx;
  line-height: 1.25;
  font-weight: 900;
  color: var(--pk-t1);
}

.training-loop-badge {
  flex-shrink: 0;
  padding: 9rpx 14rpx;
  border-radius: 999rpx;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
  font-size: 20rpx;
  line-height: 1.2;
  font-weight: 800;
  color: var(--pk-t2);
}

.training-loop-desc {
  font-size: 23rpx;
  line-height: 1.6;
  color: var(--pk-t2);
}

.training-loop-meter {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.training-loop-line {
  flex: 1;
  height: 3rpx;
  border-radius: 999rpx;
  background: var(--pk-gauge);
}

.training-loop-dot {
  width: 38rpx;
  height: 38rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
}

.training-loop-dot.active {
  background: var(--pk-accent-soft);
  border-color: var(--pk-accent-border);
}

.training-loop-dot text {
  font-size: 19rpx;
  font-weight: 900;
  color: var(--pk-t2);
}

.training-loop-stats {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.training-loop-stats text {
  padding: 7rpx 11rpx;
  border-radius: 8rpx;
  background: var(--pk-gauge);
  font-size: 20rpx;
  line-height: 1.2;
  color: var(--pk-t2);
}

.grading-loop-stages {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10rpx;
}

.grading-loop-stage {
  min-width: 0;
  padding: 12rpx;
  border-radius: 8rpx;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
}

.grading-loop-stage.status-ready,
.grading-loop-stage.status-verified {
  border-color: var(--pk-accent-border);
  background: var(--pk-accent-soft);
}

.grading-loop-stage.status-due {
  border-color: rgba(194, 101, 85, 0.32);
  background: rgba(194, 101, 85, 0.1);
}

.grading-loop-stage-label,
.grading-loop-stage-status {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.grading-loop-stage-label {
  font-size: 21rpx;
  line-height: 1.25;
  font-weight: 800;
  color: var(--pk-t1);
}

.grading-loop-stage-status {
  margin-top: 6rpx;
  font-size: 19rpx;
  line-height: 1.25;
  color: var(--pk-t2);
}

.grading-loop-stage.status-ready .grading-loop-stage-status,
.grading-loop-stage.status-verified .grading-loop-stage-status {
  color: var(--pk-grn);
}

.training-action-card {
  min-height: 86rpx;
  padding: 18rpx 20rpx;
  border-radius: 14rpx;
  background: var(--pk-grn);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  box-shadow: 0 18rpx 42rpx rgba(72, 128, 106, 0.18);
}

.training-action-card-pressed,
.battle-plan-action-pressed {
  transform: scale(0.985);
  opacity: 0.88;
}

.training-action-title,
.training-action-desc {
  display: block;
}

.training-action-title {
  font-size: 27rpx;
  line-height: 1.25;
  font-weight: 900;
  color: rgba(15, 23, 42, 0.95);
}

.training-action-desc {
  margin-top: 5rpx;
  font-size: 21rpx;
  line-height: 1.35;
  color: rgba(15, 23, 42, 0.7);
}

.training-action-arrow {
  flex-shrink: 0;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: rgba(15, 23, 42, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 34rpx;
  line-height: 44rpx;
  font-weight: 900;
  color: rgba(15, 23, 42, 0.82);
}

.progress-chart-card {
  padding: 16rpx 12rpx 8rpx;
  border-radius: 14rpx;
  background: rgba(255, 255, 255, 0.07);
}

.progress-chart-bars {
  height: 132rpx;
  display: flex;
  align-items: flex-end;
  justify-content: space-around;
  gap: 18rpx;
}

.progress-chart-bar {
  width: 82rpx;
  border-radius: 14rpx 14rpx 6rpx 6rpx;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding-bottom: 10rpx;
  background: rgba(194, 101, 85, 0.7);
}

.progress-chart-bar.today { height: 74rpx; }
.progress-chart-bar.recent { height: 116rpx; background: rgba(72, 128, 106, 0.78); }
.progress-chart-bar.streak { height: 94rpx; background: rgba(72, 128, 106, 0.72); }

.progress-chart-bar text {
  font-size: 18rpx;
  font-weight: 800;
  color: rgba(15, 23, 42, 0.82);
}

/* ── Learning Engine Evidence Fusion ───────────── */
.learning-engine-panel {
  position: relative;
  overflow: hidden;
  padding: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  border-color: var(--pk-bd);
  background: var(--pk-card);
}

.engine-panel-rail {
  position: absolute;
  left: 20rpx;
  top: 22rpx;
  bottom: 22rpx;
  width: 2rpx;
  background: var(--pk-grn);
}

.engine-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16rpx;
  padding-left: 18rpx;
}

.engine-kicker {
  display: block;
  margin-bottom: 5rpx;
  font-size: 20rpx;
  font-weight: 800;
  color: var(--pk-grn);
}

.engine-title {
  display: block;
  font-size: 32rpx;
  line-height: 1.25;
  font-weight: 900;
  color: var(--text-primary);
}

.engine-summary {
  flex-shrink: 0;
  max-width: 236rpx;
  padding: 8rpx 12rpx;
  border-radius: 999rpx;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
  font-size: 19rpx;
  line-height: 1.35;
  font-weight: 800;
  color: var(--pk-t2);
  text-align: right;
}

.engine-subtitle {
  display: block;
  padding-left: 18rpx;
  font-size: 22rpx;
  line-height: 1.45;
  color: var(--text-muted);
}

.engine-source-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10rpx;
  padding-left: 18rpx;
}

.engine-source-card {
  min-height: 108rpx;
  padding: 14rpx;
  border-radius: 14rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 5rpx;
  background: rgba(255, 255, 255, 0.045);
  border: 1rpx solid rgba(255, 255, 255, 0.075);
}

.engine-source-card.source-active {
  border-color: var(--pk-accent-border);
  background: var(--pk-accent-soft);
}

.engine-source-label {
  font-size: 20rpx;
  line-height: 1.25;
  color: var(--pk-t2);
}

.engine-source-value {
  font-size: 25rpx;
  line-height: 1.25;
  font-weight: 900;
  color: var(--text-primary);
}

.engine-source-status {
  font-size: 18rpx;
  line-height: 1.25;
  font-weight: 800;
  color: var(--pk-t3);
}

.source-active .engine-source-status {
  color: var(--pk-grn);
}

/* ── Learning State Inference ───────────────────── */
.learning-state-section {
  position: relative;
  overflow: hidden;
  padding: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  border-radius: 22rpx;
  border: 1rpx solid rgba(148, 163, 184, 0.16);
  box-shadow: 0 18rpx 48rpx rgba(2, 8, 23, 0.24);
}

.learning-state-section-rail {
  position: absolute;
  left: 0;
  top: 18rpx;
  bottom: 18rpx;
  width: 6rpx;
  border-radius: 999rpx;
  background: var(--pk-gauge);
}

.prescription-section {
  background:
    linear-gradient(135deg, rgba(6, 95, 70, 0.32), rgba(12, 28, 38, 0.78)),
    radial-gradient(circle at 88% 8%, rgba(72, 128, 106, 0.16), transparent 36%);
  border-color: rgba(72, 128, 106, 0.24);
}

.prescription-rail {
  background: var(--pk-grn);
}

.three-layer-state {
  background:
    linear-gradient(135deg, rgba(15, 23, 42, 0.9), rgba(30, 41, 59, 0.76)),
    radial-gradient(circle at 8% 0%, rgba(72, 128, 106, 0.12), transparent 34%);
  border-color: rgba(72, 128, 106, 0.18);
}

.scoring-point-map-section {
  background:
    linear-gradient(135deg, rgba(30, 27, 75, 0.36), rgba(15, 23, 42, 0.82)),
    radial-gradient(circle at 90% 4%, rgba(129, 140, 248, 0.12), transparent 36%);
  border-color: rgba(129, 140, 248, 0.2);
}

.mistake-history-section {
  background:
    linear-gradient(135deg, rgba(69, 26, 3, 0.28), rgba(15, 23, 42, 0.82)),
    radial-gradient(circle at 90% 0%, rgba(251, 146, 60, 0.13), transparent 36%);
  border-color: rgba(251, 146, 60, 0.2);
}

.mistake-rail {
  background: var(--pk-warn);
}

.scoring-rail {
  background: var(--pk-grn);
}

.learning-state-section-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.learning-state-section-title {
  display: block;
  font-size: 25rpx;
  font-weight: 800;
  color: var(--text-primary);
}

.learning-state-section-tag {
  flex-shrink: 0;
  padding: 5rpx 10rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.065);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  font-size: 18rpx;
  font-weight: 700;
  color: var(--pk-t2);
}

.learning-state-section-subtitle {
  display: block;
  font-size: 31rpx;
  line-height: 1.32;
  font-weight: 900;
  color: var(--text-primary);
}

.prescription-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.prescription-meta-chip {
  padding: 7rpx 12rpx;
  border-radius: 999rpx;
  background: rgba(72, 128, 106, 0.12);
  border: 1rpx solid rgba(72, 128, 106, 0.18);
  font-size: 19rpx;
  line-height: 1.2;
  font-weight: 800;
  color: var(--pk-grn);
}

.battle-plan-action {
  margin-top: 18rpx;
  min-height: 76rpx;
  padding: 0 18rpx;
  border-radius: 14rpx;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.battle-plan-action text:first-child {
  font-size: 24rpx;
  line-height: 1.25;
  font-weight: 900;
  color: var(--pk-t1);
}

.battle-plan-action text:last-child {
  flex-shrink: 0;
  padding: 7rpx 12rpx;
  border-radius: 999rpx;
  background: var(--pk-card);
  font-size: 20rpx;
  line-height: 1.2;
  font-weight: 800;
  color: var(--pk-t2);
}

.learning-state-section-desc {
  display: block;
  margin-top: -4rpx;
  font-size: 22rpx;
  line-height: 1.45;
  color: var(--text-muted);
}

.learning-state-steps {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
}

.learning-state-step {
  min-height: 44rpx;
  padding: 9rpx 13rpx;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
  background: var(--pk-gauge);
  border: 1rpx solid var(--pk-bd);
}

.learning-state-step-text {
  font-size: 20rpx;
  font-weight: 700;
  color: var(--pk-t2);
}

.learning-state-cta {
  min-height: 58rpx;
  justify-content: center;
  border-color: rgba(72, 128, 106, 0.3);
  background: rgba(6, 95, 70, 0.24);
}

.learning-state-cta-label {
  font-size: 23rpx;
  font-weight: 800;
  color: var(--pk-grn);
}

.learning-state-layer {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.learning-state-layer-label {
  font-size: 21rpx;
  font-weight: 800;
  color: var(--pk-grn);
}

.learning-state-row {
  padding: 18rpx;
  border-radius: 16rpx;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14rpx;
  background: rgba(255, 255, 255, 0.045);
  border: 1rpx solid rgba(255, 255, 255, 0.07);
}

.learning-state-row-main {
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.learning-state-row-title {
  font-size: 25rpx;
  line-height: 1.35;
  font-weight: 850;
  color: var(--text-primary);
}

.learning-state-row-meta {
  font-size: 20rpx;
  line-height: 1.35;
  color: var(--text-muted);
}

.learning-state-row-action {
  margin-top: 3rpx;
  font-size: 21rpx;
  line-height: 1.38;
  color: var(--pk-grn);
}

.learning-state-row-status {
  flex-shrink: 0;
  max-width: 132rpx;
  padding: 7rpx 10rpx;
  border-radius: 999rpx;
  font-size: 19rpx;
  line-height: 1.2;
  font-weight: 800;
  text-align: center;
  color: var(--pk-t2);
  background: rgba(148, 163, 184, 0.12);
}

.learning-state-row.state-warn {
  border-color: rgba(194, 101, 85, 0.18);
  background: rgba(194, 101, 85, 0.055);
}

.learning-state-row.state-warn .learning-state-row-status {
  color: var(--pk-warn);
  background: rgba(194, 101, 85, 0.12);
}

.learning-state-row.state-good {
  border-color: rgba(72, 128, 106, 0.18);
  background: rgba(72, 128, 106, 0.055);
}

.learning-state-row.state-good .learning-state-row-status {
  color: var(--pk-grn);
  background: rgba(72, 128, 106, 0.12);
}

.scoring-point-row {
  padding: 17rpx;
  border-radius: 16rpx;
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 8rpx 12rpx;
  align-items: start;
  background: rgba(255, 255, 255, 0.045);
  border: 1rpx solid rgba(255, 255, 255, 0.07);
}

.scoring-point-badge {
  padding: 5rpx 9rpx;
  border-radius: 9rpx;
  background: rgba(72, 128, 106, 0.1);
  color: var(--pk-grn);
  font-size: 18rpx;
  font-weight: 800;
}

.scoring-point-name {
  min-width: 0;
  font-size: 24rpx;
  line-height: 1.35;
  font-weight: 800;
  color: var(--text-primary);
}

.scoring-point-miss {
  grid-column: 2;
  font-size: 20rpx;
  font-weight: 800;
  color: var(--pk-warn);
}

.scoring-point-empty {
  padding: 18rpx;
  border-radius: 14rpx;
  background: rgba(99, 102, 241, 0.08);
  border: 1rpx dashed rgba(129, 140, 248, 0.22);
  font-size: 23rpx;
  line-height: 1.45;
  color: var(--pk-t2);
}

.mistake-history-card {
  padding: 18rpx;
  border-radius: 16rpx;
  background: rgba(15, 23, 42, 0.42);
  border: 1rpx solid rgba(251, 146, 60, 0.16);
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.mistake-history-card:active {
  opacity: 0.84;
}

.mistake-history-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.mistake-history-time {
  font-size: 21rpx;
  font-weight: 800;
  color: var(--pk-warn);
}

.mistake-history-result {
  padding: 5rpx 10rpx;
  border-radius: 9rpx;
  background: rgba(194, 101, 85, 0.14);
  font-size: 20rpx;
  font-weight: 800;
  color: var(--pk-warn);
}

.mistake-history-question {
  font-size: 25rpx;
  line-height: 1.45;
  font-weight: 800;
  color: var(--text-primary);
}

.mistake-history-row {
  display: grid;
  grid-template-columns: 120rpx 1fr;
  gap: 14rpx;
  padding-top: 10rpx;
  border-top: 1rpx solid rgba(148, 163, 184, 0.12);
}

.mistake-history-label {
  font-size: 21rpx;
  font-weight: 800;
  color: var(--pk-t3);
}

.mistake-history-value {
  font-size: 22rpx;
  line-height: 1.5;
  color: var(--pk-t2);
}

.mistake-history-value.strong {
  color: var(--pk-warn);
}

.mistake-history-cta {
  align-self: flex-start;
  margin-top: 2rpx;
  padding: 7rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(251, 146, 60, 0.12);
  border: 1rpx solid rgba(251, 146, 60, 0.18);
  font-size: 21rpx;
  font-weight: 800;
  color: var(--pk-warn);
}

/* ── Learning Brain ─────────────────────────────── */
.learning-review-panel {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.learning-brain {
  padding: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.learning-brain-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16rpx;
}

.learning-brain-title {
  display: block;
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.learning-brain-sub {
  display: block;
  margin-top: 6rpx;
  font-size: 22rpx;
  color: var(--text-muted);
}

.learning-brain-stats {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 8rpx;
}

.brain-stat {
  padding: 6rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(72, 128, 106, 0.12);
  border: 1rpx solid rgba(72, 128, 106, 0.18);
  font-size: 20rpx;
  color: var(--pk-grn);
}

.learning-brain-loading {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.brain-skel {
  height: 44rpx;
  width: 100%;
}

.brain-skel.short {
  width: 68%;
}

.learning-brain-state {
  padding: 8rpx 0 4rpx;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.learning-brain-state-title {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.learning-brain-state-desc {
  font-size: 23rpx;
  line-height: 1.5;
  color: var(--text-muted);
}

.learning-brain-body {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.brain-review-summary {
  padding: 18rpx;
  border-radius: 16rpx;
  background: rgba(72, 128, 106, 0.08);
  border: 1rpx solid rgba(72, 128, 106, 0.16);
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.brain-review-title {
  font-size: 22rpx;
  font-weight: 700;
  color: var(--pk-grn);
}

.brain-review-headline {
  font-size: 25rpx;
  line-height: 1.5;
  color: var(--text-primary);
}

.brain-review-metrics {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.brain-review-metric {
  padding: 5rpx 10rpx;
  border-radius: 8rpx;
  background: rgba(15, 23, 42, 0.24);
  font-size: 20rpx;
  color: var(--pk-t2);
}

.brain-column {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.brain-column-title {
  font-size: 23rpx;
  font-weight: 700;
  color: var(--pk-grn);
}

.brain-column-sub {
  margin-top: -4rpx;
  font-size: 21rpx;
  color: var(--text-muted);
}

.brain-truth-row,
.brain-diagnosis-row,
.brain-edge-row,
.brain-training-row,
.brain-chain-row,
.note-asset-row,
.attempt-card,
.brain-next-action {
  padding: 16rpx;
  border-radius: 14rpx;
  background: rgba(255, 255, 255, 0.045);
  border: 1rpx solid rgba(255, 255, 255, 0.06);
}

.brain-truth-row,
.brain-diagnosis-row {
  display: flex;
  gap: 12rpx;
}

.brain-level {
  flex-shrink: 0;
  min-width: 92rpx;
  height: 36rpx;
  padding: 0 10rpx;
  border-radius: 10rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(148, 163, 184, 0.12);
  border: 1rpx solid rgba(148, 163, 184, 0.18);
}

.brain-level-text {
  font-size: 19rpx;
  font-weight: 700;
  color: var(--pk-t2);
}

.level-L1_repeated {
  background: rgba(194, 101, 85, 0.12);
  border-color: rgba(194, 101, 85, 0.22);
}

.level-L1_repeated .brain-level-text {
  color: var(--pk-warn);
}

.level-L2_confirmed {
  background: rgba(72, 128, 106, 0.12);
  border-color: rgba(72, 128, 106, 0.22);
}

.level-L2_confirmed .brain-level-text {
  color: var(--pk-grn);
}

.brain-row-main {
  min-width: 0;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.brain-row-title,
.brain-training-title {
  font-size: 25rpx;
  line-height: 1.45;
  color: var(--text-primary);
}

.brain-row-meta,
.brain-training-meta,
.brain-edge-path,
.brain-edge-event {
  font-size: 21rpx;
  line-height: 1.4;
  color: var(--text-muted);
}

.brain-diagnosis-detail,
.brain-action-line {
  font-size: 22rpx;
  line-height: 1.45;
  color: var(--text-muted);
}

.brain-action-line {
  color: var(--pk-t2);
}

.brain-challenge-action {
  align-self: flex-start;
  min-height: 44rpx;
  margin-top: 4rpx;
  padding: 8rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(72, 128, 106, 0.12);
  border: 1rpx solid rgba(72, 128, 106, 0.2);
  color: var(--pk-grn);
  font-size: 20rpx;
  font-weight: 800;
}

.attempt-card {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.attempt-card:active,
.brain-next-action:active {
  opacity: 0.82;
}

.attempt-wrong {
  border-color: rgba(194, 101, 85, 0.18);
  background: rgba(194, 101, 85, 0.055);
}

.attempt-correct {
  border-color: rgba(72, 128, 106, 0.18);
  background: rgba(16, 185, 129, 0.055);
}

.attempt-head {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16rpx;
}

.attempt-time {
  display: block;
  margin-bottom: 4rpx;
  font-size: 20rpx;
  color: var(--pk-grn);
}

.attempt-title {
  display: block;
  font-size: 25rpx;
  font-weight: 700;
  line-height: 1.45;
  color: var(--text-primary);
}

.attempt-result {
  flex-shrink: 0;
  padding: 5rpx 10rpx;
  border-radius: 9rpx;
  font-size: 20rpx;
  font-weight: 700;
}

.result-wrong {
  background: rgba(194, 101, 85, 0.14);
  color: var(--pk-warn);
}

.result-correct {
  background: rgba(72, 128, 106, 0.14);
  color: var(--pk-grn);
}

.attempt-answer {
  font-size: 23rpx;
  line-height: 1.45;
  color: var(--pk-t2);
}

.attempt-diagnosis,
.attempt-explanation {
  font-size: 22rpx;
  line-height: 1.55;
  color: var(--text-muted);
}

.attempt-explanation {
  padding-top: 2rpx;
  color: var(--text-secondary);
}

.attempt-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  padding-top: 2rpx;
}

.attempt-action {
  padding: 7rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(72, 128, 106, 0.12);
  border: 1rpx solid rgba(72, 128, 106, 0.18);
  font-size: 21rpx;
  font-weight: 700;
  color: var(--pk-t2);
}

.bookmark-action.cloud {
  background: rgba(148, 163, 184, 0.1);
  border-color: rgba(148, 163, 184, 0.22);
  color: var(--text-muted);
  opacity: 0.78;
}

.bookmark-action.saved {
  opacity: 0.72;
}

.note-card-action {
  background: rgba(72, 128, 106, 0.1);
  border-color: rgba(72, 128, 106, 0.2);
  color: var(--pk-grn);
}

.note-card-action.primary {
  background: rgba(72, 128, 106, 0.16);
  border-color: rgba(72, 128, 106, 0.24);
  color: var(--pk-grn);
}

.note-asset-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.note-asset-title,
.note-asset-summary,
.note-asset-source {
  display: block;
}

.note-asset-title {
  font-size: 23rpx;
  font-weight: 800;
  color: var(--text-primary);
}

.note-asset-summary {
  margin-top: 4rpx;
  font-size: 21rpx;
  line-height: 1.4;
  color: var(--text-secondary);
}

.note-asset-source {
  margin-top: 6rpx;
  font-size: 19rpx;
  color: var(--pk-grn);
}

.note-asset-action {
  flex-shrink: 0;
  padding: 8rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(72, 128, 106, 0.14);
  border: 1rpx solid rgba(147, 197, 253, 0.2);
  color: var(--pk-t2);
  font-size: 20rpx;
  font-weight: 800;
}

.brain-event-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.brain-event {
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  background: rgba(15, 23, 42, 0.28);
  border: 1rpx solid rgba(148, 163, 184, 0.16);
  font-size: 19rpx;
  color: var(--pk-t2);
}

.brain-edge-row,
.brain-training-row,
.brain-chain-row,
.brain-next-action {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.brain-next-action {
  border-color: rgba(72, 128, 106, 0.18);
  background: rgba(6, 78, 59, 0.16);
}

.brain-next-cta {
  align-self: flex-start;
  margin-top: 6rpx;
  padding: 7rpx 12rpx;
  border-radius: 10rpx;
  background: rgba(72, 128, 106, 0.13);
  font-size: 21rpx;
  font-weight: 700;
  color: var(--pk-grn);
}

.brain-chain-row {
  border-color: rgba(72, 128, 106, 0.18);
  background: rgba(37, 99, 235, 0.08);
}

.chain-improved {
  border-color: rgba(72, 128, 106, 0.24);
  background: rgba(16, 185, 129, 0.09);
}

.chain-not-improved {
  border-color: rgba(194, 101, 85, 0.24);
  background: rgba(245, 158, 11, 0.09);
}

.brain-chain-title {
  font-size: 24rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.brain-chain-step {
  font-size: 21rpx;
  line-height: 1.4;
  color: var(--text-muted);
}

.brain-chain-outcome {
  align-self: flex-start;
  margin-top: 2rpx;
  padding: 5rpx 10rpx;
  border-radius: 8rpx;
  background: rgba(15, 23, 42, 0.22);
  font-size: 20rpx;
  font-weight: 700;
  color: var(--pk-t2);
}

.chain-improved .brain-chain-outcome {
  color: var(--pk-grn);
}

.chain-not-improved .brain-chain-outcome {
  color: var(--pk-warn);
}

.brain-edge-type {
  font-size: 23rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.brain-footer {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  padding-top: 2rpx;
}

.brain-footer-text {
  font-size: 20rpx;
  color: var(--text-muted);
}

/* ── Section Headers ─────────────────────────────── */
.section-header {
  padding: 6rpx 4rpx 0;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.section-header-large {
  padding-top: 4rpx;
}

.section-heading-copy {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.section-kicker {
  font-size: 22rpx;
  letter-spacing: 2rpx;
  text-transform: uppercase;
  color: var(--pk-grn);
}

.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.section-subtitle {
  font-size: 24rpx;
  line-height: 1.6;
  color: var(--text-muted);
}

.sub-header {
  margin-top: 24rpx;
  margin-bottom: 8rpx;
}

.section-title-sm {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--text-secondary);
}

.section-subtitle-sm {
  font-size: 22rpx;
  color: var(--text-muted);
}

/* ── 空状态 / 加载 ───────────────────────────────── */
.loading-card {
  padding: 48rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.empty-card {
  padding: 48rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

/* 空态印章：与 attempt-detail 空态同构（书法字 + 竹青圈），替代 emoji 字符图标 */
.empty-icon {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  border: 3rpx solid var(--pk-accent-border);
  background: var(--pk-accent-soft);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: "Long Cang", "Kaiti SC", "STKaiti", "KaiTi", "楷体", cursive;
  font-size: 44rpx;
  color: var(--pk-grn);
}

.empty-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.empty-desc {
  font-size: 24rpx;
  color: var(--text-muted);
  text-align: center;
  line-height: 1.5;
}

.empty-btn {
  margin-top: 16rpx;
  padding: 14rpx 40rpx;
  background: rgba(99, 102, 241, 0.15);
  border: 1rpx solid rgba(99, 102, 241, 0.3);
  border-radius: var(--radius-full);
}

.empty-btn:active {
  opacity: 0.7;
}

.empty-btn-txt {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--pk-grn);
}

/* ── 雷达图 Canvas ───────────────────────────────── */
.radar-canvas-wrap {
  display: flex;
  justify-content: center;
  padding: 32rpx 0 16rpx;
}

.radar-canvas {
  width: 560rpx;
  height: 560rpx;
  max-width: 100%;
}

.radar-image {
  width: 560rpx;
  height: 560rpx;
  max-width: 100%;
}

/* ── 雷达统计 ────────────────────────────────────── */
.radar-summary {
  display: flex;
  justify-content: center;
  gap: 48rpx;
  margin-bottom: 16rpx;
}

.radar-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

.stat-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  margin-bottom: 4rpx;
}

/* 四态暖色(对齐 10e):红黄绿硬编码 → 竹青/淡竹青/赭 + 未学灰 */
.dot-strong { background: var(--pk-grn); }
.dot-normal { background: rgba(72, 128, 106, 0.38); }
.dot-weak   { background: var(--pk-warn); }

.stat-num {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.stat-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

/* ── 维度列表 ────────────────────────────────────── */
.dim-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.dim-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx 24rpx;
}

.dim-rank {
  width: 48rpx;
  height: 48rpx;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.rank-strong { background: rgba(72, 128, 106, 0.15); }
.rank-normal { background: rgba(194, 101, 85, 0.15); }
.rank-weak   { background: rgba(194, 101, 85, 0.15); }

.dim-rank-txt {
  font-size: 24rpx;
  font-weight: 700;
}

/* 四态暖色(10e):禁红灯墙 */
.rank-strong .dim-rank-txt { color: var(--pk-grn); }
.rank-normal .dim-rank-txt { color: #6e937f; }
.rank-weak   .dim-rank-txt { color: var(--pk-warn); }
.rank-observed .dim-rank-txt { color: var(--pk-t3); }

.dim-name {
  flex: 1;
  font-size: 26rpx;
  color: var(--text-primary);
}

.dim-bar {
  width: 120rpx;
  height: 10rpx;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 5rpx;
  overflow: hidden;
  flex-shrink: 0;
}

.dim-bar-fill {
  height: 100%;
  border-radius: 5rpx;
  transition: width 0.6s ease;
}

.dim-pct {
  font-size: 26rpx;
  font-weight: 600;
  min-width: 64rpx;
  text-align: right;
}

/* ── 掌握度大数字 ────────────────────────────────── */
.mastery-hero {
  padding: 40rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.mastery-score-wrap {
  display: flex;
  align-items: flex-end;
  gap: 8rpx;
}

.mastery-score {
  font-size: 96rpx;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
  line-height: 0.92;
}

/* 四态暖色(10e):禁红灯墙 */
.score-good { color: var(--pk-grn); }
.score-mid  { color: #6e937f; }
.score-low  { color: var(--pk-warn); }

.mastery-unit {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--text-muted);
  line-height: 1;
  margin-bottom: 12rpx;
}

.mastery-label {
  font-size: 24rpx;
  color: var(--text-muted);
}

.mastery-map-row {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12rpx;
  width: 100%;
  margin-top: 20rpx;
}

.mastery-map-item {
  min-width: 0;
  padding: 14rpx 8rpx;
  border: 1rpx solid var(--border);
  border-radius: 12rpx;
  background: rgba(15, 23, 42, 0.28);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

.mastery-map-num {
  font-size: 30rpx;
  font-weight: 800;
  color: var(--text-primary);
  font-variant-numeric: tabular-nums;
}

.mastery-map-label {
  font-size: 20rpx;
  color: var(--text-muted);
  white-space: nowrap;
}

.mastery-map-item.warn .mastery-map-num {
  color: var(--pk-warn);
}

.mastery-sub-row {
  display: flex;
  align-items: center;
  gap: 32rpx;
  margin-top: 16rpx;
}

.mastery-sub-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

.mastery-sub-num {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.mastery-sub-warn {
  color: var(--pk-warn);
}

.mastery-sub-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

.mastery-sub-divider {
  width: 1rpx;
  height: 40rpx;
  background: var(--border);
}

.textbook-directory {
  padding: 8rpx 20rpx;
  margin-bottom: 24rpx;
}

.directory-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  min-height: 88rpx;
  border-bottom: 1rpx solid var(--border);
}

.directory-row:last-child {
  border-bottom: none;
}

.directory-main {
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.directory-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.directory-meta {
  font-size: 22rpx;
  color: var(--text-muted);
}

.directory-status {
  flex: 0 0 auto;
  padding: 6rpx 12rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  color: var(--text-muted);
  background: rgba(148, 163, 184, 0.12);
}

.directory-row.status-weak .directory-status {
  color: var(--pk-warn);
  background: rgba(194, 101, 85, 0.16);
}

.directory-row.status-strong .directory-status {
  color: var(--pk-grn);
  background: rgba(72, 128, 106, 0.14);
}

.directory-row.status-developing .directory-status {
  color: var(--pk-warn);
  background: rgba(194, 101, 85, 0.14);
}

/* ── 科目掌握度分组 ──────────────────────────────── */
.mastery-group {
  padding: 24rpx;
}

.group-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
  gap: 20rpx;
}

.group-title {
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.group-name {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.group-preview {
  font-size: 22rpx;
  color: var(--text-tertiary);
}

.group-meta {
  display: flex;
  align-items: center;
  gap: 14rpx;
  flex-shrink: 0;
}

.group-avg {
  font-size: 26rpx;
  font-weight: 700;
}

.group-toggle {
  font-size: 22rpx;
  color: var(--primary-400);
}

/* 四态暖色(10e):禁红灯墙 */
.avg-good { color: var(--pk-grn); }
.avg-mid  { color: #6e937f; }
.avg-low  { color: var(--pk-warn); }

.group-chapters {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.chapter-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.ch-name {
  flex: 1;
  font-size: 24rpx;
  color: var(--text-secondary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ch-bar {
  width: 120rpx;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 4rpx;
  overflow: hidden;
  flex-shrink: 0;
}

.ch-bar-fill {
  height: 100%;
  border-radius: 4rpx;
}

.ch-pct {
  font-size: 22rpx;
  font-weight: 600;
  min-width: 56rpx;
  text-align: right;
}

/* ── AI 作战方案 ───────────────────────────────── */
.battle-plan {
  padding: 28rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, 0.18), transparent 32%),
    linear-gradient(145deg, rgba(37, 99, 235, 0.12), rgba(15, 23, 42, 0.08));
}

.battle-plan-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.battle-plan-kicker {
  display: block;
  font-size: 22rpx;
  letter-spacing: 2rpx;
  text-transform: uppercase;
  color: var(--pk-grn);
}

.battle-plan-topic {
  display: block;
  margin-top: 10rpx;
  font-size: 40rpx;
  line-height: 1.2;
  font-weight: 700;
  color: var(--pk-t1);
}

.battle-plan-badge {
  flex-shrink: 0;
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.08);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
}

.battle-plan-badge-text {
  font-size: 22rpx;
  font-weight: 600;
  color: var(--pk-t2);
}

.battle-plan-note {
  font-size: 24rpx;
  line-height: 1.7;
  color: rgba(239, 246, 255, 0.82);
}

.battle-plan-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.battle-plan-row {
  display: grid;
  grid-template-columns: 120rpx 1fr;
  gap: 20rpx;
  align-items: start;
  padding-top: 16rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.08);
}

.battle-plan-row-first {
  padding-top: 0;
  border-top: none;
}

.battle-plan-label {
  font-size: 24rpx;
  color: rgba(191, 219, 254, 0.78);
}

.battle-plan-value {
  font-size: 24rpx;
  line-height: 1.7;
  color: var(--pk-t1);
}

/* ── 进步反馈 ───────────────────────────────────── */
.progress-panel {
  padding: 28rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 22rpx;
}

.progress-summary {
  font-size: 24rpx;
  line-height: 1.7;
  color: var(--text-secondary);
}

.progress-insight {
  margin-top: -6rpx;
  font-size: 22rpx;
  line-height: 1.7;
  color: var(--text-muted);
}

.progress-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14rpx;
}

.progress-item {
  padding: 20rpx 18rpx;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.04);
  border: 1rpx solid rgba(255, 255, 255, 0.05);
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.progress-item-label {
  font-size: 22rpx;
  color: var(--text-muted);
}

.progress-item-value {
  font-size: 36rpx;
  line-height: 1.1;
  font-weight: 700;
  color: var(--text-primary);
}

.progress-item-detail {
  font-size: 22rpx;
  line-height: 1.6;
  color: var(--text-secondary);
}

.progress-milestones {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
  padding-top: 4rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.06);
}

.progress-milestone {
  display: flex;
  align-items: flex-start;
  gap: 14rpx;
}

.progress-milestone-dot {
  width: 14rpx;
  height: 14rpx;
  margin-top: 10rpx;
  border-radius: 999rpx;
  background: var(--pk-grn);
  flex-shrink: 0;
}

.progress-milestone-copy {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.progress-milestone-title {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.progress-milestone-detail {
  font-size: 22rpx;
  line-height: 1.6;
  color: var(--text-secondary);
}

.tone-good {
  color: var(--pk-grn);
}

.tone-accent {
  color: var(--pk-grn);
}

.tone-warn {
  color: var(--pk-warn);
}

/* ═══════════════════════════════════════════════
   ★ 亮色模式
   ═══════════════════════════════════════════════ */

/* ── CSS 变量覆盖 ──────────────────────────── */
.report-page.light {
  --primary-400: var(--pk-grn);
  --primary-500: var(--pk-grn);
  --primary-600: var(--pk-grn);
  --bg-primary: var(--pk-paper);
  --bg-secondary: var(--pk-card);
  --bg-tertiary: var(--pk-gauge);
  --bg-card: var(--pk-card);
  --bg-elevated: var(--pk-card);
  --text-primary: var(--pk-t1);
  --text-secondary: var(--pk-t2);
  --text-tertiary: var(--pk-t2);
  --text-muted: var(--pk-t3);
  --border-subtle: var(--pk-bd);
  --border-light: var(--pk-bd);
  --border-medium: var(--pk-bd);
  --border: var(--pk-bd);
  --shadow-sm: 0 2rpx 12rpx var(--pk-shadow);
  --shadow-md: 0 4rpx 18rpx var(--pk-shadow);
}

/* ── 导航栏 ────────────────────────────────── */
.report-page.light .nav-bar {
  background: var(--pk-paper);
  border-color: var(--pk-bd);
  box-shadow: none;
}
.report-page.light .nav-back:active { background: rgba(0,0,0,0.04); }
.report-page.light .nav-back-arrow { color: var(--pk-grn); }
.report-page.light .nav-back-txt { color: var(--pk-grn); }

/* ── 进度条背景 ────────────────────────────── */
.report-page.light .dim-bar { background: var(--pk-gauge); }
.report-page.light .ch-bar { background: var(--pk-gauge); }
.report-page.light .empty-btn {
  background: var(--pk-accent-soft);
  border-color: var(--pk-accent-border);
}
.report-page.light .empty-btn-txt { color: var(--pk-grn); }

.report-page.light .battle-plan {
  background:
    radial-gradient(circle at top right, rgba(72, 128, 106, 0.10), transparent 32%),
    linear-gradient(145deg, rgba(72, 128, 106, 0.10), rgba(255, 255, 255, 0.92));
}

.report-page.light .battle-plan-topic {
  color: var(--pk-t1);
}

.report-page.light .battle-plan-note {
  color: var(--pk-t2);
}

.report-page.light .battle-plan-badge {
  background: rgba(72, 128, 106, 0.08);
  border-color: rgba(72, 128, 106, 0.14);
}

.report-page.light .battle-plan-badge-text,
.report-page.light .battle-plan-kicker,
.report-page.light .battle-plan-label {
  color: var(--pk-grn);
}

.report-page.light .battle-plan-row {
  border-color: var(--pk-bd);
}

.report-page.light .battle-plan-value {
  color: var(--pk-t1);
}

.report-page.light .progress-item {
  background: rgba(255, 255, 255, 0.8);
  border-color: var(--pk-bd);
}

.report-page.light .progress-milestones {
  border-color: var(--pk-bd);
}

.report-page.light .report-detail-hero {
  border-color: var(--pk-bd);
  box-shadow: 0 16rpx 38rpx rgba(72, 128, 106, 0.08);
}

.report-page.light .detail-evidence {
  background: linear-gradient(145deg, rgba(72, 128, 106, 0.12), rgba(255, 255, 255, 0.94));
}

.report-page.light .detail-map {
  background: linear-gradient(145deg, rgba(79, 70, 229, 0.12), rgba(255, 255, 255, 0.94));
}

.report-page.light .detail-training {
  background: linear-gradient(145deg, rgba(16, 185, 129, 0.12), rgba(255, 255, 255, 0.94));
}

.report-page.light .detail-progress {
  background: linear-gradient(145deg, rgba(245, 158, 11, 0.12), rgba(255, 255, 255, 0.94));
}

.report-page.light .detail-hero-title {
  color: var(--pk-t1);
}

.report-page.light .detail-hero-sub {
  color: var(--pk-t3);
}

.report-page.light .detail-metric-label,
.report-page.light .map-summary-label {
  color: var(--pk-t3);
}

.report-page.light .module-mini-stats text,
.report-page.light .training-route-step,
.report-page.light .progress-chart-card {
  background: var(--pk-gauge);
  border-color: var(--pk-bd);
  color: var(--pk-t2);
}

.report-page.light .detail-metric-row > view {
  background: var(--pk-card);
  border-color: var(--pk-bd);
  color: var(--pk-t2);
}

.report-page.light .training-route-step text {
  color: var(--pk-t2);
}

.report-page.light .progress-chart-bar text {
  color: var(--pk-t1);
}

/* ============================================================
 * 10e 诊断单(第 10 轮定稿 + round11 增量①④)
 * token 源: styles/paper-ink.wxss --pk-*(夜基,.light 覆盖)
 * 铁律: 四态暖色禁红灯墙;蓝环=接触轨,绝不进红黄绿掌握色阶;
 *       辅助字号 ≥24rpx;朱红只留品牌位,本页不用。
 * ============================================================ */

.lr-head {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  padding: 8rpx 8rpx 4rpx;
}

.lr-head-title {
  font-size: 32rpx;
  font-weight: 800;
  color: var(--pk-t1);
}

.lr-head-sub {
  font-size: 24rpx;
  color: var(--pk-t3);
}

.lr-card {
  border-radius: 34rpx;
  border: 1rpx solid var(--pk-bd);
  background: var(--pk-card);
  box-shadow: 0 4rpx 22rpx var(--pk-shadow);
  padding: 28rpx 30rpx;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  margin-top: 20rpx;
}

.lr-card-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12rpx;
}

.lr-card-title {
  font-size: 28rpx;
  font-weight: 800;
  color: var(--pk-t1);
}

.lr-card-note {
  font-size: 24rpx;
  color: var(--pk-t3);
}

.lr-card-link {
  align-self: flex-start;
  font-size: 26rpx;
  font-weight: 700;
  color: var(--pk-t1);
  text-decoration: underline;
  text-underline-offset: 6rpx;
  padding: 4rpx 0;
}

.lr-pressed {
  opacity: 0.72;
}

/* ── ① 轻量诊断卡:档位词 + 主要差距 + 进步叙述(折叠单) ── */
.lr-diag-main {
  display: flex;
  align-items: center;
  gap: 26rpx;
}

.lr-risk-ring {
  width: 142rpx;
  height: 142rpx;
  border-radius: 50%;
  border: 10rpx solid var(--pk-gauge);
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2rpx;
}

.lr-risk-gear {
  font-size: 30rpx;
  font-weight: 800;
  color: var(--pk-t1);
}

.lr-risk-label {
  font-size: 24rpx;
  color: var(--pk-t3);
}

.lr-risk-good { border-color: var(--pk-grn); }
.lr-risk-good .lr-risk-gear { color: var(--pk-grn); }
.lr-risk-mid { border-color: var(--pk-t2); }
.lr-risk-warn { border-color: var(--pk-warn); }
.lr-risk-warn .lr-risk-gear { color: var(--pk-warn); }
.lr-risk-none { border-color: var(--pk-gauge); }
.lr-risk-none .lr-risk-gear { color: var(--pk-t3); font-size: 26rpx; }

.lr-diag-copy {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.lr-diag-headline {
  font-size: 28rpx;
  line-height: 1.5;
  font-weight: 800;
  color: var(--pk-t1);
}

.lr-diag-progress {
  font-size: 24rpx;
  line-height: 1.6;
  color: var(--pk-grn);
}

.lr-diag-meta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.lr-diag-meta {
  font-size: 24rpx;
  color: var(--pk-t2);
}

.lr-diag-meta-warn {
  color: var(--pk-warn);
  font-weight: 700;
}

.lr-diag-fold-toggle {
  font-size: 24rpx;
  font-weight: 700;
  color: var(--pk-t3);
}

.lr-diag-fold {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  border-top: 1rpx solid var(--pk-bd);
  padding-top: 18rpx;
}

.lr-diag-fold-row {
  display: flex;
  align-items: flex-start;
  gap: 14rpx;
}

.lr-diag-fold-level {
  flex-shrink: 0;
  font-size: 24rpx;
  font-weight: 700;
  color: var(--pk-warn);
  border: 1rpx solid var(--pk-bd);
  border-radius: 10rpx;
  padding: 2rpx 10rpx;
}

.lr-diag-fold-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.lr-diag-fold-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--pk-t1);
}

.lr-diag-fold-meta {
  font-size: 24rpx;
  color: var(--pk-t3);
}

.lr-diag-fold-detail {
  font-size: 24rpx;
  line-height: 1.6;
  color: var(--pk-t2);
}

/* ── ⑤ 分数账本轻占位(一行,不喧宾) ── */
.lr-ledger-line {
  margin-top: 16rpx;
  padding: 0 10rpx;
  font-size: 24rpx;
  color: var(--pk-t3);
}

/* ── ② 掌握地图 40 格:四态暖色 + 蓝环第五态 ── */
.lr-map-grid {
  display: grid;
  grid-template-columns: repeat(10, 1fr);
  gap: 8rpx;
}

.lr-cell {
  aspect-ratio: 1;
  border-radius: 6rpx;
  background: var(--pk-gauge);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 掌握轨四态(暖色,待复验用赭非红,禁红灯墙) */
.lr-cell--stable { background: var(--pk-grn); }
.lr-cell--watch { background: rgba(72, 128, 106, 0.38); }
.report-page.paper:not(.light) .lr-cell--watch { background: rgba(127, 185, 157, 0.32); }
.lr-cell--reverify { background: var(--pk-warn); }
.lr-cell--unlearned { background: var(--pk-gauge); }

/* 蓝环第五态(接触轨):灰底 + 蓝描边 + 蓝勾;从对象蓝 #2f6db0 降饱和派生,
   绝不进红黄绿掌握色阶 */
.lr-cell--blue {
  background: var(--pk-gauge);
  border: 3rpx solid #4a7aab;
  box-sizing: border-box;
}
.report-page.paper:not(.light) .lr-cell--blue { border-color: #6f97bd; }

.lr-cell-bluecheck {
  font-size: 22rpx;
  line-height: 1;
  font-weight: 800;
  color: #4a7aab;
}
.report-page.paper:not(.light) .lr-cell-bluecheck { color: #6f97bd; }

.lr-map-legend {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  padding-top: 4rpx;
}

.lr-map-legend-blue {
  border-top: 1rpx dashed var(--pk-bd);
  padding-top: 14rpx;
}

.lr-legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: var(--pk-t2);
}

.lr-legend-dot {
  width: 18rpx;
  height: 18rpx;
  border-radius: 4rpx;
  aspect-ratio: auto;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.lr-legend-dot .lr-cell-bluecheck {
  font-size: 14rpx;
}

.lr-map-degraded,
.lr-map-empty {
  font-size: 24rpx;
  line-height: 1.6;
  color: var(--pk-t3);
}

/* ── ④ 趋势(方向性描述,不画假曲线) ── */
.lr-trend-text {
  font-size: 26rpx;
  line-height: 1.6;
  color: var(--pk-t1);
}

.lr-trend-empty {
  color: var(--pk-t3);
}

/* ── ⑦ 错因结构(深链错题本) ── */
.lr-error-row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16rpx;
}

.lr-error-name {
  flex: 1;
  font-size: 26rpx;
  color: var(--pk-t1);
}

.lr-error-count {
  flex-shrink: 0;
  font-size: 24rpx;
  color: var(--pk-t2);
}

.lr-error-empty {
  font-size: 24rpx;
  color: var(--pk-t3);
}

/* ── ⑥ 全页唯一行动键 ── */
.lr-absorb-btn {
  margin-top: 28rpx;
  min-height: 92rpx;
  border-radius: 24rpx;
  background: var(--pk-ink-btn);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 12rpx 30rpx var(--pk-shadow);
}

.lr-absorb-btn text {
  font-size: 28rpx;
  font-weight: 800;
  letter-spacing: 2rpx;
  color: var(--pk-ink-btn-t);
}

.lr-absorb-pressed {
  opacity: 0.82;
}

/* ── ③ 产品宣言页脚 ── */
.lr-manifesto {
  padding: 24rpx 10rpx 8rpx;
  display: flex;
  justify-content: center;
}

.lr-manifesto text {
  font-size: 24rpx;
  color: var(--pk-t3);
  text-align: center;
}

/* ── 深入页四态语义(维度/章节条,替换旧红绿硬编码色) ── */
.lr-state-strong { color: var(--pk-grn); }
.lr-state-normal { color: #6e937f; }
.report-page.paper:not(.light) .lr-state-normal { color: #8fb3a3; }
.lr-state-weak { color: var(--pk-warn); }
.lr-state-observed { color: var(--pk-t3); }

.lr-fill-strong { background: var(--pk-grn); }
.lr-fill-normal { background: rgba(72, 128, 106, 0.38); }
.report-page.paper:not(.light) .lr-fill-normal { background: rgba(127, 185, 157, 0.32); }
.lr-fill-weak { background: var(--pk-warn); }
.lr-fill-observed { background: var(--pk-gauge); }

.dot-observed { background: var(--pk-gauge); }

/* ── 学情二/三级详情统一壳 ─────────────────────────────
 * report.wxml 通过 reportDetailView 原地切换多个详情；旧实现仍携带蓝色
 * glass-card/渐变。这里仅换视觉材料，所有 read-model 与深链行为保持不变。 */
.report-page.paper.report-detail-active .nav-bar { background: var(--pk-paper); border-color: var(--pk-bd); box-shadow: none; }
.report-page.paper.report-detail-active .nav-back-arrow,
.report-page.paper.report-detail-active .nav-back-txt { color: var(--pk-grn); }
.report-page.paper.report-detail-active .nav-title { color: var(--pk-t1); }
.report-page.paper.report-detail-active .glass-card,
.report-page.paper.report-detail-active .report-detail-hero,
.report-page.paper.report-detail-active .training-loop-card,
.report-page.paper.report-detail-active .training-action-card,
.report-page.paper.report-detail-active .learning-engine-panel,
.report-page.paper.report-detail-active .engine-source-card,
.report-page.paper.report-detail-active .learning-state-section,
.report-page.paper.report-detail-active .learning-state-row,
.report-page.paper.report-detail-active .scoring-point-row,
.report-page.paper.report-detail-active .brain-stat,
.report-page.paper.report-detail-active .brain-review-summary,
.report-page.paper.report-detail-active .brain-column,
.report-page.paper.report-detail-active .attempt-card,
.report-page.paper.report-detail-active .brain-event,
.report-page.paper.report-detail-active .brain-chain-row,
.report-page.paper.report-detail-active .progress-item,
.report-page.paper.report-detail-active .battle-plan,
.report-page.paper.report-detail-active .progress-chart-card {
  background: var(--pk-card);
  border-color: var(--pk-bd);
  box-shadow: 0 2rpx 12rpx var(--pk-shadow);
}
.report-page.paper.report-detail-active .detail-evidence,
.report-page.paper.report-detail-active .detail-map,
.report-page.paper.report-detail-active .detail-training,
.report-page.paper.report-detail-active .detail-progress { background: var(--pk-card); }
.report-page.paper.report-detail-active .detail-hero-kicker,
.report-page.paper.report-detail-active .engine-kicker,
.report-page.paper.report-detail-active .learning-state-section-tag,
.report-page.paper.report-detail-active .brain-column-title,
.report-page.paper.report-detail-active .brain-next-cta { color: var(--pk-grn); }
.report-page.paper.report-detail-active .detail-hero-title,
.report-page.paper.report-detail-active .detail-metric-num,
.report-page.paper.report-detail-active .training-loop-title,
.report-page.paper.report-detail-active .engine-title,
.report-page.paper.report-detail-active .learning-state-section-title,
.report-page.paper.report-detail-active .learning-state-section-subtitle,
.report-page.paper.report-detail-active .learning-state-row-title,
.report-page.paper.report-detail-active .scoring-point-name,
.report-page.paper.report-detail-active .brain-review-title,
.report-page.paper.report-detail-active .brain-review-headline,
.report-page.paper.report-detail-active .brain-training-title,
.report-page.paper.report-detail-active .attempt-title,
.report-page.paper.report-detail-active .brain-chain-title { color: var(--pk-t1); }
.report-page.paper.report-detail-active .detail-hero-sub,
.report-page.paper.report-detail-active .detail-metric-label,
.report-page.paper.report-detail-active .training-loop-desc,
.report-page.paper.report-detail-active .engine-summary,
.report-page.paper.report-detail-active .engine-subtitle,
.report-page.paper.report-detail-active .learning-state-section-desc,
.report-page.paper.report-detail-active .learning-state-row-meta,
.report-page.paper.report-detail-active .learning-state-row-status,
.report-page.paper.report-detail-active .brain-column-sub,
.report-page.paper.report-detail-active .attempt-time,
.report-page.paper.report-detail-active .attempt-answer,
.report-page.paper.report-detail-active .attempt-explanation,
.report-page.paper.report-detail-active .brain-footer-text { color: var(--pk-t2); }
.report-page.paper.report-detail-active .engine-panel-rail,
.report-page.paper.report-detail-active .prescription-rail,
.report-page.paper.report-detail-active .scoring-rail { background: var(--pk-grn); }
.report-page.paper.report-detail-active .prescription-meta-chip,
.report-page.paper.report-detail-active .module-mini-stats text,
.report-page.paper.report-detail-active .training-loop-badge { background: var(--pk-gauge); border-color: var(--pk-bd); color: var(--pk-t2); }
.report-page.paper.report-detail-active .learning-state-cta { background: var(--pk-ink-btn); border-color: var(--pk-ink-btn); }
.report-page.paper.report-detail-active .learning-state-cta-label { color: var(--pk-ink-btn-t); }
.report-page.paper.report-detail-active .guest-report-card { background: var(--pk-card); border-color: var(--pk-bd); }
.report-page.paper.report-detail-active .guest-report-kicker { color: var(--pk-grn); }
.report-page.paper.report-detail-active .guest-report-title { color: var(--pk-t1); }
.report-page.paper.report-detail-active .guest-report-desc { color: var(--pk-t2); }
.report-page.paper.report-detail-active .guest-report-action { background: var(--pk-ink-btn); }
.report-page.paper.report-detail-active .guest-report-action-txt { color: var(--pk-ink-btn-t); }

/* ── 详情可读性 contract ─────────────────────────────────
 * 二、三级页曾直接继承旧蓝版的 glass/低字号规则；不能只换外壳，
 * 否则卡片内的证据、空态和状态标签仍会在亮底上失焦。
 * 这一段是 reportDetailView != home 的唯一材料与排版收口点。 */
.report-page.paper.report-detail-active .page-inner {
  /* custom tab bar = 140rpx + safe-area；再留 48rpx 读完最后一张卡。 */
  padding-bottom: calc(188rpx + env(safe-area-inset-bottom));
}

.report-page.paper.report-detail-active .learning-state-row,
.report-page.paper.report-detail-active .mistake-history-card,
.report-page.paper.report-detail-active .brain-truth-row,
.report-page.paper.report-detail-active .brain-diagnosis-row,
.report-page.paper.report-detail-active .brain-edge-row,
.report-page.paper.report-detail-active .brain-training-row,
.report-page.paper.report-detail-active .brain-chain-row,
.report-page.paper.report-detail-active .note-asset-row,
.report-page.paper.report-detail-active .brain-next-action {
  background: var(--pk-card);
  border-color: var(--pk-bd);
  box-shadow: none;
}

.report-page.paper.report-detail-active .attempt-card {
  background: var(--pk-card);
  border-color: var(--pk-bd);
  box-shadow: none;
}
.report-page.paper.report-detail-active .attempt-wrong {
  border-left: 6rpx solid var(--pk-warn);
}
.report-page.paper.report-detail-active .attempt-correct {
  border-left: 6rpx solid var(--pk-grn);
}

.report-page.paper.report-detail-active .learning-brain-state,
.report-page.paper.report-detail-active .scoring-point-empty {
  background: var(--pk-gauge);
  border: 1rpx dashed var(--pk-bd);
  border-radius: 14rpx;
  color: var(--pk-t2);
  opacity: 1;
}
.report-page.paper.report-detail-active .learning-brain-state {
  padding: 20rpx;
}

.report-page.paper.report-detail-active .learning-state-row.state-warn,
.report-page.paper.report-detail-active .chain-not-improved {
  border-left: 6rpx solid var(--pk-warn);
}
.report-page.paper.report-detail-active .learning-state-row.state-good,
.report-page.paper.report-detail-active .chain-improved,
.report-page.paper.report-detail-active .brain-next-action {
  border-left: 6rpx solid var(--pk-grn);
}

.report-page.paper.report-detail-active .detail-metric-row > view,
.report-page.paper.report-detail-active .prescription-meta-chip,
.report-page.paper.report-detail-active .learning-state-row-status,
.report-page.paper.report-detail-active .scoring-point-badge,
.report-page.paper.report-detail-active .attempt-result,
.report-page.paper.report-detail-active .attempt-action,
.report-page.paper.report-detail-active .brain-level,
.report-page.paper.report-detail-active .brain-event,
.report-page.paper.report-detail-active .brain-review-metric,
.report-page.paper.report-detail-active .brain-chain-outcome,
.report-page.paper.report-detail-active .note-asset-action,
.report-page.paper.report-detail-active .brain-challenge-action,
.report-page.paper.report-detail-active .brain-next-cta,
.report-page.paper.report-detail-active .mistake-history-result,
.report-page.paper.report-detail-active .mistake-history-cta {
  background: var(--pk-gauge);
  border-color: var(--pk-bd);
  color: var(--pk-t2);
  opacity: 1;
}
.report-page.paper.report-detail-active .result-wrong,
.report-page.paper.report-detail-active .learning-state-row.state-warn .learning-state-row-status,
.report-page.paper.report-detail-active .chain-not-improved .brain-chain-outcome,
.report-page.paper.report-detail-active .scoring-point-miss {
  color: var(--pk-warn);
}
.report-page.paper.report-detail-active .result-correct,
.report-page.paper.report-detail-active .learning-state-row.state-good .learning-state-row-status,
.report-page.paper.report-detail-active .chain-improved .brain-chain-outcome,
.report-page.paper.report-detail-active .note-card-action,
.report-page.paper.report-detail-active .note-card-action.primary,
.report-page.paper.report-detail-active .brain-challenge-action,
.report-page.paper.report-detail-active .brain-next-cta,
.report-page.paper.report-detail-active .note-asset-action,
.report-page.paper.report-detail-active .mistake-history-cta {
  color: var(--pk-grn);
}

.report-page.paper.report-detail-active .learning-brain-state-title,
.report-page.paper.report-detail-active .mistake-history-question,
.report-page.paper.report-detail-active .brain-row-title,
.report-page.paper.report-detail-active .brain-edge-type,
.report-page.paper.report-detail-active .note-asset-title {
  color: var(--pk-t1);
}
.report-page.paper.report-detail-active .learning-brain-state-desc,
.report-page.paper.report-detail-active .learning-state-row-meta,
.report-page.paper.report-detail-active .learning-state-row-action,
.report-page.paper.report-detail-active .mistake-history-label,
.report-page.paper.report-detail-active .mistake-history-value,
.report-page.paper.report-detail-active .brain-row-meta,
.report-page.paper.report-detail-active .brain-diagnosis-detail,
.report-page.paper.report-detail-active .brain-action-line,
.report-page.paper.report-detail-active .brain-training-meta,
.report-page.paper.report-detail-active .brain-edge-path,
.report-page.paper.report-detail-active .brain-edge-event,
.report-page.paper.report-detail-active .brain-chain-step,
.report-page.paper.report-detail-active .note-asset-summary,
.report-page.paper.report-detail-active .note-asset-source {
  color: var(--pk-t2);
}
.report-page.paper.report-detail-active .mistake-history-value.strong,
.report-page.paper.report-detail-active .mistake-history-time,
.report-page.paper.report-detail-active .attempt-time,
.report-page.paper.report-detail-active .brain-column-title,
.report-page.paper.report-detail-active .brain-review-title,
.report-page.paper.report-detail-active .brain-level-text {
  color: var(--pk-grn);
}

/* 750rpx viewport 中 24rpx 才约等于 12px；辅助信息不得再低于此阈值。 */
.report-page.paper.report-detail-active .detail-metric-label,
.report-page.paper.report-detail-active .engine-summary,
.report-page.paper.report-detail-active .engine-source-label,
.report-page.paper.report-detail-active .engine-source-status,
.report-page.paper.report-detail-active .learning-state-section-tag,
.report-page.paper.report-detail-active .learning-state-layer-label,
.report-page.paper.report-detail-active .learning-state-row-meta,
.report-page.paper.report-detail-active .learning-state-row-action,
.report-page.paper.report-detail-active .learning-state-row-status,
.report-page.paper.report-detail-active .scoring-point-badge,
.report-page.paper.report-detail-active .scoring-point-miss,
.report-page.paper.report-detail-active .mistake-history-time,
.report-page.paper.report-detail-active .mistake-history-result,
.report-page.paper.report-detail-active .mistake-history-label,
.report-page.paper.report-detail-active .mistake-history-value,
.report-page.paper.report-detail-active .mistake-history-cta,
.report-page.paper.report-detail-active .learning-brain-state-desc,
.report-page.paper.report-detail-active .brain-column-sub,
.report-page.paper.report-detail-active .brain-level-text,
.report-page.paper.report-detail-active .brain-row-meta,
.report-page.paper.report-detail-active .brain-diagnosis-detail,
.report-page.paper.report-detail-active .brain-action-line,
.report-page.paper.report-detail-active .brain-challenge-action,
.report-page.paper.report-detail-active .attempt-time,
.report-page.paper.report-detail-active .attempt-result,
.report-page.paper.report-detail-active .attempt-answer,
.report-page.paper.report-detail-active .attempt-diagnosis,
.report-page.paper.report-detail-active .attempt-explanation,
.report-page.paper.report-detail-active .attempt-action,
.report-page.paper.report-detail-active .brain-chain-step,
.report-page.paper.report-detail-active .brain-chain-outcome,
.report-page.paper.report-detail-active .brain-footer-text,
.report-page.paper.report-detail-active .note-asset-summary,
.report-page.paper.report-detail-active .note-asset-source,
.report-page.paper.report-detail-active .note-asset-action {
  font-size: 24rpx;
}
