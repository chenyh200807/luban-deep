# Capability Contract

## 范围

这一份 contract 管：

- capability 路由决策
- capability request config schema
- orchestrator 选择 capability 的规则
- registry 作为唯一 capability 注册入口

## 单一控制面

- 单一 capability 入口：`ChatOrchestrator`
- 单一 capability schema 源：`deeptutor/capabilities/request_contracts.py`
- 单一 capability 注册入口：`CapabilityRegistry`

## 硬约束

1. capability 选择必须由 orchestrator 主导，router 不得偷偷维持第二套主路由逻辑。
2. capability config 不得在不同入口使用不同字段名表达同一语义。
3. 公开 capability config 必须先过 `request_contracts.py` 校验。
4. 新 capability 如果有公开配置，就必须补 schema 和 request validator。
5. adapter 可以做输入归一化，但不能成为 capability 决策的真实来源。
6. semantic router / rollout mode / shadow decision 也属于 orchestrator 的 capability 控制面；`mobile`、`unified_ws` 这类 adapter 只能传递 hints / auth / transport metadata，不能并行决定 capability。
7. adapter 如果需要把 token claims、wallet identity 或旧字段 alias 归一到 canonical 用户上下文，也只能服务于统一 request config 装配；不能把 capability 选择下沉到 adapter 本身。
8. 响应风格公开字段只能使用 `requested_response_mode`；`teaching_mode` 若仍被旧入口传入，只能在 adapter 层归一化并删除，不能继续作为 capability config 或路由决策字段存在。
9. 请求里的 `capability` 只允许作为 hint；最终写入 turn/session 的 capability 必须是 orchestrator runtime-resolved canonical capability，不能把 request hint 当成持久化真相。
10. adapter 可以做 presentation / timestamp / conversation read-model 装配，但不得在装配层重新决定 capability、改写 canonical final answer、或把 presentation blocks 当作 capability 执行结果的新 authority；adapter 输出必须来自 runtime-resolved turn/session/message 真相。
    - mobile history serialization may project `turn_id`, `client_turn_id`, and assistant `engine_turn_id` from persisted message metadata so a backgrounded client can reconcile its pending turn. If an assistant row has empty `content` but contains the same public-safe terminal answer in `response` / `assistant_content` metadata, the adapter may project that answer into display `content` for recovery. It must not treat arbitrary `metadata.content` or `metadata.request_snapshot.content` as assistant answer authority. These are read-model projections only; they must not influence capability selection, request config, scoring, learner-state writes, or create a second answer authority.
11. `exam_track` 这类领域上下文只能作为 request config / interaction_hints / metadata 的 scoped input 进入 orchestrator 和 capability；它不得改变 capability 选择权威，也不得被 adapter 用来创建平行 capability。
12. capability 只能看到 runtime 当前可用的工具；`web_search` 关闭或未配置时，registry 必须把它从 schema、prompt hints 和 enabled tools 中过滤掉。入口可以传递用户显式联网意图，但不得绕过 runtime availability authority，也不得把显式工具请求直接升级为 `current_info_required`；当前信息需求必须由 query intent / grounding decision 统一判定。
12a. hosted Luban 教学卡的小窗答疑必须复用 mobile 的 turn bootstrap，并以 `capability=None` 交给 `ChatOrchestrator` 选择最终 capability；卡片 adapter 不得用专属 prompt、`chat_mode=fast` 或浏览器场景字段预选/锁死 TutorBot、`deep_question` 或其他 capability。`luban_teaching_card_context` 仅是运行时受限的学习位置输入，不能作为 capability 决策或题目生命周期 authority。
13. 练题 / 出题类 follow-up 的公开请求配置仍由 orchestrator 归一化：入口可以传入题量、题型、topic 等 hint，但 orchestrator 必须保留显式 config，不得用重新推断覆盖已有非空值。
    - “同考点 / 不同考点 / 换个考点 / 相关题 / 类似题”这类相对练题请求不是独立 topic authority；必须由 practice-generation topic authority 继承当前 active question、suspended learning object 或 conversation anchor 后再交给 `deep_question` / coordinator。不得把裸“不同考点”当成可提交题源，也不得在 visible sink 用“非建筑实务主题”模板补救；若缺锚点，按缺少具体考点澄清，而不是生成或拒成非建筑。
    - `case_grading` 的 capability routing 不等于评分 authority。若 `question_lifecycle_scene=case_grading` 已由前置稳定事实写入，`resolve_question_lifecycle_scene_decision` 必须尊重该事实并补齐 lifecycle decision / skill metadata；orchestrator 可按执行环境选择 `deep_question` 或 `tutorbot`（无 TutorBot 默认绑定时不得落入普通 `chat`），但 `score`、`missed point`、`deduction reason`、逐采分点裁决只能来自同一个鲁班 V1 / `rubric_grader_v1` event。TutorBot 只负责对话承接、渲染和流式输出，不得用 free text 自行判分或补官方扣分理由。
14. orchestrator 从自然语言推断出的 `num_questions`、`question_type`、`lightweight_generation` 只属于本次 capability request config；它们不得成为 session / learner state 的第二份长期真相。
15. 批量出题请求不能因为上一题已经作答或已批改而退回 grading path；生成更多题目的 intent 必须收敛到 capability routing / request config，而不是 adapter 或 presentation 层重复判定。
16. 当同一用户消息同时包含当前题目的可解析作答和“下一题 / 继续练”类训练请求时，orchestrator 必须先保持 `deep_question` 的 submission/grading 路径；训练生成只能作为后续动作，不能抢在当前作答批改之前改写为 practice generation config。
17. 对上一轮出题 / 巩固邀请的短肯定回复或复述，属于 capability route 的语义承接问题；必须先由 orchestrator 的统一 semantic decision 归一为 `practice_generation` 候选。只有语义结果是普通聊天时，`bot_id` 默认绑定才可以让现有 TutorBot runtime 作为执行引擎。
17a. 对已批改 active question 给出的“下一步 / 继续巩固”建议，`interpret_question_followup_action` 必须同时看到 history、批改状态与 canonical `construction_grading_result.next_training_signal`，并把有充分上下文的接受语归一为 `generate_more_questions`。该 action 的 `topic` 必须优先投影自 `next_training_signal.concept`，其次才是 `focus` 或当前题的 canonical concentration；禁止只修通 generation route 却把原始短词“下一步”当 topic，造成跨考点漂移。确定性短语只允许在语义解释不可用时作窄 fallback，且必须同时存在已批改证据、没有当前作答提交。QTPK 写出的 generation/grading/followup/clarify 强 `turn_semantic_decision` 不得被 orchestrator 的第二次弱语义结果覆盖。
18. `mobile` adapter 的 billing / wallet 端点只能做认证身份、wallet identity、额度 fail-closed 和展示 read-model 装配；不得把钱包、旧会员流水或展示层状态升级为 capability routing / request config authority。旧会员流水只能通过显式迁移/对账开关参与流水展示。`/api/v1/billing/usage` 的余额事实来自 wallet snapshot，百分比分母来自当前会员 entitlement tier 对应的 canonical membership package catalog `points`；wallet `plan_id` 只是投影，历史 ledger/grant/merge 与前端分页都不得重建分母。snapshot、会员 entitlement 或 catalog 读取失败时返回 `status=degraded`。`DEEPTUTOR_BILLING_ENFORCEMENT_ENABLED` 默认开启；显式配置 false / 0 / off / no 才进入内测或回滚放行。当 enforcement 开启时，实际收费的 canonical commerce authority 是 wallet ledger，但 `/billing/usage` 不读取 ledger；ledger 明细由独立 `/billing/ledger` read model 提供，失败不得改写余额百分比。余额为 0 且 wallet snapshot 存在时，`/api/v1/chat/start-turn` 可用非财务 `MemberUsageMeter` 写入有限免费试用 reservation，并把 server-authored marker 传给 turn runtime 跳过本轮钱包扣费；免费试用资格、超额和 meter 失败都只属于 commerce fail-closed 边界，不能参与 capability routing、request config 语义、learner-state 或评分 authority。当该 flag 显式关闭时，内测学员不扣费、不写 wallet ledger，`/api/v1/billing/usage` 可以读取非财务 `MemberUsageMeter` 形成 `usage_source=member_usage_meter` / `charging_status=metered_not_charged` 展示投影；该展示暂以所有会员 450 次为 100%（`DEEPTUTOR_INTERNAL_BETA_USAGE_LIMIT_TURNS` 可调），每条 meter turn 只算 1 次，不按实际成本点数放大，但公开 display label 仍必须按百分比展示，不直接展示剩余次数。这个 meter 只能表达学员用量观察，不得成为余额、扣费、capability route 或 request config authority。开启计费后 `/api/v1/chat/start-turn` 在 `start_turn` 之前做硬余额 fail-closed（见 `contracts/turn.md`）：余额不足或缺失 wallet id 必须拒绝，wallet service 未配置或不可用必须 fail closed；但该额度门只是 commerce 层 fail-closed，不得成为 capability route 或 request config authority。
    - `/api/v1/billing/checkout` 的微信 JSAPI 下单与 `/api/v1/billing/wechat/notify` 的支付回调也只属于 commerce HTTP adapter：checkout 只用已认证用户、服务端持有的 `wx_openid`、wallet/package 读模型和微信支付配置生成 provider `payment.params`；notify 只验证微信回调、解析 `attach` 并委托 `MemberConsoleService.manual_membership_purchase` / wallet ledger 写入权益。两者不得创建 turn/session，不得改写 capability route、request config、learner-state 或 scoring authority。
18a. 免费试用 reservation 的生命周期也只属于 commerce 边界：`free_trial_reserved` 防并发超领，成功可展示的 terminal answer 才终结为 `metered_not_charged`，provider raw error / 内部错误 / failed fallback 必须释放为 `free_trial_released`。这些状态只能影响免费试用计数和 billing read-model，不得被 capability、TutorBot、评分、learner-state 或 request config 读取为路由或教学策略 authority。
18b. 教学视频权益只由 `/api/v1/billing/wallet.teaching_video_limit` 投影：无有效会员或已过期为 20，`starter_19` 为 30，`light_98/vip/svip/supreme_svip` 为 `null`（无限）。该投影必须使用 canonical wallet identity 的只读 entitlement read model；不得调用会 bootstrap / 修复 / 保存会员的 profile reader。teaching-points 只能消费该字段；字段缺失、请求失败或非法时 fail-closed 到 20，不得在前端重算 tier。
19. `mobile` adapter 的 learning-brain projection 端点只能暴露 learner-state 已编译投影的 read-model；不得由 adapter 推断 capability、合成 compiled truth、触发 RAG，或把 projection 读取结果作为新的 capability authority。
20. `mobile` adapter 的 learning-report / learning-attempt detail 端点只能暴露 learner-state read model。`/mobile/learning-report` 允许做 `schema_version=1|2` / `Accept ...;v=2` 的 HTTP schema 协商，但不得据此触发 capability 或在 router 中重组学习事实；`/mobile/learning-attempts/{attempt_ref}` 不得触发 capability、不得解析训练意图、不得在 router 中重组学习事实；router 只做 auth、调用 read model、HTTP status 映射。
21. `mobile` adapter 的 mistake-book endpoints 只能把 user auth、`attempt_ref`、subject/bot 参数和 `If-Match` 交给 learner-state mistake-book service；不得从页面 key 推导 event id，不得在 adapter 中缓存 bookmark truth，也不得触发 capability。
22. `lightweight_generation` 是 `deep_question` 的 per-turn execution-strategy 配置项（详见 `docs/plan/题目生命周期与助教运行时/2026-05-20-luban-lightweight-practice-deep-grading-execution-plan.md`）。其唯一规约函数是 `deeptutor/tutorbot/teaching_modes.classify_practice_strategy`：fast/smart 模式下普通"再出 N 题 / 继续练 / 再来几道"默认 lightweight（1 ≤ N ≤ 5），仅当用户消息命中 heavy keyword（详细解析 / 命题依据 / 模拟真题 / 完整案例）、`reveal_preference=True`、`mode=deep` 或 `num_questions` 越界时回退到 heavy。orchestrator 在 `_prepare_practice_request_context` 是唯一调用方；coordinator / generator / 其它下游模块只能读取 `config_overrides["lightweight_generation"]`，不得另行判断。判断结果只属于本次 turn 的 capability request config，禁止写入 session / learner state 作为长期真相。
23. `ChatOrchestrator.handle()` 必须保证 outer cancel（turn timeout、FastAPI client disconnect、`GeneratorExit`）能向内部 capability task 传播：捕获 `CancelledError` / `GeneratorExit` 时取消 task、在 `DEEPTUTOR_CANCEL_GRACE_S`（默认 2 秒）内等待收尾、关闭 stream bus，并将 `turn_cancel_propagated=True` 写入 `context.metadata`；`_publish_completion` 必须在正常完成与取消两条路径都触发（放 `finally`）。禁止内部 capability task 在 parent turn deadline 之后继续调用 LLM。
24. capability 产出的 grading / explanation 结构体（含 `MCQGradingResult.evidence_refs`、`CaseRubricItemResult.source_fields` 等）允许在服务端内部保留完整 hidden authority；但 `/api/v1/ws` 公开出库前必须经统一 redaction（详见 [contracts/turn.md](./turn.md) §硬约束 13）：evidence-style entry 中 `field` / `source_field` / `source_key` / `name` 指向 hidden authority 的整项 drop；`source_fields: list[str]` 中 hidden 元素过滤；不允许 capability 自行另起 transport-side rewriter 绕开统一 redactor。
25. `mobile` / home dashboard 在 capability 尚未 resolved 前可以把点击意图暂存为 `learning_prompt_intent`。一旦 orchestrator 将该 turn 判定为练题生成（例如"先做一次摸底测评"、"小测"、"自测"等 starter / review prompt），必须在 `_prepare_practice_request_context` 边界把该 intent 提升为 `learning_training_intent` 交给 `deep_question`，并在用户本轮没有显式题数、也没有已有有效非默认 `num_questions` 时使用其中的 `question_count`。若本轮自然语言明确要求题数（如"用 3 道题训练水泥"）或入口已给出有效非默认 `num_questions`，该本轮 request config 是题数 authority，`learning_training_intent.question_count` 只能作为训练处方默认值，不得二次覆盖。普通 TutorBot runtime 不得直接生成可提交练题题卡；可提交题卡必须来自 `deep_question` 的 QuestionArtifact / hidden grading authority 主链路。
26. TutorBot runtime 的自由文本不得被 presentation 层抽取并升级为可提交 `mcq` 题卡。TutorBot 只有在命中已有 `exact_question` / questions-bank authority、能构造服务端 `question_followup_context` / `active_object` 时，才允许输出 submit-able MCQ presentation；否则只能作为普通教学文本展示，避免 presentation 成为第二套题目 authority。
27. 题目生命周期场景与 skill 注入的唯一 authority 在 `deeptutor/services/question_lifecycle_skills.py`（详见 `docs/plan/题目生命周期与助教运行时/2026-05-24-deeptutor-question-lifecycle-skill-authority-execution-plan.md` 与 `docs/plan/题目生命周期与助教运行时/2026-05-26-deeptutor-question-lifecycle-authority-consolidation-plan.md`）。`SCENE_COMPOSITION` 是 scene → skill stack 的唯一组合表；`resolve_question_lifecycle_scene_decision` 是 orchestrator 可调用的唯一 scene 决策入口：deterministic helper 只能收集稳定事实与安全闸门，LLM scene assistant 只能提出结构化语义候选（`llm_scene_candidate`），最终 scene / route 必须由 `QuestionLifecycleSceneDecision` 的 business gate 裁定。LLM 不得执行出题、批改、讲评或改写答案泄露策略。该 lifecycle decision 必须先于 TutorBot preselected capability / bot_id 默认绑定运行，且即使已有 active question / active object 也必须记录本轮 decision；TutorBot 是业务身份和执行引擎，不得绕过题目生命周期裁判，`chat` / `tutorbot` 请求里的 capability 只能作为入口 hint，必须先降级后交给 orchestrator 决定最终执行 capability。orchestrator 必须把 `question_lifecycle_decision`、`decision_source`、`llm_scene_candidate`、`business_gate_result`、`scene_confidence`、`required_anchor_status`、`exact_question_blocked_reason`、`selected_skill_names` 写入 turn metadata / trace metadata，供下游只读消费。capability / adapter / TutorBot loop 等下游只允许 read `context.metadata["question_lifecycle_scene"]` 和 `context.metadata["question_lifecycle_skill_names"]`，不得自建第二套 scene → skill 映射、不得在 capability 入口外重新判定 scene，也不得把 skill_context 的 scene 写回覆盖 orchestrator decision；trace / skill projection 只能 mirror 已有 decision。`QUESTION_LIFECYCLE_DECISION_AUTHORITY` 是默认开启的生产 emergency kill-switch，关闭时只能作为临时回退旧路由链路使用，正常验收和发布必须在开启状态下完成。明确要求系统出题、练题、测试学员的自然语言（例如"用 3 道题训练项目质量计划管理"）必须归入 `practice_generation`，并绑定 `construction-question-supply`；除非用户显式要求带答案或解析，否则 orchestrator 必须把 `reveal_answers=false` / `reveal_explanations=false` 交给 `deep_question`，避免练题生成泄露答案。无 active question 的自由文本 `question_review`（例如"分析一道验槽方法真题"、"用一道真题场景理解地基与基础"）必须由 orchestrator 路由到 `deep_question` 的题目讲评路径；orchestrator 只能写 scene / skill metadata，不得把该 scene 改写成 practice generation policy。低信息考试查询（例如"2025真题"、"历年真题"、"防水真题"）只能进入 clarification / catalog 语义，必须带 `required_anchor_status=missing_question_anchor`、`business_gate_result=blocked_low_information_exam_query` 与 `exact_question_blocked_reason=low_information_exam_query`；orchestrator 必须把该澄清写成 canonical `question_lifecycle_clarification` / `active_object`，用户下一轮输入"1"、"查看这一类真题目录或考点范围"或带原 topic 复述该选项时，必须由同一个 `resolve_question_lifecycle_scene_decision` 解析为 `exam_catalog_query`，而不是重新触发低信息拒答或被 legacy selector / TutorBot exact path 抢走。`exam_catalog_query` 只能回答目录、范围、可选下一步和不编造具体标准答案的安全提示，不得输出"阅卷结论 / 标准答案 / 命中题库原题"。只含年份/历年/题号但没有题干、选项或明确 topic anchor 的显式讲评请求（例如"分析一道2025真题"、"解析2025真题第15题"）同样属于低信息考试查询，不能让系统自选随机题直接讲评。无 active question 的"我选B"这类作答只能进入 clarification，必须带 `required_anchor_status=missing_active_question`；已有多题 active question 时，未带题号的单选答案（例如"我选B"）必须进入 clarification，带 `required_anchor_status=ambiguous_question_anchor` 与 `exact_question_blocked_reason=ambiguous_multi_question_answer_submission`，不得默认批改第 1 题。用户消息本身已包含完整 free-text MCQ 题干、选项和作答时，即使选项文本含内部逗号或句读，也不是无锚点提交；lifecycle 不得写 `unanchored_answer_submission` 阻断 exact-question authority。上述缺锚点输入不得触发 exact-question authority、不得输出"阅卷结论 / 标准答案 / 命中题库原题"，且不得被 preselected `deep_question` 或 legacy selector 偷走。`deep_question` 只能在 questions_bank / RAG exact-question 高置信命中时物化题干/选项并以 `review_mode=true` 展示答案解析；如果没有命中原题，必须诚实要求用户补充完整题干/选项，不得调用 Idea/Generator 虚构真题或输出可提交练习题卡。已有 active question 的批改/追问回合默认以 active question context 为 authority，TutorBot loop 不得把整张题卡再次送入 RAG 预取或工具循环；只有显式实时资料需求才允许恢复检索。未作答 active question 的直接要答案请求必须 fail-closed，仅在已有作答或用户明确"放弃/跳过这题"时允许展示参考答案/解析。`derive_question_lifecycle_scene` / `select_question_lifecycle_skill_names` 必须保持 import-safe 纯路径，不得在模块加载或 scene 判定时引入 TutorBot skill loader 的可选依赖；`SkillsLoader` 只能在 instruction builder 需要读取 skill 正文时惰性加载。`SkillsLoader` 与 `question_lifecycle_skills` 之外的任何模块**不得**直接读 `construction-*/SKILL.md` 文件（lecture skill 的 `_read_skill_file` 是历史 carve-out，与本条款无关）。
    - 2026-06-08 补充：上条中“没有命中原题”的执行解释不是“只要无原题就拒答”。当 topic-qualified `question_review` 已从 questions_bank/RAG 拿到相似题或权威知识来源时，`deep_question` 可以调用 Generator 生成明确标注的 source-backed 变式讲评卡；metadata/trace 必须写 `similar_question_variant` / `question_review.variant_mode=true`，不得声称题库原题或历年真题命中。只有连相似来源也没有时，才要求用户补充完整题干/选项。
    - 2026-06-12 补充：`_SCENE_REFERENCE_FILES` 是 scene → skill reference 注入的唯一映射表，与 `SCENE_COMPOSITION` 同级受本条款约束。学情轴 scene（`learning_evidence_story` / `study_assistant` / `learning_support`）分别注入对应 skill 的 references（degraded-claims / action-selection / support-playbook）。新增 scene reference 必须在该表登记并在 `tests/services/test_question_lifecycle_skills.py` 附带注入断言；capability / adapter / TutorBot loop 不得绕过该表自行读取 reference 文件。
    - 2026-06-26 补充（study-assistant authority / 学习计划不编造画像）：用户请求“复盘计划 / 学习计划 / 训练计划 / 备考计划 / 下一步学习安排”时，`derive_question_lifecycle_scene` 必须归入 canonical `study_assistant` scene，并加载 `construction-study-assistant`。普通 TutorBot / `construction-exam-tutor` 只能做教学表达，不能在无 `PersonalizationContextPack`、training intent、study plan、attempt detail 或 learner-state read projection 的情况下自行断言“入门摸底 / 已做 N 题 / 错 N 题 / N 个章节未开始”等学习画像或作答历史。证据不足时，`study_assistant` 只能按其 degraded 规则给摸底或最小下一步动作，并明确记录不足；不得把内部画像提示、对话观察或模型推测升级成 learner truth。旧 active question / sticky `deep_question` 不得抢走学习计划请求。回归见 `tests/services/test_question_lifecycle_scene_derivation.py::test_study_plan_requests_return_study_assistant_not_generic_chat`、`tests/services/test_question_lifecycle_acceptance.py::test_tutorbot_study_plan_intent_loads_study_assistant_authority` 与 `tests/runtime/test_orchestrator_autoroute.py::test_orchestrator_study_plan_overrides_stale_question_context`。
    - 2026-06-26 补充（study-assistant terminal no-evidence gate）：`question_lifecycle_scene=study_assistant` 已写入但本轮没有 `compiled_learning_truth`、`personalization_context`、training intent、study plan、`next_best_action`、evidence refs、recent attempts 或 attempt detail 时，`TutorBotCapability` 不得再调用 generic full-agent 生成学习画像；必须由 `question_lifecycle_skills` / `construction-study-assistant` authority 直接返回“当前记录不足 + 通用复盘计划”的 terminal response，`actual_tool_rounds=0`，并保留 lifecycle telemetry。存在结构化 learner evidence 时不得误杀原 agent path。回归见 `tests/capabilities/test_tutorbot_authority.py::test_tutorbot_study_assistant_without_learning_evidence_degrades_without_llm_call` 与 `tests/capabilities/test_tutorbot_authority.py::test_tutorbot_study_assistant_with_learning_evidence_keeps_agent_path`。
    - 2026-06-24 补充（意图单一权威 / R2 意图误路由收口，第一性病名 `intent-fast-path-as-authority`）：判「本轮意图是判分 / 生成新题 / 答疑」是 `derive_question_lifecycle_scene` 的唯一职责。**当一条自由文本消息里"可判分的学生作答 payload 在场"（`_FREE_TEXT_CASE_ANSWER_MARKER_RE` 命中且答案体非空）且带判分诉求（`_FREE_TEXT_GRADING_ACTION_MARKERS`：作答/采分点/阅卷/打分/判分/批改…）时，意图必须裁为 `case_grading`，胜过练题生成**——即便消息含"考点 X / 选择题 / 简答题"等会命中过宽练题检测器 `looks_like_practice_generation_request` 的话题词。`_looks_like_free_text_case_grading` 的正式案例壳（`_FREE_TEXT_CASE_GRADING_CONTEXT_MARKERS`）与"作答 payload 在场"是**正向并集**（其一即可）+ 判分动作；这是正向"作答在场→判分优先"信号，**禁**枚举"别出题/别给我出选择题"等否定词、**禁**给练题检测器补排除正则（那是第 N+1 decider，违背收权）。合法练题（"再出 3 题"/"出一道 X 考点题"，无作答 payload）天然不命中 answer-marker → 仍 `practice_generation`，零误伤。`semantic_router` 的无 active-object 练题短路（`_decision_from_active_open_chat_object` 等）只在 lifecycle scene 缺失时兜底，不得在 `derive_question_lifecycle_scene` 已裁 `case_grading` 时独立抢判成生成。
    - 2026-06-17 补充（"做完题没给答案"根因修复）：`mcq_grading` 作答提交轮**不得被非判分能力的 preselected capability 绕过吞掉**。`case_grading` 在 `_select_capability` 有独立分支、永远先于 preselect 运行；`mcq_grading` 此前缺对称处理，会 fall through 到 `_select_capability_after_lifecycle` 的 `if context.active_capability:` bypass —— 而 `_prepare_preselected_capability_context` 只为 `deep_question` 准备提交上下文，导致微信默认 `active_capability=tutorbot` 时 MCQ 作答被当成普通 preselected turn、判分永不触发。修复：当 `question_lifecycle_scene == "mcq_grading"` 且 preselected capability 不是 `deep_question` 时，orchestrator 必须放弃该 preselect bypass、落到判分能力路由（semantic router → `deep_question` `route_to_grading`），并在 metadata 写 `mcq_grading_preselect_bypass_recovered`。score authority 不变（鲁班 V1 / `rubric_grader_v1`）。回归见 `tests/runtime/test_orchestrator_autoroute.py::test_orchestrator_routes_mcq_grading_through_lifecycle_over_preselected_tutorbot`。
28. 个人学情状态类自由文本（例如"我最近学的怎么样"、"我的学情怎么样"、"我当前薄弱点是什么"、"我今年学习进度怎么样"）必须归入 canonical `learning_evidence_story` scene，并只读取 learner-state read model / `compiled_learning_truth` / `memory_context` 等已存在事实。除非用户显式要求联网或同时命中公共考试政策、报名、教材变化等外部实时信息边界，这类查询不得被升级为 `current_info_required`，也不得由 adapter / TutorBot loop 触发 `web_search`。TutorBot runtime bridge 只能把 `memory_context` 与 `compiled_learning_truth` 作为只读输入转发给主 loop / RAG，不得写 learner state、不得生成第二套学习事实。
29. `mobile` adapter 的 assessment TestSet 目录端点 (`/api/v1/assessment/topics`) 只能暴露 `assessment_forms` / blueprint coverage 派生的 read model，用于选择 topic TestSet。它可以附带 learner-state 派生的 display-only recommendation，但不得触发 capability、不得启动 turn、不得根据 learner state 动态生成题目，也不得把 catalog status 或 recommendation 升级为学习计划或 mastery authority。
30. `mobile` adapter 的 assessment 题目详细解析端点 (`/api/v1/assessment/{quiz_id}/items/{question_id}/explain`) 只能读取已提交 assessment session 的 result report 和服务端隐藏评分 authority，返回 post-submit explanation projection。它可以在该 HTTP 请求内做一次非流式 LLM 解析生成，并按 usage / fallback minimum 通过 wallet authority 捕获点数；扣费结果只是 commerce ledger 事实，不得成为 capability route、assessment score 或 learner-state truth。它不得触发 `ChatOrchestrator`、不得启动 capability、不得调用 `/api/v1/ws` 之外的聊天链路、不得生成新正式题、不得修改分数 / mastery / training_intent。若未来需要流式或交互式追问解析，必须回到统一 `/api/v1/ws`，本端点只能保持 post-submit projection。

31. 统一流式入口 `/api/v1/ws` 对单条入站文本帧设有应用层硬上限（`unified_ws._MAX_WS_INBOUND_FRAME_CHARS`，当前 128K 字符）：超限帧在 `json.loads` 之前 fail-fast 拒绝并返回 `{"type":"error","content":"Message too large …"}`，不静默截断、不进入 capability / turn 链路。该上限是请求边界防御（阻止单个已认证客户端用接近 16MiB 的 payload 放大内存 / LLM 成本，受 start_turn 10次/60s 限流软约束之外的硬边界），适用于所有消息类型；客户端须将单帧控制在上限内，超长内容走附件 / 分段而非超大 content / config。等价地，mobile HTTP 入口 `/api/v1/chat/start-turn`（turn 的第二个入站边界）同样设 query 字符硬上限（`mobile._MAX_MOBILE_START_TURN_QUERY_CHARS`，128K，超限 422）+ 10次/60s route 限流（`route_rate_limit("mobile_chat_start_turn")`），与 WS 入口单点同治理，避免 F5 同形放大面在 HTTP 侧重开（H9）。旧小程序包路径 `/api/v1/mobile/chat/start-turn` 与 `/api/v1/mobile/chat/start` 只允许作为同一 mobile HTTP bootstrap adapter 的隐藏兼容 alias；adapter 不得因 alias 重新决定 capability、不得改变 request config 语义、不得创建第二套 chat/capability route。
31a. mobile auth adapter（账号密码注册、手机号验证码登录、微信 `getPhoneNumber` 快速登录 / 绑定）只能把入口凭证收敛为 canonical member identity。注册必须要求有效大陆手机号；同一手机号只能绑定一个 canonical account；验证码登录和微信快速登录必须复用同一可信 `phone` alias，不得让 `wx_openid`、用户名、设备号或客户端字段生成第二套免费试用 / billing / learner identity。免费试用窗口只读该 canonical identity 的 `MemberUsageMeter`，不得由 capability、聊天入口或客户端状态另行判定。
32. `/api/v1/ws` 的 subscribe / unsubscribe / disconnect 清理只属于 transport fan-out；它不得参与 capability 选择、不得改写 runtime-resolved canonical capability，也不得把 failed live subscription task 误报为 capability 执行失败。subscription task registry 必须按 task identity 清理，避免旧 task callback 误删同 key 新 task。
33. `case_grading` scene 的评分输出必须继续服从 question lifecycle + 鲁班 V1 construction grading authority。`question_lifecycle_skills.py` 可以用稳定文本锚点识别自由文本案例批改请求并注入 `construction-case-grading` skill，例如 `背景资料/背景信息/案例背景` + `作答`、`【问题】` + `回答/作答：` 这类学生答卷格式；公开 predicate（如 `looks_like_full_case_answer_submission`）只允许作为 turn runtime 的当前题面优先输入证据，防止旧 active-object / followup context 抢占本轮案例题。runtime 可以用严格同题题面校验保留真实同一题的 hidden authority，但不得用模糊相似度把旧题参考答案并入当前批改；predicate 与同题校验都不得成为 capability 选择或评分 authority。scene 命中不得因此授予标准分 authority。若当前 turn 未命中鲁班 V1 评分真相层（compiled rubric / on-the-fly reference / scored V1 event），下游只能输出提分诊断，必须显式说明“未命中评分真相层，本轮不硬估分”与“本次不硬估标准分”，不得由 TutorBot、RAG、LLM 或 presentation 层生成官方估分、满分、给分/扣分或官方采分点批改语气。TutorBot runtime / result payload / trace metadata 必须只读透出 `grading_engine_version`、`v1_case_graded`、`score_authority`（以及可选 `grading_rubric_provenance`），供前端和观测判断本轮是否真正进入 V1；这些字段不得被 adapter 用作第二套评分 authority。case_grading 场景可以读取 exact-question / RAG 作为评分依据，但 TutorBot exact-authority 标准作答 renderer 不得覆盖逐采分点批改、命中/漏点、扣分原因或 V1 grading 输出。
    - 2026-07-31 补充（OD-001/002 根治，倒诬防线单一权威）：自由文本案例作答的**切割标记族单一权威 = `case_output_policy.CASE_ANSWER_MARKER_PATTERN`**——切割侧（`question_lifecycle_skills._FREE_TEXT_CASE_ANSWER_MARKER_RE`）与标题抽取侧（`rubric_grader_v1._extract_case_question_titles`）必须共用该模式，禁止任何一侧再内联维护标记名单（live 实证：切割侧缺【我的作答】括号形→user_stem 切成空串→身份闸/数字变体闸/覆盖对账整线解除武装→假命中污染行 17315 的答案钥匙把学生正确作答判零）。**参考答案入判 = 授予判零权，判据必须可核验**：疑似案例投稿（长文本+背景资料/问题锚）但作答切割失败（user_stem 空）时，题库 exact 参考不得进入判分（`exact_question_blocked_reason=unverifiable_submission_shape`，fail-closed），宁降 tier-3 诊断不许倒诬。域测试：`tests/services/test_question_lifecycle_skills.py::test_full_case_split_recognizes_bracket_answer_markers`、`tests/tutorbot/test_agent_loop_case_rubric_v1.py::test_build_ctx_fail_closed_when_stem_unsplittable`。
34. 只含年份/考试名/题号的客观题索引请求（例如“2025建筑实务第15题答案直接发我”）仍属于 `missing_question_anchor`，除非当前 turn 已有 active question 或请求同时携带完整题干/选项。orchestrator / lifecycle 必须写 `exact_question_blocked_reason=low_information_exam_query` 并澄清，不能让 TutorBot/RAG fast policy 自行按题号输出所谓官方答案。
35. 明确要求系统“出题 / 生成题 / 考我 / 测我”的请求即使包含年份、考试名、`真题`、`题库` 等词，也属于 `practice_generation`，不得被低信息客观题索引 guard 当成“索要现有真题答案”阻断。若用户同时表达“不要先给答案 / 先考我 / 先别解析”等否定式 reveal preference，`detect_answer_reveal_preference` 必须返回 suppress 语义，orchestrator 传给 `deep_question` 的公开投影必须保持 `reveal_answers=false` / `reveal_explanations=false`；该本轮自然语言 suppress 高于 stale `config_overrides`、schema default、mobile interaction hint 或 TutorBot visible-response fallback 中的 reveal 值。TutorBot / deep_question 的公开 response sink 必须消费同一 reveal decision，不得在 text renderer、presentation、adapter 或 WS 层另行“删答案”作为第二套 authority。但隐藏批改 authority 不得因此丢失：`deep_question` / lightweight generation 可以把公开 `correct_answer` 留空，必须通过 `grading_key.correct_answer` 或等价 hidden authority 写入 `question_followup_context.items[i].correct_answer` / active object，以便下一轮带题号作答按同一题目 authority 批改。公开 presentation / adapter / WS redaction 仍必须剥离 `correct_answer`、`grading_key`、`explanation`、`scoring_points`。
    - 2026-06-28 补充（出口科目门口径对称 / subject-scope exit gate symmetry，第一性病名 `subject-gate-positive-whitelist-as-authority`）：出题科目守门的唯一判据是 `deeptutor/tutorbot/teaching_modes.practice_generation_topic_domain_status`，入口门 `practice_generation_topic_block_decision` 与出口门 `AgentCoordinator._generated_questions_in_construction_scope` 必须**同口径**。出口门只能在生成题**确有他科证据（命中 `_OUT_OF_SCOPE_PRACTICE_TOPIC_MARKERS` = out_of_scope_topic）且无任何建筑证据（无 construction_topic）**时判 `subject_unavailable`；`unknown_topic`（建筑关键词白名单未覆盖的建筑长尾，例如“水泥 / 沟槽开挖 / 盘扣架”）一律视为 in-scope 放行。**禁止**出口门用“正向白名单命中（`any(== construction_topic)`）才算 in-scope”的口径——那等同入口门已在源码注释里明确否定过的 `!= construction_topic` 误拒（白名单永远不全，正向口径会把同主题的不同表述当跑题诚实拒答，重蹈覆辙）。**禁止**靠往建筑白名单补词（“水泥”等）来修本类误拒——那是把语义判定降级成字符串模式匹配的打地鼠第二套 authority。已知边界（诚实记录）：出口门的拦截力 = `_OUT_OF_SCOPE` 黑名单覆盖范围，白名单未覆盖的真他科（如“法语”当前不在 `_OUT_OF_SCOPE`）会 fall-through 放行，真正的科目语义守门由主 LLM 的建筑 KB grounding 承担，出口关键词门只做高置信他科止血。回归见 `tests/agents/question/test_coordinator_subject_scope_gate.py`。
    - 2026-06-29 补充（goal2+3 出题 forward 不可达深收口 / 生成器科目锁 + 单一 topic normalizer + p11 罐头 fall-through；严格顺序不可乱，S3 patch-spiral 已证逐 gate 打点=灾难）：
      1. **生成器科目锁（地基）**：`prompts/zh/generator.yaml` 的 Generator system prompt 是单一权威，必须锁定"只能出建筑实务（一建建筑）考点题，锚点为空/残缺/像动作词时也只出建筑通用考点题，绝不出非建筑（地理/历史/常识/英语等）题"。lightweight 路径**禁止丢 user_topic**（`generator.py` 旧 `user_topic="(lightweight anchor only)"` 占位已删），lightweight single/batch prompt 都注入用户主题 + 科目硬规则。→ 生成器结构上不可能出非建筑垃圾题（S3 路线③实证：松门没科目锁→出"中国最长河流/四大发明" 3/3）。**红线：松任何门前必须先有科目锁，否则出垃圾。**
      2. **单一 topic normalizer**：噪声练题串（"出一道流水施工的单选题考考我"=action+题型+intent 后缀）必须经唯一 normalizer `AgentCoordinator._derive_lightweight_anchor_label` 清洗成核心建筑 topic（domain_status==construction），**禁止**派生出动作词碎片当 topic（旧 `_extract_explicit_lightweight_topic_label` 的 `考(?!我|点|试)` 把"考考我"误抽成"考我"，丢了"流水施工"，已加 `考` 到否定前瞻）。lightweight 的 RAG anchor / concentration / anchor-block / 生成器 4 处都收敛到这单一 normalizer，不另起第二 topic decider。
      3. **p11 罐头 fall-through**：lightweight `_should_block_unresolved_lightweight_anchor` would-block 时，先经 `_resolve_practice_topic_with_context`（同一 normalizer，本轮 topic→否则从 `history_context` 继承已确立建筑考点）；继承到则重解析 anchor + 生成（`lightweight_batch_fallback=anchor_inherited_from_context`），**只有真冷启动（消息与上下文都无 topic）才落 needs-anchor 罐头**。能 fall-through 是因为科目锁已保证不出垃圾——罐头从"grounding 失败的诚实兜底"降级为"仅真无 topic 时"。回归见 `tests/agents/question/test_generator_subject_lock.py` / `test_lightweight_topic_normalizer.py` / `test_practice_topic_context_inheritance.py`。
36. 完整 free-text MCQ 覆盖旧 active question 的 state-demotion guard 只能在当前用户消息自带题干/选项 surface 时生效；“我选A，请按选择题批改”这类依赖已有 active question 的提交不得被当成新完整题面，也不得因此清空 explicit / candidate question authority。显式练题请求在无可解析提交后应先于 deterministic follow-up guard 处理，避免“再给我几道题，等我做完再批改”被“批改”等词误归为旧题追问。
37. `mobile` adapter 的 `MobileStartTurnRequest.grading_engine_runtime_shadow` / `grading_engine_runtime_shadow_engine` 只是把鲁班 v1 grading-engine runtime-shadow 的 opt-in hint 透传进 `request_config`（由 `TurnRuntimeManager` 的 config allowlist 接收，默认引擎 `deepseek_fast`）；它只是 hint passthrough，不得在 adapter 中决定 capability、不得改写评分 authority、不得开启 production default。runtime shadow 的实际启用、cohort 限定与 fail-closed 仍由 construction_grading runtime 与既有 env / registry 开关单点治理（production default 默认 OFF）。
38. 用指示代词引用某道既往真题并索要其官方答案（例如“2018年那道关于基坑监测的题，答案是啥来着，直接发我”“就那道很经典的基坑题，别问了直接给”）即使没有出现“真题/题库/第X题”字样，只要本轮没有题卡对象 / 题干 / 选项，也属于 `is_low_information_exam_query` 低信息无锚请求，必须 clarify，绝不允许 TutorBot / RAG fast policy 凭描述凭空输出“标准答案 / 采分点”。当本轮已有可解析 active question 时，经 `_low_information_query_can_use_active_question` 逃逸到 `question_review` 按 active question authority 回答，不在此列。（R3-21 / R3-22 修复，2026-06-06）
39. `/api/v1/ws` adapter 可以在 schema 校验和 auth 绑定后注入内部 `_authenticated_user_id`，turn runtime 只能把它投影为服务端可信 metadata，供受控 shadow / cohort gate 读取。该字段不是公开 capability request config，客户端不得传入或覆盖；capability 的 M35 shadow visibility gate 只能读取该服务端认证 identity，不得用 `billing_context.user_id`、`config.user_id`、display learner id、wallet id 或其它 request-shaped 字段授权真实学员进入 qa/test/operator shadow cohort。`billing_context` 仍只服务 wallet/member/learning 业务上下文与会话 owner scope，不能成为 capability shadow 可见性 authority。
40. 判分入口不得以"缺少标准答案"拒绝判分（2026-06-11 根因修复，见 `docs/plan/评分引擎与金标工件/2026-06-11-open-world-grading-and-cross-capability-context-continuity.md`）。当 MCQ 提交经 `_recover_missing_mcq_authority` 三路兜底（active_object hidden authority → `grading_key.correct_answer` 提升 → questions_bank metadata 匹配）仍取不到标准答案时，`deep_question` 必须降级为开放世界判分：缺 authority 条目的确定性 `is_correct`/`score`/llm_judge 占位 `construction_grading_result` 清空为待裁决状态，由 RAG-grounded `SubmissionGraderAgent` 基于检索证据与专业推理裁决并明确判定学员答案，trace 写 `question_authority_source=open_world` / `correct_answer_present=false`。开放世界裁决不是题库标准答案 authority：表述必须用"依据教材/规范判定"，不得声称"题库标准答案/真题官方答案"，不得写 governed release-truth 分数（`answer_key_authority` 仍按 §硬约束 24/35 的诚实溯源链路），已恢复 authority 的条目仍按确定性判分。本条不放松出题侧 authority 纪律（§硬约束 25/26/35 不变），也不影响 lifecycle 对"无 active question 的裸答案提交"的 clarification 裁决（§硬约束 27）。
41. `mobile` adapter 的认证后 LLM / 重计算端点（assessment create / submit / explain、learning-brain projection、conversations 批量）必须挂 `route_rate_limit` burst（昂贵 LLM 生成端点另加 daily 预算），具体额度与治理见 [contracts/turn.md](./turn.md) §硬约束 21；adapter 的 bcrypt 认证路径（登录 / 注册 / 重置密码）、assessment HTTP adapter 中会访问 Supabase / session repository / learner-state 的同步 service 调用、以及 capability turn 终结化副作用不得在事件循环线程做阻塞调用（见 turn.md §硬约束 22）。限流、请求体上限与线程化只是边界防护，不得改变 capability 路由、判分 authority 或 turn 状态机语义。

42. 判分态收口（单一权威，2026-06-24，task#20 簇A）：`question_lifecycle_skills` 的确定性快路径钉 grading scene（`mcq_grading` / `case_grading`）只对 **HIGH 置信作答**生效——显式提交、答案是消息主导 payload，经 `submission_confidence` 判定。LOW 置信作答（答案被埋在试探"我猜"、保留"你先别判"、回指或质疑断言里）**不**钉 grading scene，落下游 `question_review` → orchestrator 的 LLM 语义复核决定是否真在交卷，杜绝非答题轮被凭空判分（ground-truth g2/g5 SEV-1，2026-06-24 学员军团 live 复现，收口后 live GO 6/6）。这不是新决策点：`submission_confidence` 复用同一 `resolve_submission_attempt`，只在其上加正向置信维度；LOW 时 fall through 既有 `question_review` 路径，不新增第二套判分态 authority。与 §硬约束 40 协同：HIGH 置信→真作答必判（不拒判）；LOW 置信→不凭空判（不误判），两侧都收口到单一 `resolve_submission_attempt` + 下游 LLM 语义复核。配套 open-world 判分硬事实纪律（闸-4，2026-06-24）：缺参考答案时 `SubmissionGraderAgent` 开放裁决 directive 中"专业推理"只用于判断逻辑（哪个对、为什么），不得杜撰硬事实——具体数值 / 阈值 / 比例 / 规范编号 / 条文号 / 法定术语的判定依据仍受单一 `GROUNDING_CLAUSE`（`deeptutor/core/grounding.py`）约束，只能引用 grounding 证据实际给出的，证据没有的一律不得当依据写出（改定性说明或标待教材核实）；directive 不得局部重开"或专业推理"逃生口抵触该单一权威（防"二级资质合同额4000万"类运行时现编，live 3 轮验证编造消除）。
    - 2026-06-25 补充（canonical clarification terminal truth）：`turn_semantic_decision.next_action == "ask_clarifying_question"` 是终端控制信号，不是可被 `question_review` / `practice_generation` lifecycle fallthrough、preselected `deep_question` 或 full-submission fallback 覆盖的弱提示。orchestrator 在 active question / active object 场景下解析出该 decision 时必须按 `semantic_route=chat` 回到 TutorBot/chat clarification，不得继续路由到 `deep_question` 生成题、讲评或判分；`deep_question` 若因兼容旁路收到同一 canonical decision，也必须 fail-closed 输出 clarification，保持 active object 只读，不得调用 grading/generation 模板、不得泄标准答案或构造 rubric。回归见 `tests/runtime/test_orchestrator_autoroute.py::test_lifecycle_question_review_routes_clarification_decision_to_chat` 与 `tests/capabilities/test_deep_question_mcq_full_submission.py::test_deep_question_honors_clarification_decision_before_full_mcq_fallback`。
    - 2026-06-25 补充（active assessment object authority / 修复窗口 B）：完整自包含 MCQ（题干 + 当前选项 surface + 明确学员答案）必须进入 MCQ grading authority，即使上一层 lifecycle scene 被旧上下文标成 `case_grading` 也不得落入案例题逐采分点 fallback；“答案改成 D / 上一个选项改成 D / 刚才那题答案改成 D”是对当前 active assessment object 的 submission patch，必须由同一 `resolve_submission_attempt` / `submission_confidence` authority 判为 HIGH 置信作答，而“如果选 D 对不对”这类点名选项追问仍保持 `question_review`；active object 存在时，粘住的 `deep_question` preselect 不得把非答题知识问答（例如保修期知识问答）带入阅卷终端，orchestrator 必须在确认本轮不是提交、追问、出题或题目审查后降级到默认 TutorBot/chat。回归见 `tests/capabilities/test_deep_question_mcq_full_submission.py::test_deep_question_prefers_self_contained_mcq_over_case_fallback`、`tests/services/test_question_followup.py::test_submission_confidence_high_for_explicit_answer_revision`、`tests/services/test_question_lifecycle_scene_derivation.py::test_active_object_explicit_answer_revision_returns_mcq_grading` 与 `tests/runtime/test_orchestrator_autoroute.py::test_orchestrator_demotes_deep_question_preselect_for_non_answer_knowledge_question`。
    - 2026-06-25 补充（current submission projection authority）：完整自包含 MCQ / case submission 投影为 `question_followup_context` 的 helper 归属 `question_lifecycle_skills` 服务层，供 `turn_runtime` 在 capability 前绑定当前题面，也供 `deep_question` 作为薄委托读取；`deep_question` 不得保留第二份 MCQ / case full-submission parser，也不得把旧 active object / old candidate context 的 `question_id`、`correct_answer` 或 scoring authority 合并到不匹配的当前题面。
    - 2026-06-25 补充（current submission scene authority）：完整自包含 MCQ / case submission 不只要成为当前 `question_followup_context` / `active_object`，还必须在 capability 前原子绑定 terminal lifecycle scene。`turn_runtime` 只能从 `question_lifecycle_skills` 投影出的当前 context/action 盖 `mcq_grading` / `case_grading` metadata；它仍是薄 wrapper，不得新增 scene router、不得评分、不得改写 capability 结果。完整 MCQ 若显式携带 `标准答案` / `正确答案` / `参考答案`，该 marked answer 属于当前题面 reference；`我的答案` 绝不能被当成标准答案。完整案例自然格式（如 `案例题...问...我的答案`）属于 full case answer submission，不得被 `is_low_information_exam_query` 抢成缺锚澄清。回归见 `tests/api/test_unified_ws_turn_runtime.py::test_start_turn_stamps_inline_full_mcq_scene_and_marked_answer`、`tests/api/test_unified_ws_turn_runtime.py::test_start_turn_promotes_full_new_case_over_stale_active_object`、`tests/capabilities/test_deep_question_mcq_full_submission.py::test_pasted_single_choice_carries_marked_standard_answer` 与 `tests/services/test_question_lifecycle_scene_derivation.py::test_full_case_question_with_inline_ask_and_answer_layout_returns_case_grading`。
    - 2026-06-26 补充（submission/follow-up object authority）：完整自包含案例题若同一消息同时出现 `我的答案/作答` 与 `标准答案/正确答案/参考答案`，`question_lifecycle_skills` 必须把学生答案与 marked reference 原子拆开：`user_answer` 只能保存 learner answer，marked reference 只能进入既有 `correct_answer` / `reference_answer` 字段，绝不能混入 `user_answer` 让阅卷器把标准答案当学生命中。已有 active MCQ 下，用户点名不存在的选项字母（例如 A-D 题里问“如果我选 E 对不对”）属于 `question_review` follow-up，不是 submission，也不能落回 generic TutorBot 让 LLM 编造 E 选项内容；可见回答必须以当前题 `options` 为唯一 authority 说明该选项不存在。已有 active question 下索要“正确答案/标准答案/参考答案”（即使中间插入“到底”等词）也属于 `question_review`，不得落到 TutorBot 泛化回答从对话历史猜测学生曾选过哪些选项。回归见 `tests/services/test_question_lifecycle_skills.py::test_full_case_submission_keeps_marked_reference_out_of_learner_answer`、`tests/services/test_question_lifecycle_scene_derivation.py::test_active_object_invalid_option_challenge_stays_question_review`、`tests/services/test_question_lifecycle_scene_derivation.py::test_active_object_correct_answer_request_returns_question_review` 与 `tests/core/test_deep_question_submission_grading.py::test_deep_question_invalid_option_followup_uses_current_options_not_fabrication`。
    - 2026-06-26 补充（control-plane Task 3 / 单一 capability decider，已授权移除 router killswitch）：`_select_capability_after_lifecycle` 的 lifecycle 后兜底**只有一个 capability decider —— canonical semantic router**。旧的启发式 decider `_select_legacy_capability`（及它支撑的 "router 出 bug 降级到启发式" killswitch）已删除：`DEEPTUTOR_SEMANTIC_ROUTER_ENABLED` 生产默认 True、scope 默认 all，该启发式生产从未启用，属休眠的第二套路由 authority。shadow / primary / disabled / scope-excluded 四条兜底分支现在都先解析 canonical `turn_semantic_decision` 再按它路由（`route_to_grading/route_to_generation/route_to_followup_explainer → deep_question`（带提交/出题 context prep）、`chat 系 → 默认 chat/tutorbot`）。§6 安全带不变：unresolved-switch follow-up（`is_unresolved_switch_followup`）仍降级到上下文连续主 LLM（`_default_chat_capability`），永不进 `deep_question` 结构化切换解析器 fail-closed；`mcq_grading` preselect bypass 守卫仍先于本兜底运行。None-route（resolver 给不出可映射 decision 的退化态）默认到上下文连续 chat/tutorbot 执行器，**不**再回落启发式。`orchestrator_business_decision_count` 因移除一个 routing 决策引擎而下降。回归见 `tests/runtime/test_orchestrator_autoroute.py`（`test_none_route_falls_through_to_context_continuous_chat` / `test_disabled_router_routes_via_canonical_not_legacy_heuristic` / `test_shadow_mode_routes_via_canonical_not_legacy` / `test_select_legacy_capability_is_removed`）与 `tests/runtime/test_orchestrator_semantic_router.py`（`test_orchestrator_shadow_mode_routes_via_canonical_decision` / `test_orchestrator_disabled_mode_routes_via_canonical_decision` / `test_orchestrator_scope_excluded_routes_via_canonical_decision`）。
    - 2026-06-26 补充（pre-stamped grading scene revalidation / explicit reference authority）：`question_lifecycle_scene=mcq_grading|case_grading` 的 pre-capability 盖章不是无条件 authority；`resolve_question_lifecycle_scene_decision` 必须在当前 turn 重新验证它对应 HIGH 置信提交或完整自包含提交。`总结/复盘/不要展示内部证据/输出 evidence source 标题` 等 meta、history 或 public-output 请求即使上一轮仍有 active case，也不得继承 sticky `case_grading`。active subjective submission 的提交前缀与题型集合必须覆盖 `作答/我的作答/学生作答` 与 `case_study/case_background/calculation/open_ended`，但这些解析只能服务同一个 `resolve_submission_attempt`，不得在 lifecycle 外另建主观题提交 parser。当前完整案例显式携带 `标准答案/正确答案/参考答案` 时，该 marked reference 是 V1 case grading 的本轮 reference answer authority；TutorBot V1 bridge / `deep_question` 只能把它作为 `correct_answer/reference_answer` 输入鲁班 V1，不得因 exact/followup 缺 reference 而退回 `derived_from_stem` 覆盖本轮 marked reference。回归见 `tests/services/test_question_lifecycle_scene_derivation.py::test_scene_decision_ignores_stale_pre_capability_case_grading_fact`、`tests/services/test_question_followup.py::test_resolve_submission_attempt_keeps_case_exit_or_study_plan_as_followup`、`tests/tutorbot/test_agent_loop_case_rubric_v1.py::test_build_v1_case_ctx_uses_current_full_submission_marked_reference` 与 `tests/core/test_deep_question_submission_grading.py::test_deep_question_full_case_submission_marks_current_reference_answer`。

32. `ChatOrchestrator` 在路由决策点就地把本轮 routing 消息写入 `context.metadata["semantic_router_captured_input"]`，作为 semantic-router 决策遥测的 in-place 输入捕获（供 turn 完成时落 `semantic_router_telemetry` internal 事件，免事后 session+time join）。这是**纯 additive、只读观测**，**绝不改变任何 capability 路由判决**（behavior-preserving）；任何 capability/路由判定不得读取或依赖该字段做决策。2026-08-11 起该遥测 payload（`authority_probe_schema_version` 2）新增三键 `lifecycle_final` / `llm_scene_candidate` / `scene_divergence`（确定性管线最终判定 vs LLM 语义判定的分歧形态，路由收权 shadow 度量），仍属本条款同一 observe-only 合同：只写不读，无任何代码回读。

43. capability / TutorBot / provider 可以把 `llm_stream_telemetry`、provider timing、first-useful-content timing、`latency_timeline`、`latency_max_stall` 等写入 runtime/session metadata，供 `turn_runtime` terminal observer 和内部诊断消费；这些字段不得进入 `/api/v1/ws` outbound public copy，不属于 capability public result contract，也不得成为客户端展示、capability route、评分、计费或 learner-state authority。公开 WS redaction 只能删除 public copy 中的内部观测字段，不能改写持久化 turn event、canonical final answer 或 terminal observer truth。

44. 出题意图仲裁收权（2026-08-10，F1 生产事故根因修复）：「这轮是不是出题轮」的确定
    性判据全链只允许一把出题形状尺（`looks_like_practice_generation_request`）+ 一把追
    问形状尺（`looks_like_question_followup`，已否定感知：否定短语内的追问词如「不要提
    前给答案和解析」不构成追问形状），禁止任何消费点自建第二套出题/追问意图词表或显式
    闸（原 `_has_explicit_practice_generation_intent` 及其「解析」否定词表已退役，不得
    复活）。仲裁规则唯一：**无歧义单形状 → 确定性快路径直判；出题+追问双形状 → 一律
    fall-through 语义层（LLM interpreter / scene-LLM）裁决，确定性层不得抢答**。scene
    权威 `practice_generation` 分支与 orchestrator 的 followup 让位通道是该规则的上下游：
    让位通道只接收真语义层产物，被确定性补位伪装的追问判定不得再流入。批量作答 arity
    单向闸：消息可解析的带序号作答段数 > 上下文可判题数时归 `ambiguous`
    （`requires_question_index` + `arity_mismatch`），绝不蒸馏成单字母判 stale 单题；答少
    于题是合法部分提交不受拦。回归钉：`tests/services/test_semantic_router.py`（F1 原文
    快路径钉）、`tests/services/test_question_followup.py`（arity 双向钉+否定感知钉）、
    `tests/services/test_question_lifecycle_scene_derivation.py`（双形状 defer/作答优先钉）。

45. 确定性层三职责红线（2026-08-11，owner 拍板；元病「防御被实现成终局」的总纲,
    §44 是它在出题意图上的实例）：确定性代码在会话链路上只许承担三类职责——
    ①**真值供给**（题库/rubric/编译资产等 governed 事实,模型不得现编的内容）；
    ②**副作用闸**（计费、写库、发布、额度等动作前的门,门只拦动作不改写意图）；
    ③**预算与观测**（超时、并发上限、typed marker、遥测）。三职责之外,**任何用
    词表/正则/模板在语义层面判定或改写用户意图、或替主 LLM 生成学员可见回答的
    确定性逻辑,一律违规**：新增即 review blocker；存量属清偿对象,清偿方向 =
    确定性判定降级为高置信快路径或 observe-only 提示,终局权交还带完整上下文的
    主 LLM（fall-through,绝不模板兜底）。判据三问：这段代码在理解用户吗？它能
    无痕作废上游语义层已做对的结果吗？它在替模型对用户说话吗？任一为是即越权。
    路由收权迁移纪律：从确定性管线切向 LLM tool-choice 必须先 shadow 度量分歧率
    （observe-only）,有数据后灰度切换,禁止信仰式一步切。
    归属诚实条款（2026-08-11 live 防编造钉实证）：blocked/低信息真题查询轮 fall-through
    主 LLM 后,若常规检索命中相似题,回答必须如实标注该题的真实来源/卷次（以检索元数据
    为准）并明说无法确认与学员点名题号的对应关系；把相似题答案以确定口吻冒充点名的
    某年某题=归属不诚实,与内容编造同级违规。prompt hint 单一权威 =
    `build_question_lifecycle_clarification_prompt_hint`,回归钉在
    `tests/services/test_question_lifecycle_skills.py`。
    数据面强制（2026-08-11,hint 复钉 1/2 红实证 prompt≠terminal authority 后收口）：
    low_information_exam_query 锁权轮,rag 工具结果在喂给模型前必须经
    `redact_question_bank_answer_keys` 剥掉题库【答案】/【解析】段（题面/选项保留）——
    模型没有答案钥匙即无法确定性冒充;消费点与 exact 展示归一化同一汇点
    （tutorbot loop rag 工具结果后处理）,不得新增第二处 rag 结果改写点。

## Schema

- 机器可读 schema：`deeptutor/capabilities/request_contracts.py`
- 当前已导出的 schema：`CAPABILITY_REQUEST_SCHEMAS`

## 必测项

- `tests/runtime/test_orchestrator_autoroute.py`
- `tests/api/test_unified_ws_turn_runtime.py`
- `tests/api/test_mobile_router.py`
